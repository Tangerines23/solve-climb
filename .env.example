# Supabase (??? ??)
# Supabase ???? ? Project Settings ? API?? ?? ? ?????.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# CI / DB ??????? (GitHub Secrets ?? ?? db push ?)
SUPABASE_ACCESS_TOKEN=
SUPABASE_DB_PASSWORD=
# ?? ?? ? 1, ?? ?? ? ??? (????/CI ??)
SUPABASE_USE_POOLER=

# ?? ?? ?? (??)
DEV_HOST=localhost
DEV_PORT=5173

# Sentry (??) ? DSN ?? ? ?? ?? ???
VITE_SENTRY_DSN=

# Sentry ??? ???? (vite.config.js, ??)
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=

# Socket (훅 + MCP 돌려쓰기: socket.dev → Settings → API Tokens 에서 발급 후 여기와 Cursor MCP Socket env 에 동일 값)
# SOCKET_API_KEY=
