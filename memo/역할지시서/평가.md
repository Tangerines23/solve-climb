1. 라우터 설정 (페이지 접근 경로)
MathQuizPage.tsx라는 새 페이지 파일을 만들라고 지시했지만, **이 페이지로 어떻게 접근하는지(라우팅)**에 대한 지시가 빠져있습니다. 주니어 개발자는 페이지를 다 만들고 나서 "이 페이지가 왜 안 뜨죠?"라고 물을 수 있습니다.

문제점: react-router-dom 스택을 사용하지만, App.tsx나 main.tsx 등 라우터 설정 파일(<Routes>, <Route>)에 새 페이지를 등록하는 지시가 없습니다.

개선 방안: 6. AI에 대한 산출물(Output) 요구의 구현 단계에 라우터 등록 단계를 명시적으로 추가합니다.

예시 (6-2 항목 수정):

App.tsx(또는 라우터 설정파일)에 MathQuizPage를 임포트하고 /math 경로에 라우트(Route)를 추가.

handleSubmit 함수 구현...


2. 레이아웃 및 스타일링 가이드라인
TDS 컴포넌트(Page, Text, Input, Button)를 사용하라고 지시했지만, 이 컴포넌트들을 **어떻게 배치할지(Layout)**에 대한 지침이 없습니다.

문제점: 주니어 개발자가 UI 일관성을 깨뜨리는 <div> 태그나 인라인 스타일(style="...")을 임의로 사용하여 컴포넌트 간 간격을 조정할 수 있습니다. (TDS의 철학에 어긋날 수 있음)

개선 방안: TDS가 제공하는 레이아웃 전용 컴포넌트(예: <Spacing size={16}> 또는 <Flex direction="column"> 등)를 사용하도록 명시합니다.

예시 (5-1 항목 추가):

레이아웃 (TDS):

컴포넌트 수직 정렬 및 간격 조정 시, HTML <div> 태그 대신 TDS의 레이아웃 컴포넌트(예: Spacing, FlexBox 등)를 사용하여 일관성을 유지한다.


3. 확장성을 위한 로직 분리 (유틸리티)
지금은 "1~9 사이의 랜덤 정수" 로직이 간단하지만, 첫 번째 프롬프트(Gemini CLI)의 내용처럼 이 프로젝트는 "난이도 상승(예: 자릿수 증가)"을 목표로 합니다.

문제점: handleSubmit과 숫자 생성 로직이 MathQuizPage.tsx 안에 모두 포함되면, 나중에 난이도 조절 로직을 추가할 때 이 페이지 컴포넌트가 매우 복잡해지고(비대해지고) 다른 퀴즈(예: 일본어 퀴즈)에서 이 로직을 재사용하기 어렵습니다.

개선 방안: TL의 역할로서, 당장 필요하지 않더라도 향후 재사용과 확장을 고려하여 핵심 로직(랜덤 숫자 생성)을 별도의 유틸리티 함수로 분리하도록 지시합니다.

예시 (5-2 항목 수정):

핵심 로직 (handleSubmit 함수):

...

유틸리티 분리:

랜덤 숫자 생성 로직(예: function generateRandomNumber(max))은 MathQuizPage.tsx 내부에 두지 말고, src/utils/math.ts (신규 생성) 파일로 분리하여 임포트(import)해 사용한다. (향후 난이도 조절을 위한 확장성 고려)