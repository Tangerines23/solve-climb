diff --git a/knip_output.txt b/knip_output.txt
new file mode 100644
index 0000000..715622c
Binary files /dev/null and b/knip_output.txt differ
diff --git a/scratch/fix_all_imports.py b/scratch/fix_all_imports.py
new file mode 100644
index 0000000..12fd2db
--- /dev/null
+++ b/scratch/fix_all_imports.py
@@ -0,0 +1,40 @@
+import os
+import re
+
+src_dir = r'c:\Users\ghkdd\gemini-projects\solve-climb\src'
+
+replacements = [
+    (r"(['\"])@/constants/tiers(['\"])", r"\1@/features/quiz\2"),
+    (r"(['\"])@/utils/MathProblemGenerator(['\"])", r"\1@/features/quiz/utils/MathProblemGenerator\2"),
+    (r"(['\"])@/utils/StatsProblemGenerator(['\"])", r"\1@/features/quiz/utils/StatsProblemGenerator\2"),
+    (r"(['\"])@/utils/LogicProblemGenerator(['\"])", r"\1@/features/quiz/utils/LogicProblemGenerator\2"),
+    (r"(['\"])@/utils/AlgebraAdvancedGenerator(['\"])", r"\1@/features/quiz/utils/AlgebraAdvancedGenerator\2"),
+    (r"(['\"])@/utils/CalculusProblemGenerator(['\"])", r"\1@/features/quiz/utils/CalculusProblemGenerator\2"),
+    (r"(['\"])@/utils/CSProblemGenerator(['\"])", r"\1@/features/quiz/utils/CSProblemGenerator\2"),
+]
+
+for root, dirs, files in os.walk(src_dir):
+    for file in files:
+        if file.endswith(('.ts', '.tsx')):
+            path = os.path.join(root, file)
+            try:
+                # Try UTF-8 first, then CP949
+                try:
+                    with open(path, 'r', encoding='utf-8') as f:
+                        content = f.read()
+                    encoding = 'utf-8'
+                except UnicodeDecodeError:
+                    with open(path, 'r', encoding='cp949') as f:
+                        content = f.read()
+                    encoding = 'cp949'
+                
+                new_content = content
+                for pattern, repl in replacements:
+                    new_content = re.sub(pattern, repl, new_content)
+                
+                if new_content != content:
+                    with open(path, 'w', encoding=encoding) as f:
+                        f.write(new_content)
+                    print(f"Fixed: {path}")
+            except Exception as e:
+                print(f"Error processing {path}: {e}")
diff --git a/scratch/fix_tiers_test.py b/scratch/fix_tiers_test.py
new file mode 100644
index 0000000..1d408e0
--- /dev/null
+++ b/scratch/fix_tiers_test.py
@@ -0,0 +1,32 @@
+import os
+
+path = r'c:\Users\ghkdd\gemini-projects\solve-climb\src\features\quiz\constants\__tests__\tiers.test.ts'
+
+with open(path, 'r', encoding='cp949') as f:
+    lines = f.readlines()
+
+new_lines = []
+skip = False
+for line in lines:
+    if 'calculateTierSync,' in line:
+        continue
+    if "describe('calculateTierSync', () => {" in line:
+        skip = True
+        continue
+    if skip and '  });' in line:
+        # Check if it's the end of calculateTierSync block by looking at the next line if possible
+        # Actually, let's just find the next describe block or end of file
+        pass
+    
+    if skip:
+        # We need a better way to find the end of the block.
+        # Since I know the next block is describe('getNextTierInfo', ...), I'll look for that.
+        if "describe('getNextTierInfo', () => {" in line:
+            skip = False
+            new_lines.append(line)
+        continue
+    
+    new_lines.append(line)
+
+with open(path, 'w', encoding='cp949') as f:
+    f.writelines(new_lines)
diff --git a/src/features/quiz/__tests__/CategorySelectPage.test.tsx b/src/features/quiz/__tests__/CategorySelectPage.test.tsx
index ac51011..0afbf18 100644
--- a/src/features/quiz/__tests__/CategorySelectPage.test.tsx
+++ b/src/features/quiz/__tests__/CategorySelectPage.test.tsx
@@ -1,28 +1,28 @@
-import { describe, it, expect, vi, beforeEach } from 'vitest';
+癤퓁mport { describe, it, expect, vi, beforeEach } from 'vitest';
 import { render, screen, fireEvent } from '@testing-library/react';
-import { CategorySelectPage } from '../pages/CategorySelectPage';
+import { CategorySelectPage } from '@/features/quiz/pages/CategorySelectPage';
 import { BrowserRouter } from 'react-router-dom';
-import { useLevelProgressStore } from '../stores/useLevelProgressStore';
-import { useNavigationContext } from '../../../hooks/useNavigationContext';
-import { useFavoriteStore } from '../../../stores/useFavoriteStore';
-import { useDebugStore } from '../../../stores/useDebugStore';
+import { useLevelProgressStore } from '@/features/quiz/stores/useLevelProgressStore';
+import { useNavigationContext } from '@/hooks/useNavigationContext';
+import { useFavoriteStore } from '@/stores/useFavoriteStore';
+import { useDebugStore } from '@/stores/useDebugStore';
 import '@testing-library/jest-dom/vitest';
 
 // Mock dependencies
-vi.mock('../stores/useLevelProgressStore');
-vi.mock('../../../hooks/useNavigationContext');
-vi.mock('../../../stores/useFavoriteStore');
-vi.mock('../../../stores/useDebugStore');
-vi.mock('../../../config/app', () => ({
+vi.mock('@/features/quiz/stores/useLevelProgressStore');
+vi.mock('@/hooks/useNavigationContext');
+vi.mock('@/stores/useFavoriteStore');
+vi.mock('@/stores/useDebugStore');
+vi.mock('@/config/app', () => ({
   APP_CONFIG: {
     CATEGORIES: [
       {
         id: 'cat1',
         name: 'Category 1',
-        symbol: '??',
+        symbol: '??',
         color: 'blue',
         mountainId: 'math',
-        icon: '??',
+        icon: '??',
       },
     ],
     LEVELS: {
@@ -41,14 +41,14 @@ vi.mock('../../../config/app', () => ({
     },
   },
 }));
-vi.mock('../components/ClimbGraphic', () => ({
+vi.mock('@/features/quiz/components/ClimbGraphic', () => ({
   ClimbGraphic: () => <div data-testid="climb-graphic" />,
 }));
-vi.mock('../../../components/FooterNav', () => ({
+vi.mock('@/components/FooterNav', () => ({
   FooterNav: () => <div data-testid="footer-nav" />,
 }));
-vi.mock('../../../components/Header', () => ({ Header: () => <div data-testid="header" /> }));
-vi.mock('../components/TopicHeader', () => ({
+vi.mock('@/components/Header', () => ({ Header: () => <div data-testid="header" /> }));
+vi.mock('@/features/quiz/components/TopicHeader', () => ({
   TopicHeader: ({ title, onBack }: any) => (
     <div data-testid="topic-header">
       <h1>{title}</h1>
@@ -77,7 +77,7 @@ describe('CategorySelectPage', () => {
         {
           id: 'cat1',
           name: 'Category 1',
-          symbol: '??',
+          symbol: '??',
           color: 'blue',
         },
       ],
@@ -125,7 +125,7 @@ describe('CategorySelectPage', () => {
       </BrowserRouter>
     );
 
-    expect(screen.getByText('Mathematics - 遺꾩빞 ?좏깮')).toBeInTheDocument();
+    expect(screen.getByText('Mathematics - ?브쑴鍮??醫뤾문')).toBeInTheDocument();
     expect(screen.getByText('Category 1')).toBeInTheDocument();
   });
 
@@ -162,7 +162,7 @@ describe('CategorySelectPage', () => {
       </BrowserRouter>
     );
 
-    const favButton = screen.getByLabelText('利먭꺼李얘린 異붽?');
+    const favButton = screen.getByLabelText('筌앸Þ爰쇽㎕?섎┛ ?곕떽?');
     fireEvent.click(favButton);
 
     expect(defaultFavoriteStore.addFavorite).toHaveBeenCalled();
@@ -181,6 +181,6 @@ describe('CategorySelectPage', () => {
       </BrowserRouter>
     );
 
-    expect(screen.getByText('?섎せ???묎렐?낅땲??)).toBeInTheDocument();
+    expect(screen.getByText('??롢걵???臾롫젏??낅빍??)).toBeInTheDocument();
   });
 });
diff --git a/src/features/quiz/__tests__/GameFlowSection.test.tsx b/src/features/quiz/__tests__/GameFlowSection.test.tsx
index 60ed0d3..4449a9e 100644
--- a/src/features/quiz/__tests__/GameFlowSection.test.tsx
+++ b/src/features/quiz/__tests__/GameFlowSection.test.tsx
@@ -1,23 +1,23 @@
-import { describe, it, expect, vi, beforeEach } from 'vitest';
+癤퓁mport { describe, it, expect, vi, beforeEach } from 'vitest';
 import { render, screen, waitFor } from '@testing-library/react';
-import { GameFlowSection } from '../../../components/debug/GameFlowSection';
-import { supabase } from '../../../utils/supabaseClient';
+import { GameFlowSection } from '@/components/debug/GameFlowSection';
+import { supabase } from '@/utils/supabaseClient';
 import type { SupabaseClient, Session } from '@supabase/supabase-js';
-import { useQuizStore } from '../stores/useQuizStore';
+import { useQuizStore } from '@/features/quiz/stores/useQuizStore';
 
 // Mock dependencies
-vi.mock('../stores/useQuizStore', () => ({
+vi.mock('@/features/quiz/stores/useQuizStore', () => ({
   useQuizStore: vi.fn(),
 }));
 
-vi.mock('../../../stores/useGameStore', () => ({
+vi.mock('@/stores/useGameStore', () => ({
   useGameStore: vi.fn(() => ({
     score: 0,
     combo: 0,
   })),
 }));
 
-vi.mock('../../../utils/supabaseClient', () => ({
+vi.mock('@/utils/supabaseClient', () => ({
   supabase: {
     auth: {
       getSession: vi.fn(),
@@ -27,7 +27,7 @@ vi.mock('../../../utils/supabaseClient', () => ({
   },
 }));
 
-vi.mock('../utils/quizGenerator', () => ({
+vi.mock('@/features/quiz/utils/quizGenerator', () => ({
   generateQuestion: vi.fn(),
 }));
 
@@ -69,14 +69,14 @@ describe('GameFlowSection', () => {
   it('should render game flow section title', async () => {
     render(<GameFlowSection />);
     await waitFor(() => {
-      expect(screen.getByText(/寃뚯엫 ?뚮줈??)).toBeInTheDocument();
+      expect(screen.getByText(/野껊슣?????쨮??)).toBeInTheDocument();
     });
   });
 
   it('should handle session loading', async () => {
     render(<GameFlowSection />);
     await waitFor(() => {
-      expect(screen.getByText(/寃뚯엫 ?뚮줈??)).toBeInTheDocument();
+      expect(screen.getByText(/野껊슣?????쨮??)).toBeInTheDocument();
     });
   });
 });
diff --git a/src/features/quiz/__tests__/LevelSelectPage.test.tsx b/src/features/quiz/__tests__/LevelSelectPage.test.tsx
index c36e0b5..a32b1da 100644
--- a/src/features/quiz/__tests__/LevelSelectPage.test.tsx
+++ b/src/features/quiz/__tests__/LevelSelectPage.test.tsx
@@ -1,19 +1,19 @@
-import { describe, it, expect, vi, beforeEach } from 'vitest';
+癤퓁mport { describe, it, expect, vi, beforeEach } from 'vitest';
 import { render, screen, fireEvent, waitFor } from '@testing-library/react';
-import { LevelSelectPage } from '../pages/LevelSelectPage';
+import { LevelSelectPage } from '@/features/quiz/pages/LevelSelectPage';
 import { BrowserRouter, useSearchParams } from 'react-router-dom';
-import { useLevelProgressStore } from '../stores/useLevelProgressStore';
-import { useNavigationContext } from '../../../hooks/useNavigationContext';
-import { useFavoriteStore } from '../../../stores/useFavoriteStore';
-import { useDebugStore } from '../../../stores/useDebugStore';
+import { useLevelProgressStore } from '@/features/quiz/stores/useLevelProgressStore';
+import { useNavigationContext } from '@/hooks/useNavigationContext';
+import { useFavoriteStore } from '@/stores/useFavoriteStore';
+import { useDebugStore } from '@/stores/useDebugStore';
 import '@testing-library/jest-dom/vitest';
 
 // Mock dependencies
-vi.mock('../stores/useLevelProgressStore');
-vi.mock('../../../hooks/useNavigationContext');
-vi.mock('../../../stores/useFavoriteStore');
-vi.mock('../../../stores/useDebugStore');
-vi.mock('../../../config/app', () => ({
+vi.mock('@/features/quiz/stores/useLevelProgressStore');
+vi.mock('@/hooks/useNavigationContext');
+vi.mock('@/stores/useFavoriteStore');
+vi.mock('@/stores/useDebugStore');
+vi.mock('@/config/app', () => ({
   APP_CONFIG: {
     WORLD_MAP: {
       World1: 'World 1',
@@ -49,13 +49,13 @@ vi.mock('../../../config/app', () => ({
     },
   },
 }));
-vi.mock('../components/ClimbGraphic', () => ({
+vi.mock('@/features/quiz/components/ClimbGraphic', () => ({
   ClimbGraphic: () => <div data-testid="climb-graphic" />,
 }));
-vi.mock('../../../components/MyRecordCard', () => ({
+vi.mock('@/components/MyRecordCard', () => ({
   MyRecordCard: () => <div data-testid="my-record-card" />,
 }));
-vi.mock('../components/LevelListCard', () => ({
+vi.mock('@/features/quiz/components/LevelListCard', () => ({
   LevelListCard: ({ onLevelClick, onLevelLongPress, onLockedLevelClick }: any) => (
     <div data-testid="level-list-card">
       <button onClick={() => onLevelClick(1)}>Level 1</button>
@@ -66,11 +66,11 @@ vi.mock('../components/LevelListCard', () => ({
     </div>
   ),
 }));
-vi.mock('../../../components/FooterNav', () => ({
+vi.mock('@/components/FooterNav', () => ({
   FooterNav: () => <div data-testid="footer-nav" />,
 }));
-vi.mock('../../../components/Header', () => ({ Header: () => <div data-testid="header" /> }));
-vi.mock('../../../components/Toast', () => ({
+vi.mock('@/components/Header', () => ({ Header: () => <div data-testid="header" /> }));
+vi.mock('@/components/Toast', () => ({
   Toast: ({ message, isOpen }: any) => (isOpen ? <div data-testid="toast">{message}</div> : null),
 }));
 
@@ -160,7 +160,7 @@ describe('LevelSelectPage', () => {
       </BrowserRouter>
     );
 
-    const backButton = screen.getByLabelText('?ㅻ줈 媛湲?);
+    const backButton = screen.getByLabelText('??살쨮 揶쎛疫?);
     fireEvent.click(backButton);
     expect(mockNavigate).toHaveBeenCalledWith('/category-select?mountain=math');
   });
@@ -198,7 +198,7 @@ describe('LevelSelectPage', () => {
       </BrowserRouter>
     );
 
-    const survivalBtn = screen.getByRole('button', { name: /?쒕컮?대쾶 梨뚮┛吏/i });
+    const survivalBtn = screen.getByRole('button', { name: /??뺤뺍??苡?筌?슢?쏉쭪?/i });
     fireEvent.click(survivalBtn);
 
     expect(mockNavigate).toHaveBeenCalledWith(expect.stringContaining('mode=survival'));
@@ -211,7 +211,7 @@ describe('LevelSelectPage', () => {
       </BrowserRouter>
     );
 
-    const nextBtn = screen.getByText('??);
+    const nextBtn = screen.getByText('??);
     fireEvent.click(nextBtn);
 
     await waitFor(() => {
diff --git a/src/features/quiz/__tests__/QuizPage.test.tsx b/src/features/quiz/__tests__/QuizPage.test.tsx
index 54584c7..099b0f5 100644
--- a/src/features/quiz/__tests__/QuizPage.test.tsx
+++ b/src/features/quiz/__tests__/QuizPage.test.tsx
@@ -1,8 +1,8 @@
-import { describe, it, expect, vi, beforeEach } from 'vitest';
+癤퓁mport { describe, it, expect, vi, beforeEach } from 'vitest';
 import { render, screen, fireEvent } from '@testing-library/react';
-import { QuizPage } from '../pages/QuizPage';
+import { QuizPage } from '@/features/quiz/pages/QuizPage';
 import { BrowserRouter } from 'react-router-dom';
-import { useUserStore } from '../../../stores/useUserStore';
+import { useUserStore } from '@/stores/useUserStore';
 
 // Use vi.hoisted to ensure these are initialized before vi.mock
 const { mockQuizStore, mockUserStoreState, mockGameStore } = vi.hoisted(() => {
@@ -13,7 +13,7 @@ const { mockQuizStore, mockUserStoreState, mockGameStore } = vi.hoisted(() => {
       increaseScore: vi.fn(),
       decreaseScore: vi.fn(),
       resetQuiz: vi.fn(),
-      category: '湲곗큹',
+      category: '疫꿸퀣??,
       world: 'World1',
       timeLimit: 10,
       setGameMode: vi.fn(),
@@ -66,11 +66,11 @@ const { mockQuizStore, mockUserStoreState, mockGameStore } = vi.hoisted(() => {
   };
 });
 
-vi.mock('../stores/useQuizStore', () => ({
+vi.mock('@/features/quiz/stores/useQuizStore', () => ({
   useQuizStore: mockQuizStore,
 }));
 
-vi.mock('../../../stores/useSettingsStore', () => ({
+vi.mock('@/stores/useSettingsStore', () => ({
     useSettingsStore: vi.fn((selector) => {
       const state = {
       keyboardType: 'custom',
@@ -80,7 +80,7 @@ vi.mock('../../../stores/useSettingsStore', () => ({
   }),
 }));
 
-vi.mock('../../../stores/useUserStore', () => ({
+vi.mock('@/stores/useUserStore', () => ({
   useUserStore: Object.assign(
     vi.fn(() => mockUserStoreState),
     {
@@ -95,11 +95,11 @@ vi.mock('../../../stores/useUserStore', () => ({
   ),
 }));
 
-vi.mock('../../../stores/useGameStore', () => ({
+vi.mock('@/stores/useGameStore', () => ({
   useGameStore: mockGameStore,
 }));
 
-vi.mock('../stores/useDeathNoteStore', () => ({
+vi.mock('@/features/quiz/stores/useDeathNoteStore', () => ({
   useDeathNoteStore: {
     getState: () => ({
       addMissedQuestion: vi.fn(),
@@ -107,13 +107,13 @@ vi.mock('../stores/useDeathNoteStore', () => ({
   },
 }));
 
-vi.mock('../../../stores/useToastStore', () => ({
+vi.mock('@/stores/useToastStore', () => ({
   useToastStore: vi.fn(() => ({
     showToast: vi.fn(),
   })),
 }));
 
-vi.mock('../stores/useBaseCampStore', () => ({
+vi.mock('@/features/quiz/stores/useBaseCampStore', () => ({
   useBaseCampStore: {
     getState: () => ({
       startDiagnostic: vi.fn(),
@@ -121,13 +121,13 @@ vi.mock('../stores/useBaseCampStore', () => ({
   },
 }));
 
-vi.mock('../hooks/bridge/useQuestionGenerator', () => ({
+vi.mock('@/features/quiz/hooks/bridge/useQuestionGenerator', () => ({
   useQuestionGenerator: vi.fn(() => ({
     generateNewQuestion: vi.fn(),
   })),
 }));
 
-vi.mock('../hooks/bridge/useQuizInput', () => ({
+vi.mock('@/features/quiz/hooks/bridge/useQuizInput', () => ({
   useQuizInput: vi.fn(() => ({
     handleKeypadNumber: vi.fn(),
     handleQwertyKeyPress: vi.fn(),
@@ -136,7 +136,7 @@ vi.mock('../hooks/bridge/useQuizInput', () => ({
   })),
 }));
 
-vi.mock('../hooks/core/useQuizGameState', () => ({
+vi.mock('@/features/quiz/hooks/core/useQuizGameState', () => ({
   useQuizGameState: vi.fn(() => ({
     totalQuestions: 0,
     setTotalQuestions: vi.fn(),
@@ -150,7 +150,7 @@ vi.mock('../hooks/core/useQuizGameState', () => ({
   })),
 }));
 
-vi.mock('../hooks/bridge/useQuizAnimations', () => ({
+vi.mock('@/features/quiz/hooks/bridge/useQuizAnimations', () => ({
   useQuizAnimations: vi.fn(() => ({
     setIsError: vi.fn(),
     setShowFlash: vi.fn(),
@@ -169,13 +169,13 @@ vi.mock('../hooks/bridge/useQuizAnimations', () => ({
   })),
 }));
 
-vi.mock('../hooks/core/useQuizSubmit', () => ({
+vi.mock('@/features/quiz/hooks/core/useQuizSubmit', () => ({
   useQuizSubmit: vi.fn(() => ({
     handleSubmit: vi.fn(),
   })),
 }));
 
-vi.mock('../hooks/bridge/useQuizBusinessLogic', () => ({
+vi.mock('@/features/quiz/hooks/bridge/useQuizBusinessLogic', () => ({
   useQuizBusinessLogic: vi.fn(() => ({
     handleWatchAdRevive: vi.fn(),
     smartHandleGameOver: vi.fn(),
@@ -183,7 +183,7 @@ vi.mock('../hooks/bridge/useQuizBusinessLogic', () => ({
   })),
 }));
 
-vi.mock('../hooks/bridge/useQuizNavigation', () => ({
+vi.mock('@/features/quiz/hooks/bridge/useQuizNavigation', () => ({
   useQuizNavigation: vi.fn(() => ({
     showExitConfirm: false,
     setShowExitConfirm: vi.fn(),
@@ -194,7 +194,7 @@ vi.mock('../hooks/bridge/useQuizNavigation', () => ({
   })),
 }));
 
-vi.mock('../hooks/core/useQuizModals', () => ({
+vi.mock('@/features/quiz/hooks/core/useQuizModals', () => ({
   useQuizModals: vi.fn(() => ({
     modals: {
       showLastChanceModal: false,
@@ -221,7 +221,7 @@ vi.mock('../hooks/core/useQuizModals', () => ({
   })),
 }));
 
-vi.mock('../hooks/core/useQuizScoreManager', () => ({
+vi.mock('@/features/quiz/hooks/core/useQuizScoreManager', () => ({
   useQuizScoreManager: vi.fn(() => ({
     score: 0,
     combo: 0,
@@ -238,11 +238,11 @@ vi.mock('../hooks/core/useQuizScoreManager', () => ({
   })),
 }));
 
-vi.mock('../hooks/core/useQuizEventProcessor', () => ({
+vi.mock('@/features/quiz/hooks/core/useQuizEventProcessor', () => ({
   useQuizEventProcessor: vi.fn(),
 }));
 
-vi.mock('../../../lib/eventBus', () => ({
+vi.mock('@/lib/eventBus', () => ({
   quizEventBus: {
     emit: vi.fn(),
     on: vi.fn(() => vi.fn()),
@@ -250,7 +250,7 @@ vi.mock('../../../lib/eventBus', () => ({
   },
 }));
 
-vi.mock('../hooks/bridge/useQuizStartLogic', () => ({
+vi.mock('@/features/quiz/hooks/bridge/useQuizStartLogic', () => ({
   useQuizStartLogic: vi.fn(() => ({
     showTipModal: true,
     setShowTipModal: vi.fn(),
@@ -266,7 +266,7 @@ vi.mock('../hooks/bridge/useQuizStartLogic', () => ({
   })),
 }));
 
-vi.mock('../hooks/bridge/useQuizSession', () => ({
+vi.mock('@/features/quiz/hooks/bridge/useQuizSession', () => ({
   useQuizSession: vi.fn(() => ({
     sessionCreated: false,
   })),
@@ -282,7 +282,7 @@ const mockGameplayHandlers = {
   handleTutorialClick: vi.fn(),
 };
 
-vi.mock('../hooks/bridge/useQuizGameplay', () => ({
+vi.mock('@/features/quiz/hooks/bridge/useQuizGameplay', () => ({
   useQuizGameplay: vi.fn(() => ({
     showCountdown: false,
     showSafetyRope: false,
@@ -298,7 +298,7 @@ vi.mock('../hooks/bridge/useQuizGameplay', () => ({
   })),
 }));
 
-vi.mock('../hooks/core/useQuizRevive', () => ({
+vi.mock('@/features/quiz/hooks/core/useQuizRevive', () => ({
   useQuizRevive: vi.fn(() => ({
     handleRevive: vi.fn(),
     handlePurchaseAndRevive: vi.fn(),
@@ -308,17 +308,17 @@ vi.mock('../hooks/core/useQuizRevive', () => ({
 }));
 
 // Mock components with access to handlers
-vi.mock('../components/QuizLayout', async () => {
-  const { useQuiz } = await import('../contexts/QuizContext');
+vi.mock('@/features/quiz/components/QuizLayout', async () => {
+  const { useQuiz } = await import('@/features/quiz/contexts/QuizContext');
   return {
     QuizLayout: vi.fn(() => {
       const { quizState, quizHandlers, modalHandlers, modalState } = useQuiz();
 
       return (
         <div data-testid="quiz-layout">
-          {modalState.showPauseModal && <div data-testid="pause-modal">?좎떆 硫덉땄</div>}
-          {modalState.showStaminaModal && <div data-testid="stamina-modal">?ㅽ깭誘몃굹 遺議?/div>}
-          {modalState.showTutorial && <div data-testid="tutorial-modal">怨듬왂踰?/div>}
+          {modalState.showPauseModal && <div data-testid="pause-modal">?醫롫뻻 筌롫뜆??/div>}
+          {modalState.showStaminaModal && <div data-testid="stamina-modal">??쎄묶沃섎챶援??봔鈺?/div>}
+          {modalState.showTutorial && <div data-testid="tutorial-modal">?⑤벉?귟린?/div>}
 
           {quizState.isPreview ? (
             <div data-testid="quiz-preview" />
@@ -389,14 +389,14 @@ vi.mock('../components/QuizLayout', async () => {
   };
 });
 
-vi.mock('../components/QuizPreview', () => ({
+vi.mock('@/features/quiz/components/QuizPreview', () => ({
   QuizPreview: vi.fn(() => <div data-testid="quiz-preview">Quiz Preview</div>),
 }));
 
 // Mock URL params validation
-vi.mock('../../../utils/urlParams', () => ({
+vi.mock('@/utils/urlParams', () => ({
   validateWorldParam: vi.fn((v) => v || 'World1'),
-  validateCategoryInWorldParam: vi.fn((w, c) => c || '湲곗큹'),
+  validateCategoryInWorldParam: vi.fn((w, c) => c || '疫꿸퀣??),
   validateLevelParam: vi.fn((l) => (l ? parseInt(l) : 1)),
   validateModeParam: vi.fn((m) => m || 'practice'),
 }));
@@ -442,7 +442,7 @@ describe('QuizPage', () => {
 
   it('should initialize quiz context with URL parameters', () => {
     mockSearchParams.set('world', 'World2');
-    mockSearchParams.set('category', '?ы솕');
+    mockSearchParams.set('category', '????);
     mockSearchParams.set('level', '5');
 
     render(
@@ -550,7 +550,7 @@ describe('QuizPage', () => {
     });
 
     it('should handle pause and resume', async () => {
-      const { quizEventBus } = await import('../../../lib/eventBus');
+      const { quizEventBus } = await import('@/lib/eventBus');
       render(
         <BrowserRouter>
           <QuizPage />
diff --git a/src/features/quiz/__tests__/ResultPage.test.tsx b/src/features/quiz/__tests__/ResultPage.test.tsx
index 570a7dd..3e5ea94 100644
--- a/src/features/quiz/__tests__/ResultPage.test.tsx
+++ b/src/features/quiz/__tests__/ResultPage.test.tsx
@@ -1,10 +1,10 @@
-import { describe, it, expect, vi, beforeEach } from 'vitest';
+癤퓁mport { describe, it, expect, vi, beforeEach } from 'vitest';
 import { render } from '@testing-library/react';
-import { ResultPage } from '../pages/ResultPage';
+import { ResultPage } from '@/features/quiz/pages/ResultPage';
 import { BrowserRouter } from 'react-router-dom';
 
 // Mock dependencies
-vi.mock('../stores/useQuizStore', () => {
+vi.mock('@/features/quiz/stores/useQuizStore', () => {
   const mockState = { score: 100 };
   const fn = vi.fn((selector) =>
     typeof selector === 'function' ? selector(mockState) : mockState
@@ -12,7 +12,7 @@ vi.mock('../stores/useQuizStore', () => {
   return { useQuizStore: Object.assign(fn, { getState: () => mockState }) };
 });
 
-vi.mock('../stores/useLevelProgressStore', () => {
+vi.mock('@/features/quiz/stores/useLevelProgressStore', () => {
   const mockState = {
     progress: {},
     rankings: {},
@@ -33,7 +33,7 @@ vi.mock('../stores/useLevelProgressStore', () => {
   };
 });
 
-vi.mock('../../../stores/useUserStore', () => {
+vi.mock('@/stores/useUserStore', () => {
   const mockState = {
     minerals: 100,
     fetchUserData: vi.fn().mockResolvedValue({ success: true }),
@@ -51,7 +51,7 @@ vi.mock('../../../stores/useUserStore', () => {
   };
 });
 
-vi.mock('../../../stores/useSettingsStore', () => {
+vi.mock('@/stores/useSettingsStore', () => {
   const mockStore = (initialState: any) => {
     const store = vi.fn((selector) =>
       typeof selector === 'function' ? selector(initialState) : initialState
@@ -62,7 +62,7 @@ vi.mock('../../../stores/useSettingsStore', () => {
   return { useSettingsStore: mockStore({ animationEnabled: true }) };
 });
 
-vi.mock('../../../stores/useToastStore', () => {
+vi.mock('@/stores/useToastStore', () => {
   const mockStore = (initialState: any) => {
     const store = vi.fn((selector) =>
       typeof selector === 'function' ? selector(initialState) : initialState
@@ -73,14 +73,14 @@ vi.mock('../../../stores/useToastStore', () => {
   return { useToastStore: mockStore({ showToast: vi.fn() }) };
 });
 
-vi.mock('../../../services/analytics', () => ({
+vi.mock('@/services/analytics', () => ({
   analytics: {
     trackQuizEnd: vi.fn(),
     trackEvent: vi.fn(),
   },
 }));
 
-vi.mock('../../../utils/adService', () => ({
+vi.mock('@/utils/adService', () => ({
   AdService: {
     showRewardedAd: vi.fn(() => Promise.resolve({ success: true })),
   },
@@ -117,7 +117,7 @@ describe('ResultPage', () => {
     const params = new URLSearchParams({
       mode: 'survival',
       world: 'World1',
-      category: '湲곗큹',
+      category: '疫꿸퀣??,
       score: '500',
       last_q: '2 + 2',
       wrong_a: '5',
@@ -132,20 +132,20 @@ describe('ResultPage', () => {
       </BrowserRouter>
     );
 
-    expect(getAllByText(/留덉?留?怨좊퉬/i)[0]).toBeTruthy();
+    expect(getAllByText(/筌띾뜆?筌??⑥쥓??i)[0]).toBeTruthy();
     expect(getAllByText(/2 \+ 2/i)[0]).toBeTruthy();
     expect(getAllByText(/5/i)[0]).toBeTruthy();
     expect(getAllByText(/4/i)[0]).toBeTruthy();
   });
 
   it('should handle Double Reward with AdSuccess', async () => {
-    const { useUserStore } = await import('../../../stores/useUserStore');
-    const { AdService } = await import('../../../utils/adService');
+    const { useUserStore } = await import('@/stores/useUserStore');
+    const { AdService } = await import('@/utils/adService');
     const params = new URLSearchParams({
       score: '200',
       mode: 'time-attack',
       world: 'World1',
-      category: '湲곗큹',
+      category: '疫꿸퀣??,
     });
     const { useSearchParams } = await import('react-router-dom');
     vi.mocked(useSearchParams).mockReturnValue([params, vi.fn()]);
@@ -156,7 +156,7 @@ describe('ResultPage', () => {
       </BrowserRouter>
     );
 
-    const doubleBtn = getByText(/蹂댁긽 2諛?i);
+    const doubleBtn = getByText(/癰귣똻湲?2獄?i);
     const { fireEvent, act } = await import('@testing-library/react');
 
     await act(async () => {
@@ -171,7 +171,7 @@ describe('ResultPage', () => {
     const params = new URLSearchParams({
       mode: 'base-camp-result',
       accuracy: '85',
-      recommendation: '???,
+      recommendation: '????,
     });
     const { useSearchParams } = await import('react-router-dom');
     vi.mocked(useSearchParams).mockReturnValue([params, vi.fn()]);
@@ -182,9 +182,9 @@ describe('ResultPage', () => {
       </BrowserRouter>
     );
 
-    expect(getByText(/吏꾨떒 ?꾨즺/i)).toBeTruthy();
+    expect(getByText(/筌욊쑬???袁⑥┷/i)).toBeTruthy();
     expect(getByText(/85%/i)).toBeTruthy();
-    expect(getByText(/湲됯꼍???깅컲/i)).toBeTruthy();
+    expect(getByText(/疫뀀맦瑗???源낆뺘/i)).toBeTruthy();
   });
 
   it('should handle navigation buttons', async () => {
@@ -192,7 +192,7 @@ describe('ResultPage', () => {
       score: '100',
       mode: 'time-attack',
       world: 'World1',
-      category: '湲곗큹',
+      category: '疫꿸퀣??,
       level: '1',
     });
     const { useSearchParams } = await import('react-router-dom');
@@ -206,13 +206,13 @@ describe('ResultPage', () => {
     );
 
     await act(async () => {
-      const retryBtn = getAllByText(/?ㅼ떆 ?꾩쟾/i)[0];
+      const retryBtn = getAllByText(/??쇰뻻 ?袁⑹읈/i)[0];
       fireEvent.click(retryBtn);
     });
     expect(mockNavigate).toHaveBeenCalled();
 
     await act(async () => {
-      const otherBtn = getAllByText(/?ㅻⅨ ?덈꺼/i)[0];
+      const otherBtn = getAllByText(/??삘뀲 ??덇볼/i)[0];
       fireEvent.click(otherBtn);
     });
     expect(mockNavigate).toHaveBeenCalled();
@@ -222,7 +222,7 @@ describe('ResultPage', () => {
     const params = new URLSearchParams({
       mode: 'base-camp-result',
       accuracy: '85',
-      recommendation: '???,
+      recommendation: '????,
       world: 'World1',
     });
     const { useSearchParams } = await import('react-router-dom');
@@ -237,12 +237,12 @@ describe('ResultPage', () => {
     const { fireEvent, act } = await import('@testing-library/react');
 
     await act(async () => {
-      fireEvent.click(getByText(/異붿쿇 肄붿뒪濡??깅컲 ?쒖옉/i));
+      fireEvent.click(getByText(/?곕뗄荑??꾨뗄?ゆ에??源낆뺘 ??뽰삂/i));
     });
     expect(mockNavigate).toHaveBeenCalled();
 
     // Test close button
-    const closeBtn = getByText('??);
+    const closeBtn = getByText('??);
     fireEvent.click(closeBtn);
     expect(mockNavigate).toHaveBeenCalled();
   });
@@ -258,7 +258,7 @@ describe('ResultPage', () => {
       </BrowserRouter>
     );
 
-    const revengeBtn = getByText(/?곗뒪?명듃 蹂듭닔?섍린/i);
+    const revengeBtn = getByText(/?怨쀫뮞?紐낅뱜 癰귣벊???띾┛/i);
 
     // Mock window.location.href
     const originalLocation = window.location.href;
diff --git a/src/features/quiz/__tests__/useBaseCampStore.test.ts b/src/features/quiz/__tests__/useBaseCampStore.test.ts
index 93045a4..0c41431 100644
--- a/src/features/quiz/__tests__/useBaseCampStore.test.ts
+++ b/src/features/quiz/__tests__/useBaseCampStore.test.ts
@@ -1,9 +1,9 @@
-import { describe, it, expect, beforeEach, vi } from 'vitest';
+癤퓁mport { describe, it, expect, beforeEach, vi } from 'vitest';
 import { renderHook, act } from '@testing-library/react';
-import { useBaseCampStore } from '../stores/useBaseCampStore';
+import { useBaseCampStore } from '@/features/quiz/stores/useBaseCampStore';
 
 // Mock generateQuestion
-vi.mock('../utils/quizGenerator', () => ({
+vi.mock('@/features/quiz/utils/quizGenerator', () => ({
   generateQuestion: vi.fn((type, world, category, id, difficulty) => ({
     id: id.toString(),
     question: `Question ${id}`,
@@ -56,7 +56,7 @@ describe('useBaseCampStore', () => {
     expect(result.current.results[1]).toEqual({ isCorrect: false, time: 2000 });
   });
 
-  it('should recommend "?ы솕" (Rock Climbing) for high accuracy and fast speed', () => {
+  it('should recommend "???? (Rock Climbing) for high accuracy and fast speed', () => {
     const { result } = renderHook(() => useBaseCampStore());
 
     act(() => {
@@ -70,10 +70,10 @@ describe('useBaseCampStore', () => {
     const recommendation = result.current.getRecommendation();
     expect(recommendation.accuracy).toBe(100);
     expect(recommendation.avgTime).toBe(1000);
-    expect(recommendation.recommendation).toBe('?ы솕');
+    expect(recommendation.recommendation).toBe('????);
   });
 
-  it('should recommend "??? (Steep) for 80% accuracy', () => {
+  it('should recommend "???? (Steep) for 80% accuracy', () => {
     const { result } = renderHook(() => useBaseCampStore());
 
     act(() => {
@@ -84,10 +84,10 @@ describe('useBaseCampStore', () => {
 
     const recommendation = result.current.getRecommendation();
     expect(recommendation.accuracy).toBe(80);
-    expect(recommendation.recommendation).toBe('???);
+    expect(recommendation.recommendation).toBe('????);
   });
 
-  it('should recommend "?쇰━" (Exploration) for 60% accuracy', () => {
+  it('should recommend "??겸봺" (Exploration) for 60% accuracy', () => {
     const { result } = renderHook(() => useBaseCampStore());
 
     act(() => {
@@ -98,10 +98,10 @@ describe('useBaseCampStore', () => {
 
     const recommendation = result.current.getRecommendation();
     expect(recommendation.accuracy).toBe(60);
-    expect(recommendation.recommendation).toBe('?쇰━');
+    expect(recommendation.recommendation).toBe('??겸봺');
   });
 
-  it('should recommend "湲곗큹" (General) for low accuracy', () => {
+  it('should recommend "疫꿸퀣?? (General) for low accuracy', () => {
     const { result } = renderHook(() => useBaseCampStore());
 
     act(() => {
@@ -112,7 +112,7 @@ describe('useBaseCampStore', () => {
 
     const recommendation = result.current.getRecommendation();
     expect(recommendation.accuracy).toBe(40);
-    expect(recommendation.recommendation).toBe('湲곗큹');
+    expect(recommendation.recommendation).toBe('疫꿸퀣??);
   });
 
   it('should reset all state including questions and results', () => {
diff --git a/src/features/quiz/__tests__/useDeathNoteStore.test.ts b/src/features/quiz/__tests__/useDeathNoteStore.test.ts
index f041c57..807e882 100644
--- a/src/features/quiz/__tests__/useDeathNoteStore.test.ts
+++ b/src/features/quiz/__tests__/useDeathNoteStore.test.ts
@@ -1,14 +1,14 @@
-import { describe, it, expect, beforeEach } from 'vitest';
+癤퓁mport { describe, it, expect, beforeEach } from 'vitest';
 import { renderHook, act } from '@testing-library/react';
-import { useDeathNoteStore } from '../stores/useDeathNoteStore';
-import { QuizQuestion } from '../types/quiz';
+import { useDeathNoteStore } from '@/features/quiz/stores/useDeathNoteStore';
+import { QuizQuestion } from '@/features/quiz/types/quiz';
 
 describe('useDeathNoteStore', () => {
   const mockQuizQuestion: QuizQuestion = {
     question: 'How much is 2+2?',
     answer: '4',
     options: ['3', '4', '5'],
-    category: '湲곗큹',
+    category: '疫꿸퀣??,
   };
 
   beforeEach(() => {
@@ -22,7 +22,7 @@ describe('useDeathNoteStore', () => {
     const { result } = renderHook(() => useDeathNoteStore());
 
     act(() => {
-      result.current.addMissedQuestion(mockQuizQuestion, 'World1', '湲곗큹');
+      result.current.addMissedQuestion(mockQuizQuestion, 'World1', '疫꿸퀣??);
     });
 
     expect(result.current.missedQuestions.length).toBe(1);
@@ -36,12 +36,12 @@ describe('useDeathNoteStore', () => {
     const { result } = renderHook(() => useDeathNoteStore());
 
     act(() => {
-      result.current.addMissedQuestion(mockQuizQuestion, 'World1', '湲곗큹');
+      result.current.addMissedQuestion(mockQuizQuestion, 'World1', '疫꿸퀣??);
     });
 
     act(() => {
       // Adding it again with same params
-      result.current.addMissedQuestion({ ...mockQuizQuestion }, 'World1', '湲곗큹');
+      result.current.addMissedQuestion({ ...mockQuizQuestion }, 'World1', '疫꿸퀣??);
     });
 
     expect(result.current.missedQuestions.length).toBe(1);
@@ -51,8 +51,8 @@ describe('useDeathNoteStore', () => {
     const { result } = renderHook(() => useDeathNoteStore());
 
     act(() => {
-      result.current.addMissedQuestion(mockQuizQuestion, 'World1', '湲곗큹');
-      result.current.addMissedQuestion(mockQuizQuestion, 'World2', '湲곗큹');
+      result.current.addMissedQuestion(mockQuizQuestion, 'World1', '疫꿸퀣??);
+      result.current.addMissedQuestion(mockQuizQuestion, 'World2', '疫꿸퀣??);
     });
 
     expect(result.current.missedQuestions.length).toBe(2);
@@ -62,7 +62,7 @@ describe('useDeathNoteStore', () => {
     const { result } = renderHook(() => useDeathNoteStore());
 
     act(() => {
-      result.current.addMissedQuestion(mockQuizQuestion, 'World1', '湲곗큹');
+      result.current.addMissedQuestion(mockQuizQuestion, 'World1', '疫꿸퀣??);
     });
 
     const idToRemove = result.current.missedQuestions[0].id;
@@ -78,11 +78,11 @@ describe('useDeathNoteStore', () => {
     const { result } = renderHook(() => useDeathNoteStore());
 
     act(() => {
-      result.current.addMissedQuestion({ ...mockQuizQuestion, question: 'Q1' }, 'World1', '湲곗큹');
-      result.current.addMissedQuestion({ ...mockQuizQuestion, question: 'Q2' }, 'World2', '???);
+      result.current.addMissedQuestion({ ...mockQuizQuestion, question: 'Q1' }, 'World1', '疫꿸퀣??);
+      result.current.addMissedQuestion({ ...mockQuizQuestion, question: 'Q2' }, 'World2', '????);
     });
 
-    const filtered = result.current.getQuestionsByCategory('World1', '湲곗큹');
+    const filtered = result.current.getQuestionsByCategory('World1', '疫꿸퀣??);
     expect(filtered.length).toBe(1);
     expect(filtered[0].question).toBe('Q1');
   });
@@ -94,7 +94,7 @@ describe('useDeathNoteStore', () => {
       // Add 60 unique questions
       for (let i = 0; i < 60; i++) {
         const q = { ...mockQuizQuestion, question: `Unique Question ${i}` };
-        result.current.addMissedQuestion(q, 'World1', '湲곗큹');
+        result.current.addMissedQuestion(q, 'World1', '疫꿸퀣??);
       }
     });
 
@@ -107,7 +107,7 @@ describe('useDeathNoteStore', () => {
     const { result } = renderHook(() => useDeathNoteStore());
 
     act(() => {
-      result.current.addMissedQuestion(mockQuizQuestion, 'World1', '湲곗큹');
+      result.current.addMissedQuestion(mockQuizQuestion, 'World1', '疫꿸퀣??);
       result.current.clearDeathNote();
     });
 
diff --git a/src/features/quiz/__tests__/useLevelProgressStore.test.ts b/src/features/quiz/__tests__/useLevelProgressStore.test.ts
deleted file mode 100644
index 45cffa3..0000000
--- a/src/features/quiz/__tests__/useLevelProgressStore.test.ts
+++ /dev/null
@@ -1,394 +0,0 @@
-import { describe, it, expect, beforeEach, vi, afterEach } from 'vitest';
-import { renderHook, act } from '@testing-library/react';
-import { useLevelProgressStore } from '../stores/useLevelProgressStore';
-import { supabase } from '../../../utils/supabaseClient';
-import { useToastStore } from '../../../stores/useToastStore';
-import { useDebugStore } from '../../../stores/useDebugStore';
-
-// Mock dependencies
-vi.mock('../../../utils/supabaseClient', () => ({
-  supabase: {
-    auth: {
-      getUser: vi.fn(),
-    },
-    from: vi.fn(),
-    rpc: vi.fn(),
-    channel: vi.fn(),
-  },
-}));
-
-vi.mock('../../../stores/useToastStore', () => ({
-  useToastStore: {
-    getState: vi.fn(() => ({
-      showToast: vi.fn(),
-    })),
-  },
-}));
-
-vi.mock('../../../stores/useDebugStore', () => {
-  const mockState = { bypassLevelLock: false };
-  const mockStore = Object.assign(
-    vi.fn(() => mockState),
-    {
-      getState: vi.fn(() => mockState),
-      setState: vi.fn(),
-      subscribe: vi.fn(),
-    }
-  );
-  return { useDebugStore: mockStore };
-});
-
-const mockShowToast = vi.fn();
-(useToastStore.getState as any).mockReturnValue({ showToast: mockShowToast });
-
-const createMockRpcBuilder = (data: any, error: any = null) => {
-  const promise = Promise.resolve({ data, error });
-  return Object.assign(promise, {
-    eq: vi.fn().mockReturnThis(),
-    neq: vi.fn().mockReturnThis(),
-    gt: vi.fn().mockReturnThis(),
-    gte: vi.fn().mockReturnThis(),
-    lt: vi.fn().mockReturnThis(),
-    lte: vi.fn().mockReturnThis(),
-    order: vi.fn().mockReturnThis(),
-    limit: vi.fn().mockReturnThis(),
-    single: vi.fn().mockReturnThis(),
-    maybeSingle: vi.fn().mockReturnThis(),
-    select: vi.fn().mockReturnThis(),
-    then: promise.then.bind(promise),
-    catch: promise.catch.bind(promise),
-    finally: promise.finally.bind(promise),
-  }) as any;
-};
-
-describe('useLevelProgressStore', () => {
-  const world = 'math';
-  const category = 'arithmetic_addition';
-
-  beforeEach(async () => {
-    vi.clearAllMocks();
-    // Ensure bypassLevelLock is false by default for consistency
-    vi.mocked(useDebugStore.getState).mockReturnValue({ bypassLevelLock: false } as any);
-    // Default mock setup for successful calls
-    vi.mocked(supabase.auth.getUser).mockResolvedValue({
-      data: { user: { id: 'user-123' } },
-      error: null,
-    } as any);
-    vi.mocked(supabase.rpc).mockImplementation(() => createMockRpcBuilder({ success: true }));
-
-    vi.mocked(supabase.from).mockReturnValue({
-      select: vi.fn().mockReturnThis(),
-      eq: vi.fn().mockReturnThis(),
-      order: vi.fn().mockReturnThis(),
-      limit: vi.fn().mockReturnThis(),
-      then: vi.fn().mockImplementation((cb) => cb({ data: [], error: null })),
-    } as any);
-
-    // Reset store state
-    const { result } = renderHook(() => useLevelProgressStore());
-    await act(async () => {
-      await result.current.resetProgress();
-    });
-    vi.clearAllMocks(); // Clear mocks again after reset
-  });
-
-  afterEach(() => {
-    vi.restoreAllMocks();
-  });
-
-  describe('Core Functionality', () => {
-    it('should initialize with empty progress', () => {
-      const { result } = renderHook(() => useLevelProgressStore());
-      expect(result.current.progress).toEqual({});
-    });
-
-    it('should handle bypassLevelLock debug setting', () => {
-      vi.mocked(useDebugStore.getState).mockReturnValue({ bypassLevelLock: true } as any);
-      const { result } = renderHook(() => useLevelProgressStore());
-
-      expect(result.current.isLevelCleared(world, category, 1)).toBe(true);
-      expect(result.current.getNextLevel(world, category)).toBe(999);
-    });
-
-    it('should handle tier support (Hard Mode)', async () => {
-      const { result } = renderHook(() => useLevelProgressStore());
-
-      await act(async () => {
-        await result.current.clearLevel(
-          world,
-          category,
-          1,
-          'time-attack',
-          100,
-          0,
-          undefined,
-          'hard'
-        );
-      });
-
-      expect(result.current.isLevelCleared(world, category, 1, 'hard')).toBe(true);
-      expect(result.current.isLevelCleared(world, category, 1, 'normal')).toBe(false);
-      expect(result.current.getNextLevel(world, category, 'hard')).toBe(2);
-    });
-  });
-
-  describe('clearLevel and Persistence', () => {
-    it('should rollback on RPC failure in clearLevel', async () => {
-      const { result } = renderHook(() => useLevelProgressStore());
-      const rollbackWorld = 'math_rollback';
-
-      // Specifically target this world for failure
-      vi.mocked(supabase.rpc).mockImplementationOnce(() =>
-        createMockRpcBuilder(null, { message: 'RPC Failure' })
-      );
-
-      await act(async () => {
-        await result.current.clearLevel(rollbackWorld, category, 5, 'time-attack', 500);
-      });
-
-      // Should have rolled back (isLevelCleared check)
-      // Note: we use our deep-clone fix to ensure previousProgress was unpolluted
-      expect(result.current.isLevelCleared(rollbackWorld, category, 5)).toBe(false);
-    });
-
-    it('should handle tier support (Hard Mode) isolated from Normal', async () => {
-      const { result } = renderHook(() => useLevelProgressStore());
-      const tierWorld = 'math_tier';
-
-      await act(async () => {
-        // Clear level 1 in HARD mode
-        await result.current.clearLevel(
-          tierWorld,
-          category,
-          1,
-          'time-attack',
-          100,
-          0,
-          undefined,
-          'hard'
-        );
-      });
-
-      expect(result.current.isLevelCleared(tierWorld, category, 1, 'hard')).toBe(true);
-      // NORMAL mode should still be locked
-      expect(result.current.isLevelCleared(tierWorld, category, 1, 'normal')).toBe(false);
-    });
-  });
-
-  describe('getBestRecords', () => {
-    it('should calculate global best records across levels', async () => {
-      const { result } = renderHook(() => useLevelProgressStore());
-      const bestWorld = 'math_best';
-
-      await act(async () => {
-        await result.current.clearLevel(bestWorld, category, 1, 'time-attack', 100);
-        await result.current.clearLevel(bestWorld, category, 2, 'time-attack', 200);
-        await result.current.clearLevel(bestWorld, category, 1, 'survival', 500);
-        await result.current.updateBestScore(bestWorld, category, 3, 'infinite', 1000);
-      });
-
-      const best = result.current.getBestRecords(bestWorld, category);
-      expect(best['time-attack']).toBe(200);
-      expect(best.survival).toBe(500);
-      expect((best as any).infinite).toBe(1000);
-    });
-  });
-
-  describe('syncProgress and Reconciliation', () => {
-    it('should reconcile local and server scores', async () => {
-      const { result } = renderHook(() => useLevelProgressStore());
-
-      // 1. Set local high score
-      act(() => {
-        result.current.clearLevel(world, 'arithmetic_addition', 1, 'time-attack', 300);
-      });
-
-      // 2. Mock server response with low score for L1 and high score for L2
-      const mockRecords = [
-        {
-          world_id: world,
-          category_id: 'arithmetic',
-          subject_id: 'addition',
-          level: 1,
-          mode_code: 1,
-          best_score: 100, // Lower than local
-          updated_at: '2024-01-01',
-        },
-        {
-          world_id: world,
-          category_id: 'arithmetic',
-          subject_id: 'addition',
-          level: 2,
-          mode_code: 1,
-          best_score: 500, // New from server
-          updated_at: '2024-01-02',
-        },
-      ];
-
-      vi.mocked(supabase.from).mockReturnValue({
-        select: vi.fn().mockReturnThis(),
-        eq: vi.fn().mockReturnThis(),
-        then: vi.fn().mockImplementation((cb) => cb({ data: mockRecords, error: null })),
-      } as any);
-
-      await act(async () => {
-        await result.current.syncProgress();
-      });
-
-      const progress = result.current.getLevelProgress(world, 'arithmetic_addition');
-      expect(progress.find((p) => p.level === 1)?.bestScore['time-attack']).toBe(300); // Local won
-      expect(progress.find((p) => p.level === 2)?.bestScore['time-attack']).toBe(500); // Server added
-    });
-  });
-
-  describe('Rankings and Realtime', () => {
-    it('should fetch Hall of Fame (all-time) rankings', async () => {
-      const { result } = renderHook(() => useLevelProgressStore());
-      const mockHof = [{ user_id: 'legend', nickname: 'The King', score: 9999, rank: 1 }];
-
-      vi.mocked(supabase.from).mockReturnValue({
-        select: vi.fn().mockReturnThis(),
-        eq: vi.fn().mockReturnThis(),
-        order: vi.fn().mockReturnThis(),
-        limit: vi.fn().mockReturnThis(),
-        then: vi.fn().mockImplementation((cb) => cb({ data: mockHof, error: null })),
-      } as any);
-
-      await act(async () => {
-        await result.current.fetchRanking(null, null, 'all-time', 'total');
-      });
-
-      expect(result.current.rankings['all-time-total']).toEqual(mockHof);
-    });
-
-    it('should handle realtime subscription lifecycle and events', () => {
-      let eventCallback: any;
-      const mockUnsubscribe = vi.fn();
-      const mockSubscribe = vi.fn().mockReturnValue({ unsubscribe: mockUnsubscribe });
-      const mockOn = vi.fn().mockImplementation((_type, _filter, callback) => {
-        eventCallback = callback;
-        return { subscribe: mockSubscribe };
-      });
-
-      vi.mocked(supabase.channel).mockReturnValue({
-        on: mockOn,
-        subscribe: mockSubscribe,
-      } as any);
-
-      const { result } = renderHook(() => useLevelProgressStore());
-
-      act(() => {
-        result.current.subscribeToRankingUpdates();
-      });
-
-      expect(supabase.channel).toHaveBeenCalledWith('ranking-updates');
-
-      // Simulate realtime update
-      act(() => {
-        eventCallback({ new: { id: 'test' } });
-      });
-      expect(result.current.rankingVersion).toBe(1);
-
-      act(() => {
-        result.current.unsubscribeFromRankingUpdates();
-      });
-
-      expect(mockUnsubscribe).toHaveBeenCalled();
-    });
-
-    it('should handle syncProgress error', async () => {
-      vi.mocked(supabase.from).mockImplementation(() => {
-        throw new Error('Sync failed');
-      });
-      const { result } = renderHook(() => useLevelProgressStore());
-
-      await act(async () => {
-        await result.current.syncProgress();
-      });
-      expect(mockShowToast).toHaveBeenCalled();
-    });
-
-    it('should handle resetProgress failure', async () => {
-      vi.mocked(supabase.rpc).mockImplementationOnce(() =>
-        createMockRpcBuilder({ success: false, message: 'Reset failed' })
-      );
-      const { result } = renderHook(() => useLevelProgressStore());
-
-      await act(async () => {
-        await result.current.resetProgress();
-      });
-      expect(mockShowToast).toHaveBeenCalled();
-    });
-
-    it('should handle resetProgress exception', async () => {
-      vi.mocked(supabase.rpc).mockImplementationOnce(() => {
-        throw new Error('Exception');
-      });
-      const { result } = renderHook(() => useLevelProgressStore());
-
-      await act(async () => {
-        await result.current.resetProgress();
-      });
-      expect(mockShowToast).toHaveBeenCalled();
-    });
-
-    it('should reconcile when server has higher score', async () => {
-      const { result } = renderHook(() => useLevelProgressStore());
-      const mockRecords = [
-        {
-          world_id: world,
-          category_id: 'arithmetic',
-          subject_id: 'addition',
-          level: 1,
-          mode_code: 1,
-          best_score: 1000, // Higher than default 0
-          updated_at: '2024-01-01',
-        },
-      ];
-
-      vi.mocked(supabase.from).mockReturnValue({
-        select: vi.fn().mockReturnThis(),
-        eq: vi.fn().mockReturnThis(),
-        then: vi.fn().mockImplementation((cb) => cb({ data: mockRecords, error: null })),
-      } as any);
-
-      await act(async () => {
-        await result.current.syncProgress();
-      });
-
-      const entry = result.current.getLevelProgress(world, 'arithmetic_addition')[0];
-      expect(entry.bestScore['time-attack']).toBe(1000);
-    });
-  });
-
-  describe('Additional score tests', () => {
-    it('should return default record if no progress exists in getLevelProgress', () => {
-      const { result } = renderHook(() => useLevelProgressStore());
-      expect(result.current.getLevelProgress('none', 'none')).toEqual([]);
-    });
-
-    it('should handle updateBestScore failure', async () => {
-      vi.mocked(supabase.rpc).mockImplementationOnce(() =>
-        createMockRpcBuilder(null, { message: 'Failure' })
-      );
-      const { result } = renderHook(() => useLevelProgressStore());
-
-      await act(async () => {
-        await result.current.updateBestScore(world, category, 1, 'time-attack', 100);
-      });
-      // Rollback logic is internal but we check it doesn't crash
-    });
-
-    it('should handle clearLevel exception', async () => {
-      vi.mocked(supabase.rpc).mockImplementationOnce(() => {
-        throw new Error('Exception');
-      });
-      const { result } = renderHook(() => useLevelProgressStore());
-
-      await act(async () => {
-        await result.current.clearLevel(world, category, 1, 'time-attack', 100);
-      });
-      expect(result.current.isLevelCleared(world, category, 1)).toBe(false);
-    });
-  });
-});
diff --git a/src/features/quiz/__tests__/useQuestionGenerator.test.ts b/src/features/quiz/__tests__/useQuestionGenerator.test.ts
index b1084ba..78e42a3 100644
--- a/src/features/quiz/__tests__/useQuestionGenerator.test.ts
+++ b/src/features/quiz/__tests__/useQuestionGenerator.test.ts
@@ -1,16 +1,16 @@
-import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
+癤퓁mport { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
 import { renderHook, act } from '@testing-library/react';
-import { useQuestionGenerator } from '../hooks/bridge/useQuestionGenerator';
-import { generateQuestion } from '../utils/quizGenerator';
-import type { QuizQuestion } from '../types/quiz';
-import { quizEventBus } from '../../../lib/eventBus';
+import { useQuestionGenerator } from '@/features/quiz/hooks/bridge/useQuestionGenerator';
+import { generateQuestion } from '@/features/quiz/utils/quizGenerator';
+import type { QuizQuestion } from '@/features/quiz/types/quiz';
+import { quizEventBus } from '@/lib/eventBus';
 
 // Mock dependencies
-vi.mock('../utils/quizGenerator', () => ({
+vi.mock('@/features/quiz/utils/quizGenerator', () => ({
   generateQuestion: vi.fn(),
 }));
 
-vi.mock('../../../lib/eventBus', () => ({
+vi.mock('@/lib/eventBus', () => ({
   quizEventBus: {
     emit: vi.fn(),
     on: vi.fn(() => vi.fn()),
@@ -18,7 +18,7 @@ vi.mock('../../../lib/eventBus', () => ({
   },
 }));
 
-vi.mock('../stores/useBaseCampStore', () => ({
+vi.mock('@/features/quiz/stores/useBaseCampStore', () => ({
   useBaseCampStore: {
     getState: () => ({
       questions: [],
@@ -28,7 +28,7 @@ vi.mock('../stores/useBaseCampStore', () => ({
   },
 }));
 
-vi.mock('../stores/useDeathNoteStore', () => ({
+vi.mock('@/features/quiz/stores/useDeathNoteStore', () => ({
   useDeathNoteStore: {
     getState: () => ({
       getQuestionsByCategory: vi.fn().mockReturnValue([]),
@@ -38,7 +38,7 @@ vi.mock('../stores/useDeathNoteStore', () => ({
 
 describe('useQuestionGenerator', () => {
   const defaultParams = {
-    category: '湲곗큹' as const,
+    category: '疫꿸퀣?? as const,
     difficulty: 'easy' as const,
     gameMode: 'time-attack' as const,
     categoryParam: null,
@@ -89,7 +89,7 @@ describe('useQuestionGenerator', () => {
     const { result } = renderHook(() =>
       useQuestionGenerator({
         ...defaultParams,
-        categoryParam: '湲곗큹',
+        categoryParam: '疫꿸퀣??,
         levelParam: 1,
       })
     );
@@ -101,7 +101,7 @@ describe('useQuestionGenerator', () => {
     expect(generateQuestion).toHaveBeenCalledWith(
       'math',
       'World1',
-      'World1-湲곗큹',
+      'World1-疫꿸퀣??,
       1,
       'easy',
       'normal',
@@ -118,7 +118,7 @@ describe('useQuestionGenerator', () => {
     const { result } = renderHook(() =>
       useQuestionGenerator({
         ...defaultParams,
-        categoryParam: '???,
+        categoryParam: '????,
         levelParam: 1,
       })
     );
@@ -130,7 +130,7 @@ describe('useQuestionGenerator', () => {
     expect(generateQuestion).toHaveBeenCalledWith(
       'math',
       'World1',
-      'World1-???,
+      'World1-????,
       1,
       'easy',
       'normal',
@@ -199,7 +199,7 @@ describe('useQuestionGenerator', () => {
     const { result } = renderHook(() =>
       useQuestionGenerator({
         ...defaultParams,
-        categoryParam: '???,
+        categoryParam: '????,
         levelParam: null,
       })
     );
@@ -211,7 +211,7 @@ describe('useQuestionGenerator', () => {
     expect(generateQuestion).toHaveBeenCalledWith(
       'math',
       'World1',
-      'World1-???,
+      'World1-????,
       1,
       'easy',
       'normal',
@@ -228,7 +228,7 @@ describe('useQuestionGenerator', () => {
     const { result } = renderHook(() =>
       useQuestionGenerator({
         ...defaultParams,
-        categoryParam: '?ы솕',
+        categoryParam: '????,
         levelParam: null,
       })
     );
@@ -240,7 +240,7 @@ describe('useQuestionGenerator', () => {
     expect(generateQuestion).toHaveBeenCalledWith(
       'math',
       'World1',
-      'World1-?ы솕',
+      'World1-????,
       1,
       'easy',
       'normal',
@@ -317,7 +317,7 @@ describe('useQuestionGenerator', () => {
       const { result } = renderHook(() =>
         useQuestionGenerator({
           ...defaultParams,
-          categoryParam: '湲곗큹',
+          categoryParam: '疫꿸퀣??,
           levelParam: level,
         })
       );
@@ -329,7 +329,7 @@ describe('useQuestionGenerator', () => {
       expect(generateQuestion).toHaveBeenCalledWith(
         'math',
         'World1',
-        'World1-湲곗큹',
+        'World1-疫꿸퀣??,
         level,
         'easy',
         'normal',
@@ -347,7 +347,7 @@ describe('useQuestionGenerator', () => {
     const { result } = renderHook(() =>
       useQuestionGenerator({
         ...defaultParams,
-        categoryParam: '湲곗큹',
+        categoryParam: '疫꿸퀣??,
         levelParam: null,
       })
     );
@@ -359,7 +359,7 @@ describe('useQuestionGenerator', () => {
     expect(generateQuestion).toHaveBeenCalledWith(
       'math',
       'World1',
-      'World1-湲곗큹',
+      'World1-疫꿸퀣??,
       1,
       'easy',
       'normal',
diff --git a/src/features/quiz/__tests__/useQuizStore.test.ts b/src/features/quiz/__tests__/useQuizStore.test.ts
index ef2f990..e57c0b6 100644
--- a/src/features/quiz/__tests__/useQuizStore.test.ts
+++ b/src/features/quiz/__tests__/useQuizStore.test.ts
@@ -1,7 +1,7 @@
-import { describe, it, expect, beforeEach } from 'vitest';
+癤퓁mport { describe, it, expect, beforeEach } from 'vitest';
 import { renderHook, act } from '@testing-library/react';
-import { useQuizStore } from '../stores/useQuizStore';
-import type { Category, Topic, GameMode, Difficulty, World } from '../types/quiz';
+import { useQuizStore } from '@/features/quiz/stores/useQuizStore';
+import type { Category, Topic, GameMode, Difficulty, World } from '@/features/quiz/types/quiz';
 
 type TimeLimit = 10 | 15 | 60 | 120 | 180;
 
@@ -101,10 +101,10 @@ describe('useQuizStore', () => {
     const { result } = renderHook(() => useQuizStore());
 
     act(() => {
-      result.current.setCategoryTopic('湲곗큹', 'World1');
+      result.current.setCategoryTopic('疫꿸퀣??, 'World1');
     });
 
-    expect(result.current.category).toBe('湲곗큹');
+    expect(result.current.category).toBe('疫꿸퀣??);
     expect(result.current.world).toBe('World1');
   });
 
@@ -129,7 +129,7 @@ describe('useQuizStore', () => {
       result.current.increaseScore(100);
       result.current.setDifficulty('hard');
       result.current.setTimeLimit(120);
-      result.current.setCategoryTopic('湲곗큹', 'World1');
+      result.current.setCategoryTopic('疫꿸퀣??, 'World1');
     });
 
     act(() => {
@@ -140,7 +140,7 @@ describe('useQuizStore', () => {
     expect(result.current.difficulty).toBe('easy');
     expect(result.current.timeLimit).toBe(60);
     // resetQuiz does not reset category/world
-    expect(result.current.category).toBe('湲곗큹');
+    expect(result.current.category).toBe('疫꿸퀣??);
     expect(result.current.world).toBe('World1');
   });
 
diff --git a/src/components/__tests__/BackpackBottomSheet.test.tsx b/src/features/quiz/components/__tests__/BackpackBottomSheet.test.tsx
similarity index 78%
rename from src/components/__tests__/BackpackBottomSheet.test.tsx
rename to src/features/quiz/components/__tests__/BackpackBottomSheet.test.tsx
index 758c8c8..8537842 100644
--- a/src/components/__tests__/BackpackBottomSheet.test.tsx
+++ b/src/features/quiz/components/__tests__/BackpackBottomSheet.test.tsx
@@ -1,10 +1,10 @@
-import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
+癤퓁mport { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
 import { render, screen, fireEvent } from '@testing-library/react';
-import { BackpackBottomSheet } from '../game/BackpackBottomSheet';
-import { useUserStore } from '../../stores/useUserStore';
+import { BackpackBottomSheet } from '@/features/quiz/components/game/BackpackBottomSheet';
+import { useUserStore } from '@/stores/useUserStore';
 
 // Mock dependencies
-vi.mock('../../stores/useUserStore', () => ({
+vi.mock('@/stores/useUserStore', () => ({
   useUserStore: vi.fn(),
 }));
 
@@ -17,7 +17,7 @@ describe('BackpackBottomSheet', () => {
     vi.useFakeTimers();
     vi.mocked(useUserStore).mockReturnValue({
       inventory: [
-        { id: 1, code: 'oxygen_tank', name: '?곗냼??, description: '?쒓컙 異붽?', quantity: 5 },
+        { id: 1, code: 'oxygen_tank', name: '?怨쀫꺖??, description: '??볦퍢 ?곕떽?', quantity: 5 },
       ],
     } as never);
   });
@@ -49,7 +49,7 @@ describe('BackpackBottomSheet', () => {
       />
     );
 
-    expect(screen.getByText(/?섏쓽 諛곕궘/)).toBeInTheDocument();
+    expect(screen.getByText(/??륁벥 獄쏄퀡沅?)).toBeInTheDocument();
   });
 
   it('should call onClose when close button is clicked', () => {
@@ -62,7 +62,7 @@ describe('BackpackBottomSheet', () => {
       />
     );
 
-    const closeButton = screen.getByText(/以鍮??꾨즺/);
+    const closeButton = screen.getByText(/餓Β???袁⑥┷/);
     fireEvent.click(closeButton);
 
     expect(mockOnClose).toHaveBeenCalled();
@@ -78,7 +78,7 @@ describe('BackpackBottomSheet', () => {
       />
     );
 
-    const itemCard = screen.getByText('?곗냼??);
+    const itemCard = screen.getByText('?怨쀫꺖??);
     fireEvent.click(itemCard);
 
     expect(mockOnToggleItem).toHaveBeenCalledWith(1);
@@ -98,7 +98,7 @@ describe('BackpackBottomSheet', () => {
       />
     );
 
-    expect(screen.getByText(/諛곕궘??鍮꾩뼱?덉뒿?덈떎/)).toBeInTheDocument();
+    expect(screen.getByText(/獄쏄퀡沅????쑴堉??됰뮸??덈뼄/)).toBeInTheDocument();
   });
 
   it('should show selected state for selected items', () => {
@@ -111,7 +111,7 @@ describe('BackpackBottomSheet', () => {
       />
     );
 
-    const itemCard = screen.getByText('?곗냼??);
+    const itemCard = screen.getByText('?怨쀫꺖??);
     const cardElement = itemCard.closest('.backpack-item-card');
     expect(cardElement).toHaveClass('selected');
   });
@@ -119,7 +119,7 @@ describe('BackpackBottomSheet', () => {
   it('should not toggle item when quantity is 0', () => {
     vi.mocked(useUserStore).mockReturnValue({
       inventory: [
-        { id: 1, code: 'oxygen_tank', name: '?곗냼??, description: '?쒓컙 異붽?', quantity: 0 },
+        { id: 1, code: 'oxygen_tank', name: '?怨쀫꺖??, description: '??볦퍢 ?곕떽?', quantity: 0 },
       ],
     } as never);
 
@@ -132,7 +132,7 @@ describe('BackpackBottomSheet', () => {
       />
     );
 
-    const itemCard = screen.getByText('?곗냼??);
+    const itemCard = screen.getByText('?怨쀫꺖??);
     fireEvent.click(itemCard);
 
     expect(mockOnToggleItem).not.toHaveBeenCalled();
@@ -148,7 +148,7 @@ describe('BackpackBottomSheet', () => {
       />
     );
 
-    const overlay = screen.getByText(/?섏쓽 諛곕궘/).closest('.backpack-sheet-overlay');
+    const overlay = screen.getByText(/??륁벥 獄쏄퀡沅?).closest('.backpack-sheet-overlay');
     if (overlay) {
       fireEvent.click(overlay);
       expect(mockOnClose).toHaveBeenCalled();
@@ -171,7 +171,7 @@ describe('BackpackBottomSheet', () => {
   it('should not display quantity when quantity is 0', () => {
     vi.mocked(useUserStore).mockReturnValue({
       inventory: [
-        { id: 1, code: 'oxygen_tank', name: '?곗냼??, description: '?쒓컙 異붽?', quantity: 0 },
+        { id: 1, code: 'oxygen_tank', name: '?怨쀫꺖??, description: '??볦퍢 ?곕떽?', quantity: 0 },
       ],
     } as never);
 
diff --git a/src/components/__tests__/CategoryList.test.tsx b/src/features/quiz/components/__tests__/CategoryList.test.tsx
similarity index 81%
rename from src/components/__tests__/CategoryList.test.tsx
rename to src/features/quiz/components/__tests__/CategoryList.test.tsx
index 9c0cf85..cada0c4 100644
--- a/src/components/__tests__/CategoryList.test.tsx
+++ b/src/features/quiz/components/__tests__/CategoryList.test.tsx
@@ -1,6 +1,6 @@
-import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
+癤퓁mport { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
 import { render, screen, fireEvent } from '@testing-library/react';
-import { CategoryList } from '../CategoryList';
+import { CategoryList } from '@/features/quiz/components/CategoryList';
 import { BrowserRouter } from 'react-router-dom';
 import { useNavigate } from 'react-router-dom';
 
@@ -16,7 +16,7 @@ vi.mock('react-router-dom', async () => {
 const mockIsFavorite = vi.fn(() => false);
 const mockAddFavorite = vi.fn();
 
-vi.mock('../../stores/useFavoriteStore', () => ({
+vi.mock('@/stores/useFavoriteStore', () => ({
   useFavoriteStore: vi.fn((selector) => {
     const state = {
       isFavorite: mockIsFavorite,
@@ -26,15 +26,15 @@ vi.mock('../../stores/useFavoriteStore', () => ({
   }),
 }));
 
-vi.mock('../../utils/scoreCalculator', () => ({
+vi.mock('@/utils/scoreCalculator', () => ({
   calculateCategoryAltitude: vi.fn(() => ({ totalAltitude: 0, totalProblems: 0 })),
 }));
 
-vi.mock('../UnknownMountainCard', () => ({
+vi.mock('@/features/quiz/components/UnknownMountainCard', () => ({
   UnknownMountainCard: () => <div data-testid="unknown-mountain">Unknown Mountain</div>,
 }));
 
-vi.mock('../Toast', () => ({
+vi.mock('@/features/quiz/Toast', () => ({
   Toast: () => null,
 }));
 
@@ -58,7 +58,7 @@ describe('CategoryList', () => {
       </BrowserRouter>
     );
 
-    expect(screen.getByText('?깅컲?????좏깮?섍린')).toBeInTheDocument();
+    expect(screen.getByText('?源낆뺘?????醫뤾문??띾┛')).toBeInTheDocument();
     expect(screen.getByTestId('unknown-mountain')).toBeInTheDocument();
   });
 
@@ -95,6 +95,6 @@ describe('CategoryList', () => {
     );
 
     // Should display title
-    expect(screen.getByText('?깅컲?????좏깮?섍린')).toBeInTheDocument();
+    expect(screen.getByText('?源낆뺘?????醫뤾문??띾┛')).toBeInTheDocument();
   });
 });
diff --git a/src/components/__tests__/ChallengeCard.test.tsx b/src/features/quiz/components/__tests__/ChallengeCard.test.tsx
similarity index 77%
rename from src/components/__tests__/ChallengeCard.test.tsx
rename to src/features/quiz/components/__tests__/ChallengeCard.test.tsx
index f432547..2b5a94f 100644
--- a/src/components/__tests__/ChallengeCard.test.tsx
+++ b/src/features/quiz/components/__tests__/ChallengeCard.test.tsx
@@ -1,18 +1,18 @@
-import { describe, it, expect, vi, beforeEach } from 'vitest';
+癤퓁mport { describe, it, expect, vi, beforeEach } from 'vitest';
 import { render, screen, waitFor } from '@testing-library/react';
 import { BrowserRouter } from 'react-router-dom';
-import { ChallengeCard } from '../ChallengeCard';
-import { getTodayChallenge } from '../../utils/challenge';
+import { ChallengeCard } from '@/features/quiz/components/ChallengeCard';
+import { getTodayChallenge } from '@/utils/challenge';
 
 // Mock dependencies
-vi.mock('../../utils/challenge', () => ({
+vi.mock('@/utils/challenge', () => ({
   getTodayChallenge: vi.fn(),
 }));
 
 const mockSetCategoryTopic = vi.fn();
 const mockSetTimeLimit = vi.fn();
 
-vi.mock('../../stores/useQuizStore', () => ({
+vi.mock('@/stores/useQuizStore', () => ({
   useQuizStore: vi.fn((selector) => {
     const state = {
       setCategoryTopic: mockSetCategoryTopic,
@@ -51,15 +51,15 @@ describe('ChallengeCard', () => {
 
     renderChallengeCard();
 
-    expect(screen.getByText('?ㅻ뒛??梨뚮┛吏')).toBeInTheDocument();
-    expect(screen.getByText('濡쒕뵫 以?..')).toBeInTheDocument();
+    expect(screen.getByText('??삳뮎??筌?슢?쏉쭪?')).toBeInTheDocument();
+    expect(screen.getByText('嚥≪뮆逾?餓?..')).toBeInTheDocument();
   });
 
   it('should render challenge when loaded', async () => {
     const mockChallenge = {
       id: '1',
       title: 'Test Challenge',
-      category: '?섑븰',
+      category: '??묐린',
       categoryId: 'math',
       topic: 'Addition',
       topicId: 'addition',
@@ -75,7 +75,7 @@ describe('ChallengeCard', () => {
       expect(screen.getByText('Test Challenge')).toBeInTheDocument();
     });
 
-    expect(screen.getByText('?꾩쟾?섍린')).toBeInTheDocument();
+    expect(screen.getByText('?袁⑹읈??띾┛')).toBeInTheDocument();
   });
 
   it('should render error state when challenge fails to load', async () => {
@@ -84,7 +84,7 @@ describe('ChallengeCard', () => {
     renderChallengeCard();
 
     await waitFor(() => {
-      expect(screen.getByText('梨뚮┛吏瑜?遺덈윭?????놁뒿?덈떎.')).toBeInTheDocument();
+      expect(screen.getByText('筌?슢?쏉쭪????븍뜄???????곷뮸??덈뼄.')).toBeInTheDocument();
     });
   });
 
@@ -92,7 +92,7 @@ describe('ChallengeCard', () => {
     const mockChallenge = {
       id: '1',
       title: 'Test Challenge',
-      category: '?섑븰',
+      category: '??묐린',
       categoryId: 'math',
       topic: 'Addition',
       topicId: 'addition',
@@ -108,7 +108,7 @@ describe('ChallengeCard', () => {
       expect(screen.getByText('Test Challenge')).toBeInTheDocument();
     });
 
-    const challengeButton = screen.getByText('?꾩쟾?섍린');
+    const challengeButton = screen.getByText('?袁⑹읈??띾┛');
     challengeButton.click();
 
     expect(mockNavigate).toHaveBeenCalled();
@@ -119,7 +119,7 @@ describe('ChallengeCard', () => {
 
     renderChallengeCard();
 
-    const challengeButton = screen.getByText('?꾩쟾?섍린');
+    const challengeButton = screen.getByText('?袁⑹읈??띾┛');
     expect(challengeButton).toBeDisabled();
   });
 
@@ -129,10 +129,10 @@ describe('ChallengeCard', () => {
     renderChallengeCard();
 
     await waitFor(() => {
-      expect(screen.getByText('梨뚮┛吏瑜?遺덈윭?????놁뒿?덈떎.')).toBeInTheDocument();
+      expect(screen.getByText('筌?슢?쏉쭪????븍뜄???????곷뮸??덈뼄.')).toBeInTheDocument();
     });
 
-    const challengeButton = screen.getByText('?꾩쟾?섍린');
+    const challengeButton = screen.getByText('?袁⑹읈??띾┛');
     expect(challengeButton).toBeDisabled();
   });
 
@@ -140,7 +140,7 @@ describe('ChallengeCard', () => {
     const mockChallenge = {
       id: '1',
       title: 'Test Challenge',
-      category: '?섑븰',
+      category: '??묐린',
       categoryId: 'math',
       topic: 'Addition',
       topicId: 'addition',
@@ -156,7 +156,7 @@ describe('ChallengeCard', () => {
       expect(screen.getByText('Test Challenge')).toBeInTheDocument();
     });
 
-    const challengeButton = screen.getByText('?꾩쟾?섍린');
+    const challengeButton = screen.getByText('?袁⑹읈??띾┛');
     expect(challengeButton).not.toBeDisabled();
   });
 
@@ -164,7 +164,7 @@ describe('ChallengeCard', () => {
     const mockChallenge = {
       id: '1',
       title: 'Test Challenge',
-      category: '?섑븰',
+      category: '??묐린',
       categoryId: 'math',
       topic: 'Addition',
       topicId: 'addition',
@@ -180,7 +180,7 @@ describe('ChallengeCard', () => {
       expect(screen.getByText('Test Challenge')).toBeInTheDocument();
     });
 
-    const challengeButton = screen.getByText('?꾩쟾?섍린');
+    const challengeButton = screen.getByText('?袁⑹읈??띾┛');
     challengeButton.click();
 
     expect(mockSetCategoryTopic).toHaveBeenCalledWith('math', 'World1');
@@ -192,7 +192,7 @@ describe('ChallengeCard', () => {
 
     renderChallengeCard();
 
-    expect(screen.getByText('?ㅻ뒛??梨뚮┛吏')).toBeInTheDocument();
-    expect(screen.getByText('?뵦')).toBeInTheDocument();
+    expect(screen.getByText('??삳뮎??筌?슢?쏉쭪?')).toBeInTheDocument();
+    expect(screen.getByText('?逾?)).toBeInTheDocument();
   });
 });
diff --git a/src/components/__tests__/ClimbGraphic.test.tsx b/src/features/quiz/components/__tests__/ClimbGraphic.test.tsx
similarity index 94%
rename from src/components/__tests__/ClimbGraphic.test.tsx
rename to src/features/quiz/components/__tests__/ClimbGraphic.test.tsx
index 02ac5f6..963e099 100644
--- a/src/components/__tests__/ClimbGraphic.test.tsx
+++ b/src/features/quiz/components/__tests__/ClimbGraphic.test.tsx
@@ -1,13 +1,13 @@
-import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
+癤퓁mport { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
 import { render, screen, fireEvent } from '@testing-library/react';
-import { ClimbGraphic, LevelButton } from '../ClimbGraphic';
+import { ClimbGraphic, LevelButton } from '@/features/quiz/components/ClimbGraphic';
 import { BrowserRouter } from 'react-router-dom';
 
 // Mock dependencies
 const mockIsLevelCleared = vi.fn(() => false);
 const mockGetNextLevel = vi.fn(() => ({ level: 1, name: 'Level 1' }));
 
-vi.mock('../../features/quiz/stores/useLevelProgressStore', () => ({
+vi.mock('@/features/quiz/stores/useLevelProgressStore', () => ({
   useLevelProgressStore: vi.fn((selector) => {
     const state = {
       isLevelCleared: mockIsLevelCleared,
@@ -20,7 +20,7 @@ vi.mock('../../features/quiz/stores/useLevelProgressStore', () => ({
   }),
 }));
 
-vi.mock('../stores/useProfileStore', () => ({
+vi.mock('@/features/quiz/stores/useProfileStore', () => ({
   useProfileStore: vi.fn((selector) => {
     const state = {
       activeProfileId: 'profile1',
@@ -30,7 +30,7 @@ vi.mock('../stores/useProfileStore', () => ({
   }),
 }));
 
-vi.mock('../components/ClimbGraphicBackgrounds', () => ({
+vi.mock('@/features/quiz/components/ClimbGraphicBackgrounds', () => ({
   ArithmeticBackground: () => <div data-testid="arithmetic-background">Arithmetic Background</div>,
   EquationsBackground: () => <div data-testid="equations-background">Equations Background</div>,
 }));
diff --git a/src/components/__tests__/ClimbGraphicBackgrounds.test.tsx b/src/features/quiz/components/__tests__/ClimbGraphicBackgrounds.test.tsx
similarity index 91%
rename from src/components/__tests__/ClimbGraphicBackgrounds.test.tsx
rename to src/features/quiz/components/__tests__/ClimbGraphicBackgrounds.test.tsx
index 9dd416e..0c2b699 100644
--- a/src/components/__tests__/ClimbGraphicBackgrounds.test.tsx
+++ b/src/features/quiz/components/__tests__/ClimbGraphicBackgrounds.test.tsx
@@ -1,6 +1,6 @@
-import { describe, it, expect } from 'vitest';
+癤퓁mport { describe, it, expect } from 'vitest';
 import { render } from '@testing-library/react';
-import { ArithmeticBackground, EquationsBackground } from '../ClimbGraphicBackgrounds';
+import { ArithmeticBackground, EquationsBackground } from '@/features/quiz/components/ClimbGraphicBackgrounds';
 
 describe('ClimbGraphicBackgrounds', () => {
   describe('ArithmeticBackground', () => {
diff --git a/src/features/quiz/components/__tests__/CountdownOverlay.test.tsx b/src/features/quiz/components/__tests__/CountdownOverlay.test.tsx
index 9212dd5..7960a8b 100644
--- a/src/features/quiz/components/__tests__/CountdownOverlay.test.tsx
+++ b/src/features/quiz/components/__tests__/CountdownOverlay.test.tsx
@@ -1,6 +1,6 @@
-import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
+癤퓁mport { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
 import { render, screen, act } from '@testing-library/react';
-import { CountdownOverlay } from '../overlays/CountdownOverlay';
+import { CountdownOverlay } from '@/features/quiz/components/overlays/CountdownOverlay';
 
 describe('CountdownOverlay', () => {
   beforeEach(() => {
diff --git a/src/features/quiz/components/__tests__/CustomKeypad.test.tsx b/src/features/quiz/components/__tests__/CustomKeypad.test.tsx
index f49ef6d..8975e4a 100644
--- a/src/features/quiz/components/__tests__/CustomKeypad.test.tsx
+++ b/src/features/quiz/components/__tests__/CustomKeypad.test.tsx
@@ -1,21 +1,21 @@
-import { describe, it, expect, beforeEach, vi } from 'vitest';
+癤퓁mport { describe, it, expect, beforeEach, vi } from 'vitest';
 import { render, screen, fireEvent } from '@testing-library/react';
-import { CustomKeypad } from '../input/CustomKeypad';
-import { useSettingsStore } from '../../../../stores/useSettingsStore';
-import { KEYPAD_SYMBOLS } from '../../../../constants/ui';
+import { CustomKeypad } from '@/features/quiz/components/input/CustomKeypad';
+import { useSettingsStore } from '@/stores/useSettingsStore';
+import { KEYPAD_SYMBOLS } from '@/constants/ui';
 
 // Mock haptic
-vi.mock('../../../../utils/haptic', () => ({
+vi.mock('@/utils/haptic', () => ({
   vibrateShort: vi.fn(),
 }));
 
 // Mock stores
-vi.mock('../../../../stores/useSettingsStore', () => ({
+vi.mock('@/stores/useSettingsStore', () => ({
   useSettingsStore: vi.fn(),
 }));
 
 // Mock haptic
-vi.mock('../../../../utils/haptic', () => ({
+vi.mock('@/utils/haptic', () => ({
   vibrateShort: vi.fn(),
 }));
 
diff --git a/src/features/quiz/components/__tests__/CyclePromotionModal.test.tsx b/src/features/quiz/components/__tests__/CyclePromotionModal.test.tsx
index 83dc570..2bd57f3 100644
--- a/src/features/quiz/components/__tests__/CyclePromotionModal.test.tsx
+++ b/src/features/quiz/components/__tests__/CyclePromotionModal.test.tsx
@@ -1,6 +1,6 @@
-import { describe, it, expect, vi, beforeEach } from 'vitest';
+癤퓁mport { describe, it, expect, vi, beforeEach } from 'vitest';
 import { render, screen, waitFor, fireEvent, act } from '@testing-library/react';
-import { CyclePromotionModal } from '../CyclePromotionModal';
+import { CyclePromotionModal } from '@/features/quiz/components/CyclePromotionModal';
 import { supabase } from '@/utils/supabaseClient';
 import type { PostgrestSingleResponse } from '@supabase/supabase-js';
 
@@ -40,8 +40,8 @@ describe('CyclePromotionModal', () => {
       />
     );
 
-    expect(screen.getByText(/?꾩꽕 ?ъ꽦/)).toBeInTheDocument();
-    expect(screen.getByText(/?댁썡???먯닔/)).toBeInTheDocument();
+    expect(screen.getByText(/?袁⑷퐬 ??苑?)).toBeInTheDocument();
+    expect(screen.getByText(/??곸뜞???癒?땾/)).toBeInTheDocument();
   });
 
   it('should call onPromote when promotion succeeds', async () => {
@@ -61,7 +61,7 @@ describe('CyclePromotionModal', () => {
       />
     );
 
-    const promoteButton = screen.getByText('?ㅼ쓬 ?꾩쟾 ?쒖옉?섍린');
+    const promoteButton = screen.getByText('??쇱벉 ?袁⑹읈 ??뽰삂??띾┛');
     fireEvent.click(promoteButton);
 
     await waitFor(() => {
@@ -86,11 +86,11 @@ describe('CyclePromotionModal', () => {
       />
     );
 
-    const promoteButton = screen.getByText('?ㅼ쓬 ?꾩쟾 ?쒖옉?섍린');
+    const promoteButton = screen.getByText('??쇱벉 ?袁⑹읈 ??뽰삂??띾┛');
     fireEvent.click(promoteButton);
 
     await waitFor(() => {
-      expect(screen.getByText('?밴툒 泥섎━ 以??ㅻ쪟媛 諛쒖깮?덉뒿?덈떎.')).toBeInTheDocument();
+      expect(screen.getByText('?諛댄닋 筌ｌ꼶??餓???살첒揶쎛 獄쏆뮇源??됰뮸??덈뼄.')).toBeInTheDocument();
     });
 
     expect(onPromote).not.toHaveBeenCalled();
@@ -113,7 +113,7 @@ describe('CyclePromotionModal', () => {
       />
     );
 
-    const promoteButton = screen.getByText('?ㅼ쓬 ?꾩쟾 ?쒖옉?섍린');
+    const promoteButton = screen.getByText('??쇱벉 ?袁⑹읈 ??뽰삂??띾┛');
     fireEvent.click(promoteButton);
 
     await waitFor(() => {
@@ -142,12 +142,12 @@ describe('CyclePromotionModal', () => {
       />
     );
 
-    const promoteButton = screen.getByText('?ㅼ쓬 ?꾩쟾 ?쒖옉?섍린');
+    const promoteButton = screen.getByText('??쇱벉 ?袁⑹읈 ??뽰삂??띾┛');
     fireEvent.click(promoteButton);
 
     // Should show loading state
     await waitFor(() => {
-      expect(screen.getByText('泥섎━ 以?..')).toBeInTheDocument();
+      expect(screen.getByText('筌ｌ꼶??餓?..')).toBeInTheDocument();
     });
 
     // Resolve the promise
@@ -182,8 +182,8 @@ describe('CyclePromotionModal', () => {
       />
     );
 
-    const promoteButton = screen.getByText('?ㅼ쓬 ?꾩쟾 ?쒖옉?섍린');
-    const cancelButton = screen.getByText('?섏쨷??);
+    const promoteButton = screen.getByText('??쇱벉 ?袁⑹읈 ??뽰삂??띾┛');
+    const cancelButton = screen.getByText('??륁㉦??);
 
     fireEvent.click(promoteButton);
 
diff --git a/src/components/__tests__/GameOverlay.test.tsx b/src/features/quiz/components/__tests__/GameOverlay.test.tsx
similarity index 91%
rename from src/components/__tests__/GameOverlay.test.tsx
rename to src/features/quiz/components/__tests__/GameOverlay.test.tsx
index 4519205..e8d78ea 100644
--- a/src/components/__tests__/GameOverlay.test.tsx
+++ b/src/features/quiz/components/__tests__/GameOverlay.test.tsx
@@ -1,9 +1,9 @@
-import { describe, it, expect, vi, beforeEach } from 'vitest';
+癤퓁mport { describe, it, expect, vi, beforeEach } from 'vitest';
 import { render, screen } from '@testing-library/react';
-import { GameOverlay } from '../game/GameOverlay';
-import { useGameStore } from '../../stores/useGameStore';
+import { GameOverlay } from '@/features/quiz/components/game/GameOverlay';
+import { useGameStore } from '@/stores/useGameStore';
 
-vi.mock('../../stores/useGameStore', () => ({
+vi.mock('@/stores/useGameStore', () => ({
   useGameStore: vi.fn(),
 }));
 
diff --git a/src/components/__tests__/ItemFeedbackOverlay.test.tsx b/src/features/quiz/components/__tests__/ItemFeedbackOverlay.test.tsx
similarity index 94%
rename from src/components/__tests__/ItemFeedbackOverlay.test.tsx
rename to src/features/quiz/components/__tests__/ItemFeedbackOverlay.test.tsx
index 9a9c311..75107bb 100644
--- a/src/components/__tests__/ItemFeedbackOverlay.test.tsx
+++ b/src/features/quiz/components/__tests__/ItemFeedbackOverlay.test.tsx
@@ -1,6 +1,6 @@
-import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
+癤퓁mport { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
 import { render, screen, act } from '@testing-library/react';
-import { ItemFeedbackOverlay } from '../game/ItemFeedbackOverlay';
+import { ItemFeedbackOverlay } from '@/features/quiz/components/game/ItemFeedbackOverlay';
 import { ItemFeedbackRef } from '@/types/feedback';
 import React, { useRef } from 'react';
 
diff --git a/src/features/quiz/components/__tests__/KeyboardInfoModal.test.tsx b/src/features/quiz/components/__tests__/KeyboardInfoModal.test.tsx
index 0149a43..30ec4cd 100644
--- a/src/features/quiz/components/__tests__/KeyboardInfoModal.test.tsx
+++ b/src/features/quiz/components/__tests__/KeyboardInfoModal.test.tsx
@@ -1,15 +1,15 @@
-import { describe, it, expect, vi, beforeEach } from 'vitest';
+癤퓁mport { describe, it, expect, vi, beforeEach } from 'vitest';
 import { render, screen, fireEvent, act } from '@testing-library/react';
-import { KeyboardInfoModal } from '../modals/KeyboardInfoModal';
-import { useSettingsStore } from '../../../../stores/useSettingsStore';
-import { APP_CONFIG } from '../../../../config/app';
+import { KeyboardInfoModal } from '@/features/quiz/components/modals/KeyboardInfoModal';
+import { useSettingsStore } from '@/stores/useSettingsStore';
+import { APP_CONFIG } from '@/config/app';
 
 // Mock dependencies
-vi.mock('../../../../stores/useSettingsStore', () => ({
+vi.mock('@/stores/useSettingsStore', () => ({
   useSettingsStore: vi.fn(),
 }));
 
-vi.mock('../input/CustomKeypad', () => ({
+vi.mock('@/features/quiz/components/input/CustomKeypad', () => ({
   CustomKeypad: ({
     onNumberClick,
     onClear,
@@ -30,7 +30,7 @@ vi.mock('../input/CustomKeypad', () => ({
   ),
 }));
 
-vi.mock('../input/QwertyKeypad', () => ({
+vi.mock('@/features/quiz/components/input/QwertyKeypad', () => ({
   QwertyKeypad: ({
     onKeyPress,
     onClear,
@@ -88,18 +88,17 @@ describe('KeyboardInfoModal', () => {
 
   it('should not render when isOpen is false', () => {
     render(<KeyboardInfoModal isOpen={false} onClose={mockOnClose} />);
-    expect(screen.queryByText(/?ㅻ낫??誘몃━蹂닿린/)).not.toBeInTheDocument();
+    expect(screen.queryByText(/??삳궖??沃섎챶?곮퉪?용┛/)).not.toBeInTheDocument();
   });
 
   it('should render modal when isOpen is true', () => {
     const { container } = render(<KeyboardInfoModal isOpen={true} onClose={mockOnClose} />);
-    // Portal???듯빐 document.body???뚮뜑留곷릺誘濡?container媛 ?꾨땶 document.body?먯꽌 李얠븘????-    expect(container).toBeTruthy();
+    // Portal?????퉸 document.body?????쐭筌띻낮由븃첋?嚥?container揶쎛 ?袁⑤빒 document.body?癒?퐣 筌≪뼚釉????    expect(container).toBeTruthy();
   });
 
   it('should call onClose when close button is clicked', () => {
     render(<KeyboardInfoModal isOpen={true} onClose={mockOnClose} />);
-    // Portal???듯빐 ?뚮뜑留곷릺誘濡?document.body?먯꽌 李얘린
+    // Portal?????퉸 ???쐭筌띻낮由븃첋?嚥?document.body?癒?퐣 筌≪뼐由?     const closeButton =
       document.querySelector('.keyboard-info-modal-close') ||
       document.querySelector('.quiz-back-button');
@@ -189,7 +188,7 @@ describe('KeyboardInfoModal', () => {
     render(<KeyboardInfoModal isOpen={true} onClose={mockOnClose} />);
 
     // Modal should render but show empty state message
-    expect(document.body).toHaveTextContent(/?ъ슜 媛?ν븳 ?ㅻ낫?쒓? ?놁뒿?덈떎/);
+    expect(document.body).toHaveTextContent(/????揶쎛?館釉???삳궖??? ??곷뮸??덈뼄/);
 
     // Restore
     (APP_CONFIG as any).SUB_TOPICS = originalSubTopics;
@@ -224,7 +223,7 @@ describe('KeyboardInfoModal', () => {
     } as unknown);
 
     // Ensure we have a language category in APP_CONFIG
-    // (Assuming it exists based on the code logic for '?덈씪媛??)
+    // (Assuming it exists based on the code logic for '??덉뵬揶쎛??)
     render(<KeyboardInfoModal isOpen={true} onClose={mockOnClose} />);
 
     // Switch to next keyboard type if available
diff --git a/src/components/__tests__/LevelListCard.test.tsx b/src/features/quiz/components/__tests__/LevelListCard.test.tsx
similarity index 78%
rename from src/components/__tests__/LevelListCard.test.tsx
rename to src/features/quiz/components/__tests__/LevelListCard.test.tsx
index ac9c591..e5b9b14 100644
--- a/src/components/__tests__/LevelListCard.test.tsx
+++ b/src/features/quiz/components/__tests__/LevelListCard.test.tsx
@@ -1,6 +1,6 @@
-import { describe, it, expect, vi, beforeEach } from 'vitest';
+癤퓁mport { describe, it, expect, vi, beforeEach } from 'vitest';
 import { render, screen } from '@testing-library/react';
-import { LevelListCard } from '../LevelListCard';
+import { LevelListCard } from '@/features/quiz/components/LevelListCard';
 
 const mockIsLevelCleared = vi.fn(() => false);
 const mockGetLevelProgress = vi.fn(() => []);
@@ -8,7 +8,7 @@ const mockGetNextLevel = vi.fn(() => 1);
 const mockIsAdmin = vi.fn(() => false);
 
 // Mock dependencies
-vi.mock('../../features/quiz/stores/useLevelProgressStore', () => ({
+vi.mock('@/features/quiz/stores/useLevelProgressStore', () => ({
   useLevelProgressStore: (selector: (state: unknown) => unknown) => {
     const state = {
       isLevelCleared: mockIsLevelCleared,
@@ -19,7 +19,7 @@ vi.mock('../../features/quiz/stores/useLevelProgressStore', () => ({
   },
 }));
 
-vi.mock('../../stores/useProfileStore', () => ({
+vi.mock('@/stores/useProfileStore', () => ({
   useProfileStore: (selector: (state: unknown) => unknown) => {
     const state = {
       isAdmin: mockIsAdmin,
@@ -28,7 +28,7 @@ vi.mock('../../stores/useProfileStore', () => ({
   },
 }));
 
-vi.mock('../UnderDevelopmentModal', () => ({
+vi.mock('@/features/quiz/UnderDevelopmentModal', () => ({
   UnderDevelopmentModal: () => null,
 }));
 
@@ -56,7 +56,7 @@ describe('LevelListCard', () => {
       />
     );
 
-    expect(screen.getByText('?덈꺼 紐⑸줉')).toBeInTheDocument();
+    expect(screen.getByText('??덇볼 筌뤴뫖以?)).toBeInTheDocument();
   });
 
   it('should render level items', () => {
@@ -88,18 +88,18 @@ describe('LevelListCard', () => {
     );
 
     // Find and click the challenge button if it exists
-    const challengeButtons = screen.queryAllByText(/?꾩쟾?섍린/);
+    const challengeButtons = screen.queryAllByText(/?袁⑹읈??띾┛/);
     if (challengeButtons.length > 0) {
       challengeButtons[0].click();
       expect(onLevelClick).toHaveBeenCalled();
     } else {
       // If no challenge button, verify component rendered
-      expect(screen.getByText('?덈꺼 紐⑸줉')).toBeInTheDocument();
+      expect(screen.getByText('??덇볼 筌뤴뫖以?)).toBeInTheDocument();
     }
   });
 
   it('should display locked status for locked levels', () => {
-    mockGetNextLevel.mockReturnValue(1); // Level 1???ㅼ쓬 ?덈꺼, Level 2???좉?
+    mockGetNextLevel.mockReturnValue(1); // Level 1????쇱벉 ??덇볼, Level 2???醫?
 
     render(
       <LevelListCard
@@ -110,13 +110,13 @@ describe('LevelListCard', () => {
       />
     );
 
-    // Level 2???좉꺼?덉뼱????(?좉? ?띿뒪?멸? ?덈뒗吏 ?뺤씤)
-    const lockedText = screen.queryByText(/?좉?/);
+    // Level 2???醫됯볼??됰선????(?醫? ??용뮞?硫? ??덈뮉筌왖 ?類ㅼ뵥)
+    const lockedText = screen.queryByText(/?醫?/);
     if (lockedText) {
       expect(lockedText).toBeInTheDocument();
     } else {
       // If locked status not shown, verify component rendered
-      expect(screen.getByText('?덈꺼 紐⑸줉')).toBeInTheDocument();
+      expect(screen.getByText('??덇볼 筌뤴뫖以?)).toBeInTheDocument();
     }
   });
 
@@ -133,7 +133,7 @@ describe('LevelListCard', () => {
       />
     );
 
-    expect(screen.getByText('?대━????)).toBeInTheDocument();
+    expect(screen.getByText('???????)).toBeInTheDocument();
   });
 
   it('should call onLockedLevelClick when locked level is clicked', () => {
@@ -151,7 +151,7 @@ describe('LevelListCard', () => {
     );
 
     // Try to find locked item and click it
-    const lockedText = screen.queryByText(/?좉?/);
+    const lockedText = screen.queryByText(/?醫?/);
     if (lockedText) {
       const lockedItem = lockedText.closest('.level-list-item');
       if (lockedItem) {
@@ -160,7 +160,7 @@ describe('LevelListCard', () => {
       }
     } else {
       // If no locked item, verify component rendered
-      expect(screen.getByText('?덈꺼 紐⑸줉')).toBeInTheDocument();
+      expect(screen.getByText('??덇볼 筌뤴뫖以?)).toBeInTheDocument();
     }
   });
 
@@ -186,10 +186,10 @@ describe('LevelListCard', () => {
       />
     );
 
-    expect(screen.getByText(/理쒓퀬:/)).toBeInTheDocument();
+    expect(screen.getByText(/筌ㅼ뮄??/)).toBeInTheDocument();
   });
 
-  it('should show "?ㅼ떆?섍린" button for cleared levels', () => {
+  it('should show "??쇰뻻??띾┛" button for cleared levels', () => {
     mockIsLevelCleared.mockImplementation((cat: string, sub: string, level: number) => level === 1);
     mockGetNextLevel.mockReturnValue(2);
 
@@ -202,8 +202,8 @@ describe('LevelListCard', () => {
       />
     );
 
-    // Level 1 should be cleared and show "?ㅼ떆?섍린" button
-    const retryButtons = screen.queryAllByText('?ㅼ떆?섍린 >');
+    // Level 1 should be cleared and show "??쇰뻻??띾┛" button
+    const retryButtons = screen.queryAllByText('??쇰뻻??띾┛ >');
     expect(retryButtons.length).toBeGreaterThan(0);
   });
 });
diff --git a/src/features/quiz/components/__tests__/ModeSelectModal.test.tsx b/src/features/quiz/components/__tests__/ModeSelectModal.test.tsx
index dca75cb..f1f5f4a 100644
--- a/src/features/quiz/components/__tests__/ModeSelectModal.test.tsx
+++ b/src/features/quiz/components/__tests__/ModeSelectModal.test.tsx
@@ -1,6 +1,6 @@
-import { describe, it, expect, vi } from 'vitest';
+癤퓁mport { describe, it, expect, vi } from 'vitest';
 import { render, screen } from '@testing-library/react';
-import { ModeSelectModal } from '../ModeSelectModal';
+import { ModeSelectModal } from '@/features/quiz/components/modals/ModeSelectModal';
 
 describe('ModeSelectModal', () => {
   it('should not render when isOpen is false', () => {
@@ -28,8 +28,8 @@ describe('ModeSelectModal', () => {
     );
 
     expect(screen.getByText(/Level 1/)).toBeInTheDocument();
-    expect(screen.getByText('????댄깮')).toBeInTheDocument();
-    expect(screen.getByText('?쒕컮?대쾶')).toBeInTheDocument();
+    expect(screen.getByText('??????꾧문')).toBeInTheDocument();
+    expect(screen.getByText('??뺤뺍??苡?)).toBeInTheDocument();
   });
 
   it('should call onSelectMode when time-attack is selected', () => {
@@ -45,7 +45,7 @@ describe('ModeSelectModal', () => {
       />
     );
 
-    const timeAttackButton = screen.getByText('????댄깮').closest('button');
+    const timeAttackButton = screen.getByText('??????꾧문').closest('button');
     timeAttackButton?.click();
 
     expect(onSelectMode).toHaveBeenCalledWith('time-attack');
@@ -65,7 +65,7 @@ describe('ModeSelectModal', () => {
       />
     );
 
-    const survivalButton = screen.getByText('?쒕컮?대쾶').closest('button');
+    const survivalButton = screen.getByText('??뺤뺍??苡?).closest('button');
     survivalButton?.click();
 
     expect(onSelectMode).toHaveBeenCalledWith('survival');
@@ -84,7 +84,7 @@ describe('ModeSelectModal', () => {
       />
     );
 
-    const cancelButton = screen.getByText('痍⑥냼');
+    const cancelButton = screen.getByText('?띯뫁??);
     cancelButton.click();
 
     expect(onClose).toHaveBeenCalled();
@@ -155,7 +155,7 @@ describe('ModeSelectModal', () => {
       />
     );
 
-    expect(screen.getByText('60珥덇컙 理쒓퀬 ?먯닔 ?꾩쟾')).toBeInTheDocument();
-    expect(screen.getByText('?由??뚭퉴吏 ?꾩쟾')).toBeInTheDocument();
+    expect(screen.getByText('60?λ뜃而?筌ㅼ뮄???癒?땾 ?袁⑹읈')).toBeInTheDocument();
+    expect(screen.getByText('???????돱筌왖 ?袁⑹읈')).toBeInTheDocument();
   });
 });
diff --git a/src/features/quiz/components/__tests__/MyRecordCard.test.tsx b/src/features/quiz/components/__tests__/MyRecordCard.test.tsx
index 1c87559..1517cb2 100644
--- a/src/features/quiz/components/__tests__/MyRecordCard.test.tsx
+++ b/src/features/quiz/components/__tests__/MyRecordCard.test.tsx
@@ -1,10 +1,10 @@
-import { describe, it, expect, vi, beforeEach } from 'vitest';
+癤퓁mport { describe, it, expect, vi, beforeEach } from 'vitest';
 import { render, screen, waitFor, act } from '@testing-library/react';
-import { MyRecordCard } from '../MyRecordCard';
+import { MyRecordCard } from '@/features/quiz/components/MyRecordCard';
 
 // Mock useLevelProgressStore
 const mockGetBestRecords = vi.fn(() => ({ 'time-attack': null, survival: null }));
-vi.mock('../../features/quiz/stores/useLevelProgressStore', () => ({
+vi.mock('@/features/quiz/stores/useLevelProgressStore', () => ({
   useLevelProgressStore: (selector: (state: any) => any) => {
     const mockState = {
       getBestRecords: mockGetBestRecords,
@@ -21,9 +21,9 @@ describe('MyRecordCard', () => {
 
   it('should render loading state initially', async () => {
     await act(async () => {
-      render(<MyRecordCard world="math" category="arithmetic" categoryName="?ъ튃?곗궛" />);
+      render(<MyRecordCard world="math" category="arithmetic" categoryName="????怨쀪텦" />);
     });
-    expect(screen.queryByText('?ъ튃?곗궛 理쒓퀬 湲곕줉')).not.toBeInTheDocument();
+    expect(screen.queryByText('????怨쀪텦 筌ㅼ뮄??疫꿸퀡以?)).not.toBeInTheDocument();
   });
 
   it('should render records after loading', async () => {
@@ -33,40 +33,40 @@ describe('MyRecordCard', () => {
     });
 
     await act(async () => {
-      render(<MyRecordCard world="math" category="arithmetic" categoryName="?ъ튃?곗궛" />);
+      render(<MyRecordCard world="math" category="arithmetic" categoryName="????怨쀪텦" />);
     });
 
     await waitFor(
       () => {
-        expect(screen.getByText('?ъ튃?곗궛 理쒓퀬 湲곕줉')).toBeInTheDocument();
+        expect(screen.getByText('????怨쀪텦 筌ㅼ뮄??疫꿸퀡以?)).toBeInTheDocument();
       },
       { timeout: 2000 }
     );
 
-    expect(screen.getByText(/????댄깮:/)).toBeInTheDocument();
-    expect(screen.getByText(/?쒕컮?대쾶:/)).toBeInTheDocument();
+    expect(screen.getByText(/??????꾧문:/)).toBeInTheDocument();
+    expect(screen.getByText(/??뺤뺍??苡?/)).toBeInTheDocument();
     expect(screen.getByText('1,000m')).toBeInTheDocument();
     expect(screen.getByText('2,000m')).toBeInTheDocument();
   });
 
-  it('should display "湲곕줉 ?놁쓬" when no records exist', async () => {
+  it('should display "疫꿸퀡以???곸벉" when no records exist', async () => {
     mockGetBestRecords.mockReturnValue({
       'time-attack': null,
       survival: null,
     });
 
     await act(async () => {
-      render(<MyRecordCard world="math" category="arithmetic" categoryName="?ъ튃?곗궛" />);
+      render(<MyRecordCard world="math" category="arithmetic" categoryName="????怨쀪텦" />);
     });
 
     await waitFor(
       () => {
-        expect(screen.getByText('?ъ튃?곗궛 理쒓퀬 湲곕줉')).toBeInTheDocument();
+        expect(screen.getByText('????怨쀪텦 筌ㅼ뮄??疫꿸퀡以?)).toBeInTheDocument();
       },
       { timeout: 2000 }
     );
 
-    const noRecordTexts = screen.getAllByText('湲곕줉 ?놁쓬');
+    const noRecordTexts = screen.getAllByText('疫꿸퀡以???곸벉');
     expect(noRecordTexts.length).toBeGreaterThan(0);
   });
 
@@ -77,18 +77,18 @@ describe('MyRecordCard', () => {
     });
 
     await act(async () => {
-      render(<MyRecordCard world="math" category="arithmetic" categoryName="?ъ튃?곗궛" />);
+      render(<MyRecordCard world="math" category="arithmetic" categoryName="????怨쀪텦" />);
     });
 
     await waitFor(
       () => {
-        expect(screen.getByText('?ъ튃?곗궛 理쒓퀬 湲곕줉')).toBeInTheDocument();
+        expect(screen.getByText('????怨쀪텦 筌ㅼ뮄??疫꿸퀡以?)).toBeInTheDocument();
       },
       { timeout: 2000 }
     );
 
     expect(screen.getByText('1,500m')).toBeInTheDocument();
-    expect(screen.getByText('湲곕줉 ?놁쓬')).toBeInTheDocument();
+    expect(screen.getByText('疫꿸퀡以???곸벉')).toBeInTheDocument();
   });
 
   it('should display only survival record when time-attack is null', async () => {
@@ -98,18 +98,18 @@ describe('MyRecordCard', () => {
     });
 
     await act(async () => {
-      render(<MyRecordCard world="math" category="arithmetic" categoryName="?ъ튃?곗궛" />);
+      render(<MyRecordCard world="math" category="arithmetic" categoryName="????怨쀪텦" />);
     });
 
     await waitFor(
       () => {
-        expect(screen.getByText('?ъ튃?곗궛 理쒓퀬 湲곕줉')).toBeInTheDocument();
+        expect(screen.getByText('????怨쀪텦 筌ㅼ뮄??疫꿸퀡以?)).toBeInTheDocument();
       },
       { timeout: 2000 }
     );
 
     expect(screen.getByText('3,000m')).toBeInTheDocument();
-    expect(screen.getByText('湲곕줉 ?놁쓬')).toBeInTheDocument();
+    expect(screen.getByText('疫꿸퀡以???곸벉')).toBeInTheDocument();
   });
 
   it('should format numbers with locale string', async () => {
@@ -119,12 +119,12 @@ describe('MyRecordCard', () => {
     });
 
     await act(async () => {
-      render(<MyRecordCard world="math" category="arithmetic" categoryName="?ъ튃?곗궛" />);
+      render(<MyRecordCard world="math" category="arithmetic" categoryName="????怨쀪텦" />);
     });
 
     await waitFor(
       () => {
-        expect(screen.getByText('?ъ튃?곗궛 理쒓퀬 湲곕줉')).toBeInTheDocument();
+        expect(screen.getByText('????怨쀪텦 筌ㅼ뮄??疫꿸퀡以?)).toBeInTheDocument();
       },
       { timeout: 2000 }
     );
diff --git a/src/features/quiz/components/__tests__/QuizAnswerArea.test.tsx b/src/features/quiz/components/__tests__/QuizAnswerArea.test.tsx
index 6be5831..515c0cf 100644
--- a/src/features/quiz/components/__tests__/QuizAnswerArea.test.tsx
+++ b/src/features/quiz/components/__tests__/QuizAnswerArea.test.tsx
@@ -1,11 +1,11 @@
-import { describe, it, expect, vi, beforeEach } from 'vitest';
+癤퓁mport { describe, it, expect, vi, beforeEach } from 'vitest';
 import { render, screen, fireEvent, act } from '@testing-library/react';
-import { QuizAnswerArea } from '../QuizAnswerArea';
+import { QuizAnswerArea } from '@/features/quiz/components/QuizAnswerArea';
 import React from 'react';
-import { useQuiz } from '../../contexts/QuizContext';
+import { useQuiz } from '@/features/quiz/contexts/QuizContext';
 
 // Mock useQuiz
-vi.mock('../../contexts/QuizContext', () => ({
+vi.mock('@/features/quiz/contexts/QuizContext', () => ({
   useQuiz: vi.fn(),
 }));
 
@@ -19,7 +19,7 @@ describe('QuizAnswerArea', () => {
     quizState: {
       currentQuestion: {
         id: 'q1',
-        question: '?뚯뒪??吏덈Ц',
+        question: '???뮞??筌욌뜄揆',
         answer: '10',
         inputType: 'number',
       },
@@ -81,7 +81,7 @@ describe('QuizAnswerArea', () => {
 
   it('should render input for numeric quiz', () => {
     render(<QuizAnswerArea />);
-    const input = screen.getByPlaceholderText('?뺣떟 ?낅젰');
+    const input = screen.getByPlaceholderText('?類ｋ뼗 ??낆젾');
     expect(input).toBeInTheDocument();
     expect(input).toHaveAttribute('type', 'text');
     expect(input).toHaveAttribute('inputMode', 'numeric');
@@ -89,7 +89,7 @@ describe('QuizAnswerArea', () => {
 
   it('should handle numeric input change', () => {
     render(<QuizAnswerArea />);
-    const input = screen.getByPlaceholderText('?뺣떟 ?낅젰');
+    const input = screen.getByPlaceholderText('?類ｋ뼗 ??낆젾');
 
     fireEvent.change(input, { target: { value: '123' } });
     expect(mockSetAnswerInput).toHaveBeenCalledWith('123');
@@ -98,7 +98,7 @@ describe('QuizAnswerArea', () => {
 
   it('should filter non-numeric input for basic numeric quiz', () => {
     render(<QuizAnswerArea />);
-    const input = screen.getByPlaceholderText('?뺣떟 ?낅젰');
+    const input = screen.getByPlaceholderText('?類ｋ뼗 ??낆젾');
 
     fireEvent.change(input, { target: { value: 'abc123' } });
     expect(mockSetAnswerInput).toHaveBeenCalledWith('123');
@@ -114,7 +114,7 @@ describe('QuizAnswerArea', () => {
       },
     });
     render(<QuizAnswerArea />);
-    const input = screen.getByPlaceholderText('?뺣떟 ?낅젰');
+    const input = screen.getByPlaceholderText('?類ｋ뼗 ??낆젾');
 
     fireEvent.change(input, { target: { value: '-5' } });
     expect(mockSetAnswerInput).toHaveBeenCalledWith('-5');
@@ -130,7 +130,7 @@ describe('QuizAnswerArea', () => {
       },
     });
     render(<QuizAnswerArea />);
-    const input = screen.getByPlaceholderText('濡쒕쭏吏 ?낅젰 (?? a, ki)');
+    const input = screen.getByPlaceholderText('嚥≪뮆彛륅쭪? ??낆젾 (?? a, ki)');
 
     fireEvent.change(input, { target: { value: 'ka123' } });
     expect(mockSetAnswerInput).toHaveBeenCalledWith('ka');
@@ -149,7 +149,7 @@ describe('QuizAnswerArea', () => {
     });
 
     // Mock CoordinateGrid to avoid deep testing
-    vi.mock('../CoordinateGrid', () => ({
+    vi.mock('@/features/quiz/components/CoordinateGrid', () => ({
       CoordinateGrid: ({ onShoot }: any) => (
         <button onClick={() => onShoot(3, 4)}>Shoot 3,4</button>
       ),
@@ -174,7 +174,7 @@ describe('QuizAnswerArea', () => {
 
   it('should handle Enter key press', () => {
     render(<QuizAnswerArea />);
-    const input = screen.getByPlaceholderText('?뺣떟 ?낅젰');
+    const input = screen.getByPlaceholderText('?類ｋ뼗 ??낆젾');
 
     fireEvent.keyDown(input, { key: 'Enter' });
     expect(mockHandleSubmit).toHaveBeenCalled();
@@ -191,7 +191,7 @@ describe('QuizAnswerArea', () => {
     });
     render(<QuizAnswerArea />);
     expect(screen.getByText('42')).toBeInTheDocument();
-    expect(screen.queryByPlaceholderText('?뺣떟 ?낅젰')).not.toBeInTheDocument();
+    expect(screen.queryByPlaceholderText('?類ｋ뼗 ??낆젾')).not.toBeInTheDocument();
   });
 
   it('should handle input filtering for allowNegative with multiple signs', () => {
@@ -204,7 +204,7 @@ describe('QuizAnswerArea', () => {
       },
     });
     render(<QuizAnswerArea />);
-    const input = screen.getByPlaceholderText('?뺣떟 ?낅젰');
+    const input = screen.getByPlaceholderText('?類ｋ뼗 ??낆젾');
 
     // Multiple minus signs should result in only one at the start
     fireEvent.change(input, { target: { value: '--5' } });
@@ -218,7 +218,7 @@ describe('QuizAnswerArea', () => {
   it('should enforce max length for different input types', () => {
     // Numeric basic: max 6
     render(<QuizAnswerArea />);
-    const input = screen.getByPlaceholderText('?뺣떟 ?낅젰');
+    const input = screen.getByPlaceholderText('?類ｋ뼗 ??낆젾');
     fireEvent.change(input, { target: { value: '1234567' } });
     expect(mockSetAnswerInput).toHaveBeenCalledWith('123456');
 
@@ -232,7 +232,7 @@ describe('QuizAnswerArea', () => {
       },
     });
     render(<QuizAnswerArea />);
-    const jaInput = screen.getByPlaceholderText('濡쒕쭏吏 ?낅젰 (?? a, ki)');
+    const jaInput = screen.getByPlaceholderText('嚥≪뮆彛륅쭪? ??낆젾 (?? a, ki)');
     fireEvent.change(jaInput, { target: { value: 'a'.repeat(21) } });
     expect(mockSetAnswerInput).toHaveBeenCalledWith('a'.repeat(20));
   });
diff --git a/src/features/quiz/components/__tests__/QuizCard.test.tsx b/src/features/quiz/components/__tests__/QuizCard.test.tsx
index 92eb4db..78585a2 100644
--- a/src/features/quiz/components/__tests__/QuizCard.test.tsx
+++ b/src/features/quiz/components/__tests__/QuizCard.test.tsx
@@ -1,22 +1,22 @@
-import { describe, it, expect, beforeEach, vi } from 'vitest';
+癤퓁mport { describe, it, expect, beforeEach, vi } from 'vitest';
 import { render, screen, fireEvent } from '@testing-library/react';
 import '@testing-library/jest-dom';
-import { QuizCard } from '../QuizCard';
-import type { QuizQuestion, Category, GameMode } from '../../types/quiz';
-import { QuizContext } from '../../contexts/QuizContext';
+import { QuizCard } from '@/features/quiz/components/QuizCard';
+import type { QuizQuestion, Category, GameMode } from '@/features/quiz/types/quiz';
+import { QuizContext } from '@/features/quiz/contexts/QuizContext';
 
 // Mock dependencies
-vi.mock('../../utils/debugLogger', () => ({
+vi.mock('@/features/quiz/debugLogger', () => ({
   sendDebugLog: vi.fn(),
 }));
 
-vi.mock('../TimerCircle', () => ({
+vi.mock('@/features/quiz/components/TimerCircle', () => ({
   TimerCircle: ({ duration }: { duration: number; onComplete: () => void }) => (
     <div data-testid="timer-circle">Timer: {duration}s</div>
   ),
 }));
 
-vi.mock('../input/CustomKeypad', () => ({
+vi.mock('@/features/quiz/components/input/CustomKeypad', () => ({
   CustomKeypad: ({
     onNumberClick,
     onBackspace,
@@ -42,7 +42,7 @@ vi.mock('../input/CustomKeypad', () => ({
   ),
 }));
 
-vi.mock('../input/QwertyKeypad', () => ({
+vi.mock('@/features/quiz/components/input/QwertyKeypad', () => ({
   QwertyKeypad: ({
     onKeyPress,
     disabled,
@@ -69,8 +69,8 @@ describe('QuizCard', () => {
       currentQuestion: mockQuestion,
       answerInput: '',
       displayValue: '',
-      category: '?섑븰' as Category,
-      topic: '?㏃뀍',
+      category: '??묐린' as Category,
+      topic: '??껊?,
       categoryParam: 'math',
       subParam: 'addition',
       levelParam: null,
@@ -198,7 +198,7 @@ describe('QuizCard', () => {
         subParam: 'addition',
       },
     });
-    expect(screen.getByText('臾몄젣瑜??앹꽦?섎뒗 以?..')).toBeInTheDocument();
+    expect(screen.getByText('?얜챷?ｇ몴???밴쉐??롫뮉 餓?..')).toBeInTheDocument();
   });
 
   it('should display answer when showAnswer is true', () => {
@@ -206,7 +206,7 @@ describe('QuizCard', () => {
       ...defaultMockValues,
       quizState: { ...defaultMockValues.quizState, showAnswer: true },
     });
-    expect(screen.getByText(/?뺣떟:/)).toBeInTheDocument();
+    expect(screen.getByText(/?類ｋ뼗:/)).toBeInTheDocument();
     expect(screen.getByText('8')).toBeInTheDocument();
   });
 
@@ -259,7 +259,7 @@ describe('QuizCard', () => {
       quizState: { ...defaultMockValues.quizState, useSystemKeyboard: true, answerInput: '8' },
       quizAnimations: { ...defaultMockValues.quizAnimations, isPaused: true, isInputPaused: true },
     });
-    const submitButton = screen.getByText('?쒖텧');
+    const submitButton = screen.getByText('??뽱뀱');
     expect(submitButton).toBeDisabled();
   });
 
@@ -268,11 +268,11 @@ describe('QuizCard', () => {
       ...defaultMockValues,
       quizState: {
         ...defaultMockValues.quizState,
-        category: '?섑븰',
+        category: '??묐린',
         worldParam: 'World1',
       },
     });
-    // Renders as "{category} 쨌 {worldName}" in QuizCard.tsx
-    expect(screen.getByText(/?섑븰 쨌 World 1: ?섎━遊?)).toBeInTheDocument();
+    // Renders as "{category} 夷?{worldName}" in QuizCard.tsx
+    expect(screen.getByText(/??묐린 夷?World 1: ??롡봺??)).toBeInTheDocument();
   });
 });
diff --git a/src/features/quiz/components/__tests__/QwertyKeypad.test.tsx b/src/features/quiz/components/__tests__/QwertyKeypad.test.tsx
index 6321542..c1eb583 100644
--- a/src/features/quiz/components/__tests__/QwertyKeypad.test.tsx
+++ b/src/features/quiz/components/__tests__/QwertyKeypad.test.tsx
@@ -1,6 +1,6 @@
-import { describe, it, expect, vi, beforeEach } from 'vitest';
+癤퓁mport { describe, it, expect, vi, beforeEach } from 'vitest';
 import { render, screen } from '@testing-library/react';
-import { QwertyKeypad } from '../input/QwertyKeypad';
+import { QwertyKeypad } from '@/features/quiz/components/input/QwertyKeypad';
 
 // Mock navigator.vibrate
 Object.defineProperty(navigator, 'vibrate', {
@@ -45,7 +45,7 @@ describe('QwertyKeypad', () => {
   it('should call onBackspace when backspace button is clicked', () => {
     render(<QwertyKeypad {...defaultProps} />);
     // Backspace button is rendered in the second row
-    const backspaceButton = screen.getByText('??);
+    const backspaceButton = screen.getByText('??);
     backspaceButton.click();
 
     expect(defaultProps.onBackspace).toHaveBeenCalled();
@@ -61,14 +61,14 @@ describe('QwertyKeypad', () => {
 
   it('should show negative button in number mode when allowNegative is true', () => {
     render(<QwertyKeypad {...defaultProps} mode="number" allowNegative={true} />);
-    // Negative button is rendered as 짹 symbol
-    const negativeButton = screen.getByText('짹');
+    // Negative button is rendered as 吏?symbol
+    const negativeButton = screen.getByText('吏?);
     expect(negativeButton).toBeInTheDocument();
   });
 
   it('should not show negative button in text mode', () => {
     render(<QwertyKeypad {...defaultProps} mode="text" allowNegative={true} />);
-    expect(screen.queryByText('짹')).not.toBeInTheDocument();
+    expect(screen.queryByText('吏?)).not.toBeInTheDocument();
   });
 
   it('should call onClear when clear is triggered', () => {
@@ -80,7 +80,7 @@ describe('QwertyKeypad', () => {
 
   it('should call onSubmit when submit button is clicked', () => {
     render(<QwertyKeypad {...defaultProps} mode="text" />);
-    const submitButton = screen.getByText('??);
+    const submitButton = screen.getByText('??);
     submitButton.click();
 
     expect(defaultProps.onSubmit).toHaveBeenCalled();
@@ -88,7 +88,7 @@ describe('QwertyKeypad', () => {
 
   it('should not call onSubmit when disabled', () => {
     render(<QwertyKeypad {...defaultProps} mode="text" disabled={true} />);
-    const submitButton = screen.getByText('??);
+    const submitButton = screen.getByText('??);
     submitButton.click();
 
     expect(defaultProps.onSubmit).not.toHaveBeenCalled();
@@ -175,7 +175,7 @@ describe('QwertyKeypad', () => {
 
   it('should call onKeyPress with "-" when negative button is clicked', () => {
     render(<QwertyKeypad {...defaultProps} mode="number" allowNegative={true} />);
-    const negativeButton = screen.getByText('짹');
+    const negativeButton = screen.getByText('吏?);
     negativeButton.click();
 
     expect(defaultProps.onKeyPress).toHaveBeenCalledWith('-');
diff --git a/src/components/__tests__/SafetyRopeOverlay.test.tsx b/src/features/quiz/components/__tests__/SafetyRopeOverlay.test.tsx
similarity index 90%
rename from src/components/__tests__/SafetyRopeOverlay.test.tsx
rename to src/features/quiz/components/__tests__/SafetyRopeOverlay.test.tsx
index 42e82e2..f327b3f 100644
--- a/src/components/__tests__/SafetyRopeOverlay.test.tsx
+++ b/src/features/quiz/components/__tests__/SafetyRopeOverlay.test.tsx
@@ -1,6 +1,6 @@
-import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
+癤퓁mport { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
 import { render, screen, act } from '@testing-library/react';
-import { SafetyRopeOverlay } from '../game/SafetyRopeOverlay';
+import { SafetyRopeOverlay } from '@/features/quiz/components/game/SafetyRopeOverlay';
 
 describe('SafetyRopeOverlay', () => {
   const mockOnAnimationComplete = vi.fn();
@@ -32,7 +32,7 @@ describe('SafetyRopeOverlay', () => {
     render(<SafetyRopeOverlay isVisible={true} onAnimationComplete={mockOnAnimationComplete} />);
 
     expect(screen.getByText('SAFE!')).toBeInTheDocument();
-    expect(screen.getByText('?덉쟾 濡쒗봽媛 1??諛⑹뼱?덉뒿?덈떎')).toBeInTheDocument();
+    expect(screen.getByText('??됱읈 嚥≪뮉遊썲첎? 1??獄쎻뫗堉??됰뮸??덈뼄')).toBeInTheDocument();
   });
 
   it('should call onAnimationComplete after 1.5 seconds', () => {
diff --git a/src/features/quiz/components/__tests__/StaminaGauge.test.tsx b/src/features/quiz/components/__tests__/StaminaGauge.test.tsx
index 5817205..ead9171 100644
--- a/src/features/quiz/components/__tests__/StaminaGauge.test.tsx
+++ b/src/features/quiz/components/__tests__/StaminaGauge.test.tsx
@@ -1,10 +1,10 @@
-import { describe, it, expect, beforeEach, vi } from 'vitest';
+癤퓁mport { describe, it, expect, beforeEach, vi } from 'vitest';
 import { render, screen } from '@testing-library/react';
-import { StaminaGauge } from '../StaminaGauge';
-import { useUserStore } from '../../stores/useUserStore';
+import { StaminaGauge } from '@/features/quiz/components/StaminaGauge';
+import { useUserStore } from '@/features/quiz/stores/useUserStore';
 
 // Mock useUserStore
-vi.mock('../../stores/useUserStore', () => ({
+vi.mock('@/features/quiz/stores/useUserStore', () => ({
   useUserStore: vi.fn(),
 }));
 
diff --git a/src/components/__tests__/StaminaWarningModal.test.tsx b/src/features/quiz/components/__tests__/StaminaWarningModal.test.tsx
similarity index 71%
rename from src/components/__tests__/StaminaWarningModal.test.tsx
rename to src/features/quiz/components/__tests__/StaminaWarningModal.test.tsx
index 8bf3fa2..5f527a6 100644
--- a/src/components/__tests__/StaminaWarningModal.test.tsx
+++ b/src/features/quiz/components/__tests__/StaminaWarningModal.test.tsx
@@ -1,6 +1,6 @@
-import { describe, it, expect, vi, beforeEach } from 'vitest';
+癤퓁mport { describe, it, expect, vi, beforeEach } from 'vitest';
 import { render, screen, fireEvent } from '@testing-library/react';
-import { StaminaWarningModal } from '../game/StaminaWarningModal';
+import { StaminaWarningModal } from '@/features/quiz/components/game/StaminaWarningModal';
 
 describe('StaminaWarningModal', () => {
   const mockOnClose = vi.fn();
@@ -21,7 +21,7 @@ describe('StaminaWarningModal', () => {
       />
     );
 
-    expect(screen.queryByText(/泥대젰??遺議깊빀?덈떎/)).not.toBeInTheDocument();
+    expect(screen.queryByText(/筌ｋ?????봔鈺곌퉲鍮??덈뼄/)).not.toBeInTheDocument();
   });
 
   it('should render when isOpen is true', () => {
@@ -34,7 +34,7 @@ describe('StaminaWarningModal', () => {
       />
     );
 
-    expect(screen.getByText(/泥대젰??遺議깊빀?덈떎/)).toBeInTheDocument();
+    expect(screen.getByText(/筌ｋ?????봔鈺곌퉲鍮??덈뼄/)).toBeInTheDocument();
   });
 
   it('should call onPlayAnyway when play anyway button is clicked', () => {
@@ -47,7 +47,7 @@ describe('StaminaWarningModal', () => {
       />
     );
 
-    const playButton = screen.getByText(/洹몃깷 ?뚮젅?댄븯湲?);
+    const playButton = screen.getByText(/域밸챶源????쟿??꾨릭疫?);
     fireEvent.click(playButton);
 
     expect(mockOnPlayAnyway).toHaveBeenCalled();
@@ -63,7 +63,7 @@ describe('StaminaWarningModal', () => {
       />
     );
 
-    const adButton = screen.getByText(/愿묎퀬 蹂닿퀬 異⑹쟾/);
+    const adButton = screen.getByText(/?용쵌??癰귣떯???겸뫗??);
     fireEvent.click(adButton);
 
     expect(mockOnWatchAd).toHaveBeenCalled();
@@ -79,7 +79,7 @@ describe('StaminaWarningModal', () => {
       />
     );
 
-    const cancelButton = screen.getByText(/痍⑥냼/);
+    const cancelButton = screen.getByText(/?띯뫁??);
     fireEvent.click(cancelButton);
 
     expect(mockOnClose).toHaveBeenCalled();
@@ -95,11 +95,11 @@ describe('StaminaWarningModal', () => {
       />
     );
 
-    expect(screen.getByText(/?ㅽ깭誘몃굹媛 遺議깊븯??)).toBeInTheDocument();
-    expect(screen.getByText(/吏移??곹깭/)).toBeInTheDocument();
-    expect(screen.getByText(/?띾뱷 ?먯닔? 誘몃꽕?꾩씠/)).toBeInTheDocument();
+    expect(screen.getByText(/??쎄묶沃섎챶援밧첎? ?봔鈺곌퉲釉??)).toBeInTheDocument();
+    expect(screen.getByText(/筌왖燁??怨밴묶/)).toBeInTheDocument();
+    expect(screen.getByText(/??얜굣 ?癒?땾?? 沃섎챶苑?袁⑹뵠/)).toBeInTheDocument();
     expect(screen.getByText(/80%/)).toBeInTheDocument();
-    expect(screen.getByText(/?붾㈃??遺됯쾶 ?대몢?뚯쭛?덈떎/)).toBeInTheDocument();
+    expect(screen.getByText(/?遺얇늺???븍맦苡???紐???춿??덈뼄/)).toBeInTheDocument();
   });
 
   it('should display warning icon', () => {
@@ -112,7 +112,7 @@ describe('StaminaWarningModal', () => {
       />
     );
 
-    expect(screen.getByText('??)).toBeInTheDocument();
+    expect(screen.getByText('??)).toBeInTheDocument();
   });
 
   it('should display ad button with icon', () => {
@@ -125,9 +125,9 @@ describe('StaminaWarningModal', () => {
       />
     );
 
-    const adButton = screen.getByText(/愿묎퀬 蹂닿퀬 異⑹쟾/);
+    const adButton = screen.getByText(/?용쵌??癰귣떯???겸뫗??);
     expect(adButton).toBeInTheDocument();
-    expect(screen.getByText('?벟')).toBeInTheDocument();
+    expect(screen.getByText('?踰?)).toBeInTheDocument();
   });
 
   it('should display warning message', () => {
@@ -140,6 +140,6 @@ describe('StaminaWarningModal', () => {
       />
     );
 
-    expect(screen.getByText(/泥대젰??遺議깊빀?덈떎/)).toBeInTheDocument();
+    expect(screen.getByText(/筌ｋ?????봔鈺곌퉲鍮??덈뼄/)).toBeInTheDocument();
   });
 });
diff --git a/src/features/quiz/components/__tests__/StatusCard.test.tsx b/src/features/quiz/components/__tests__/StatusCard.test.tsx
index 076da86..d4a6294 100644
--- a/src/features/quiz/components/__tests__/StatusCard.test.tsx
+++ b/src/features/quiz/components/__tests__/StatusCard.test.tsx
@@ -1,11 +1,11 @@
-import { describe, it, expect, vi, beforeEach } from 'vitest';
+癤퓁mport { describe, it, expect, vi, beforeEach } from 'vitest';
 import { render, screen, act, waitFor, fireEvent } from '@testing-library/react';
 import { BrowserRouter } from 'react-router-dom';
-import { StatusCard } from '../StatusCard';
+import { StatusCard } from '@/features/quiz/components/StatusCard';
 import { useLevelProgressStore } from '@/features/quiz';
 
 // Mock dependencies
-vi.mock('../../features/quiz/stores/useLevelProgressStore', () => ({
+vi.mock('@/features/quiz/stores/useLevelProgressStore', () => ({
   useLevelProgressStore: {
     getState: vi.fn(() => ({
       progress: {},
@@ -68,7 +68,7 @@ describe('StatusCard', () => {
     });
 
     await waitFor(() => {
-      expect(screen.getByText('?섏쓽 ??궧')).toBeInTheDocument();
+      expect(screen.getByText('??륁벥 ??沅?)).toBeInTheDocument();
     });
   });
 
@@ -82,7 +82,7 @@ describe('StatusCard', () => {
     });
 
     await waitFor(() => {
-      const detailButton = screen.queryByText('?먯꽭??);
+      const detailButton = screen.queryByText('?癒?쉭??);
       if (detailButton) {
         fireEvent.click(detailButton);
         expect(mockNavigate).toHaveBeenCalled();
@@ -100,7 +100,7 @@ describe('StatusCard', () => {
     });
 
     await waitFor(() => {
-      expect(screen.getByText('?뺣낫瑜?遺덈윭?????놁뒿?덈떎')).toBeInTheDocument();
+      expect(screen.getByText('?類ｋ궖???븍뜄???????곷뮸??덈뼄')).toBeInTheDocument();
     });
   });
 
@@ -137,7 +137,7 @@ describe('StatusCard', () => {
     });
 
     await waitFor(() => {
-      expect(screen.getByText(/?섏쓽 ??궧/)).toBeInTheDocument();
+      expect(screen.getByText(/??륁벥 ??沅?)).toBeInTheDocument();
     });
   });
 });
diff --git a/src/features/quiz/components/__tests__/TierBadge.test.tsx b/src/features/quiz/components/__tests__/TierBadge.test.tsx
index e8a3655..c1e94e1 100644
--- a/src/features/quiz/components/__tests__/TierBadge.test.tsx
+++ b/src/features/quiz/components/__tests__/TierBadge.test.tsx
@@ -1,10 +1,10 @@
-import { describe, it, expect, beforeEach, vi } from 'vitest';
+癤퓁mport { describe, it, expect, beforeEach, vi } from 'vitest';
 import { render, screen, waitFor } from '@testing-library/react';
-import { TierBadge } from '../TierBadge';
-import { calculateTier, getTierInfo } from '../../constants/tiers';
+import { TierBadge } from '@/features/quiz/components/TierBadge';
+import { calculateTier, getTierInfo } from '@/features/quiz/constants/tiers';
 
 // Mock tiers functions
-vi.mock('../../constants/tiers', () => ({
+vi.mock('@/features/quiz/constants/tiers', () => ({
   calculateTier: vi.fn(),
   getTierInfo: vi.fn(),
 }));
@@ -20,7 +20,7 @@ describe('TierBadge', () => {
 
     render(<TierBadge totalScore={1000} />);
 
-    expect(screen.getByText('??)).toBeInTheDocument();
+    expect(screen.getByText('??)).toBeInTheDocument();
   });
 
   it('should render tier badge with tier information', async () => {
@@ -32,8 +32,8 @@ describe('TierBadge', () => {
     });
     vi.mocked(getTierInfo).mockResolvedValue({
       level: 1,
-      name: '?깆궛濡?,
-      icon: '??',
+      name: '?源녾텦嚥?,
+      icon: '??',
       minScore: 1000,
       colorVar: '--color-tier-trail',
     });
@@ -41,10 +41,10 @@ describe('TierBadge', () => {
     render(<TierBadge totalScore={1000} />);
 
     await waitFor(() => {
-      expect(screen.getByText('?깆궛濡?)).toBeInTheDocument();
+      expect(screen.getByText('?源녾텦嚥?)).toBeInTheDocument();
     });
 
-    expect(screen.getByText('??')).toBeInTheDocument();
+    expect(screen.getByText('??')).toBeInTheDocument();
   });
 
   it('should display tier label when showLabel is true', async () => {
@@ -56,8 +56,8 @@ describe('TierBadge', () => {
     });
     vi.mocked(getTierInfo).mockResolvedValue({
       level: 1,
-      name: '?깆궛濡?,
-      icon: '??',
+      name: '?源녾텦嚥?,
+      icon: '??',
       minScore: 1000,
       colorVar: '--color-tier-trail',
     });
@@ -65,7 +65,7 @@ describe('TierBadge', () => {
     render(<TierBadge totalScore={1000} showLabel={true} />);
 
     await waitFor(() => {
-      expect(screen.getByText('?깆궛濡?)).toBeInTheDocument();
+      expect(screen.getByText('?源녾텦嚥?)).toBeInTheDocument();
     });
   });
 
@@ -78,8 +78,8 @@ describe('TierBadge', () => {
     });
     vi.mocked(getTierInfo).mockResolvedValue({
       level: 1,
-      name: '?깆궛濡?,
-      icon: '??',
+      name: '?源녾텦嚥?,
+      icon: '??',
       minScore: 1000,
       colorVar: '--color-tier-trail',
     });
@@ -87,10 +87,10 @@ describe('TierBadge', () => {
     render(<TierBadge totalScore={1000} showLabel={false} />);
 
     await waitFor(() => {
-      expect(screen.getByText('??')).toBeInTheDocument();
+      expect(screen.getByText('??')).toBeInTheDocument();
     });
 
-    expect(screen.queryByText('?깆궛濡?)).not.toBeInTheDocument();
+    expect(screen.queryByText('?源녾텦嚥?)).not.toBeInTheDocument();
   });
 
   it('should display stars when showStars is true and stars > 0', async () => {
@@ -102,8 +102,8 @@ describe('TierBadge', () => {
     });
     vi.mocked(getTierInfo).mockResolvedValue({
       level: 1,
-      name: '?깆궛濡?,
-      icon: '??',
+      name: '?源녾텦嚥?,
+      icon: '??',
       minScore: 1000,
       colorVar: '--color-tier-trail',
     });
@@ -111,11 +111,11 @@ describe('TierBadge', () => {
     render(<TierBadge totalScore={1000} showStars={true} />);
 
     await waitFor(() => {
-      expect(screen.getByText('?깆궛濡?)).toBeInTheDocument();
+      expect(screen.getByText('?源녾텦嚥?)).toBeInTheDocument();
     });
 
     // Stars should be displayed (3 stars)
-    const starsElement = screen.getByText(/??);
+    const starsElement = screen.getByText(/??);
     expect(starsElement).toBeInTheDocument();
   });
 
@@ -128,8 +128,8 @@ describe('TierBadge', () => {
     });
     vi.mocked(getTierInfo).mockResolvedValue({
       level: 1,
-      name: '?깆궛濡?,
-      icon: '??',
+      name: '?源녾텦嚥?,
+      icon: '??',
       minScore: 1000,
       colorVar: '--color-tier-trail',
     });
@@ -137,7 +137,7 @@ describe('TierBadge', () => {
     render(<TierBadge totalScore={1000} showStars={true} />);
 
     await waitFor(() => {
-      expect(screen.getByText(/?끖?/)).toBeInTheDocument();
+      expect(screen.getByText(/???/)).toBeInTheDocument();
     });
   });
 
@@ -150,8 +150,8 @@ describe('TierBadge', () => {
     });
     vi.mocked(getTierInfo).mockResolvedValue({
       level: 1,
-      name: '?깆궛濡?,
-      icon: '??',
+      name: '?源녾텦嚥?,
+      icon: '??',
       minScore: 1000,
       colorVar: '--color-tier-trail',
     });
@@ -159,11 +159,11 @@ describe('TierBadge', () => {
     render(<TierBadge totalScore={1000} showStars={false} />);
 
     await waitFor(() => {
-      expect(screen.getByText('?깆궛濡?)).toBeInTheDocument();
+      expect(screen.getByText('?源녾텦嚥?)).toBeInTheDocument();
     });
 
     // Stars should not be displayed
-    expect(screen.queryByText(/??)).not.toBeInTheDocument();
+    expect(screen.queryByText(/??)).not.toBeInTheDocument();
   });
 
   it('should use currentTierLevel when provided', async () => {
@@ -175,8 +175,8 @@ describe('TierBadge', () => {
     });
     vi.mocked(getTierInfo).mockResolvedValue({
       level: 1,
-      name: '?깆궛濡?,
-      icon: '??',
+      name: '?源녾텦嚥?,
+      icon: '??',
       minScore: 1000,
       colorVar: '--color-tier-trail',
     });
@@ -184,7 +184,7 @@ describe('TierBadge', () => {
     render(<TierBadge totalScore={5000} currentTierLevel={1} />);
 
     await waitFor(() => {
-      expect(screen.getByText('?깆궛濡?)).toBeInTheDocument();
+      expect(screen.getByText('?源녾텦嚥?)).toBeInTheDocument();
     });
 
     // Should use currentTierLevel (1) instead of calculated level (2)
@@ -200,8 +200,8 @@ describe('TierBadge', () => {
     });
     vi.mocked(getTierInfo).mockResolvedValue({
       level: 1,
-      name: '?깆궛濡?,
-      icon: '??',
+      name: '?源녾텦嚥?,
+      icon: '??',
       minScore: 1000,
       colorVar: '--color-tier-trail',
     });
@@ -209,17 +209,17 @@ describe('TierBadge', () => {
     const { rerender } = render(<TierBadge totalScore={1000} size="small" />);
 
     await waitFor(() => {
-      expect(screen.getByText('?깆궛濡?)).toBeInTheDocument();
+      expect(screen.getByText('?源녾텦嚥?)).toBeInTheDocument();
     });
 
     rerender(<TierBadge totalScore={1000} size="medium" />);
     rerender(<TierBadge totalScore={1000} size="large" />);
 
     // Component should render with different sizes
-    expect(screen.getByText('?깆궛濡?)).toBeInTheDocument();
+    expect(screen.getByText('?源녾텦嚥?)).toBeInTheDocument();
   });
 
-  it('should handle tier level 0 (踰좎씠?ㅼ틺??', async () => {
+  it('should handle tier level 0 (甕곗쥙???쇳떧??', async () => {
     vi.mocked(calculateTier).mockResolvedValue({
       level: 0,
       stars: 0,
@@ -228,8 +228,8 @@ describe('TierBadge', () => {
     });
     vi.mocked(getTierInfo).mockResolvedValue({
       level: 0,
-      name: '踰좎씠?ㅼ틺??,
-      icon: '??,
+      name: '甕곗쥙???쇳떧??,
+      icon: '??,
       minScore: 0,
       colorVar: '--color-tier-base',
     });
@@ -237,7 +237,7 @@ describe('TierBadge', () => {
     render(<TierBadge totalScore={0} />);
 
     await waitFor(() => {
-      expect(screen.getByText('踰좎씠?ㅼ틺??)).toBeInTheDocument();
+      expect(screen.getByText('甕곗쥙???쇳떧??)).toBeInTheDocument();
     });
   });
 
@@ -254,8 +254,8 @@ describe('TierBadge', () => {
       { timeout: 3000 }
     );
 
-    // Should still show loading state (??
-    expect(screen.getByText('??)).toBeInTheDocument();
+    // Should still show loading state (??
+    expect(screen.getByText('??)).toBeInTheDocument();
 
     consoleErrorSpy.mockRestore();
   });
@@ -279,8 +279,8 @@ describe('TierBadge', () => {
       { timeout: 3000 }
     );
 
-    // Should still show loading state (??
-    expect(screen.getByText('??)).toBeInTheDocument();
+    // Should still show loading state (??
+    expect(screen.getByText('??)).toBeInTheDocument();
 
     consoleErrorSpy.mockRestore();
   });
@@ -294,8 +294,8 @@ describe('TierBadge', () => {
     });
     vi.mocked(getTierInfo).mockResolvedValue({
       level: 1,
-      name: '?깆궛濡?,
-      icon: '??',
+      name: '?源녾텦嚥?,
+      icon: '??',
       minScore: 1000,
       colorVar: '--color-tier-trail',
     });
@@ -303,11 +303,11 @@ describe('TierBadge', () => {
     render(<TierBadge totalScore={1000} showStars={true} />);
 
     await waitFor(() => {
-      expect(screen.getByText('?깆궛濡?)).toBeInTheDocument();
+      expect(screen.getByText('?源녾텦嚥?)).toBeInTheDocument();
     });
 
     // Stars should not be displayed when stars is 0
-    expect(screen.queryByText(/??)).not.toBeInTheDocument();
+    expect(screen.queryByText(/??)).not.toBeInTheDocument();
   });
 
   it('should update when totalScore changes', async () => {
@@ -319,8 +319,8 @@ describe('TierBadge', () => {
     });
     vi.mocked(getTierInfo).mockResolvedValueOnce({
       level: 1,
-      name: '?깆궛濡?,
-      icon: '??',
+      name: '?源녾텦嚥?,
+      icon: '??',
       minScore: 1000,
       colorVar: '--color-tier-trail',
     });
@@ -328,7 +328,7 @@ describe('TierBadge', () => {
     const { rerender } = render(<TierBadge totalScore={1000} />);
 
     await waitFor(() => {
-      expect(screen.getByText('?깆궛濡?)).toBeInTheDocument();
+      expect(screen.getByText('?源녾텦嚥?)).toBeInTheDocument();
     });
 
     // Update score
@@ -340,8 +340,8 @@ describe('TierBadge', () => {
     });
     vi.mocked(getTierInfo).mockResolvedValueOnce({
       level: 2,
-      name: '以묓꽦',
-      icon: '?곤툘',
+      name: '餓λ쵑苑?,
+      icon: '?怨ㅽ닔',
       minScore: 5000,
       colorVar: '--color-tier-mid',
     });
@@ -349,7 +349,7 @@ describe('TierBadge', () => {
     rerender(<TierBadge totalScore={5000} />);
 
     await waitFor(() => {
-      expect(screen.getByText('以묓꽦')).toBeInTheDocument();
+      expect(screen.getByText('餓λ쵑苑?)).toBeInTheDocument();
     });
   });
 
@@ -362,8 +362,8 @@ describe('TierBadge', () => {
     });
     vi.mocked(getTierInfo).mockResolvedValue({
       level: 1,
-      name: '?깆궛濡?,
-      icon: '??',
+      name: '?源녾텦嚥?,
+      icon: '??',
       minScore: 1000,
       colorVar: '--color-tier-trail',
     });
@@ -371,7 +371,7 @@ describe('TierBadge', () => {
     render(<TierBadge totalScore={5000} currentTierLevel={1} />);
 
     await waitFor(() => {
-      expect(screen.getByText('?깆궛濡?)).toBeInTheDocument();
+      expect(screen.getByText('?源녾텦嚥?)).toBeInTheDocument();
     });
 
     // Should use currentTierLevel (1) instead of calculated level (2)
diff --git a/src/features/quiz/components/__tests__/TierProgressBar.test.tsx b/src/features/quiz/components/__tests__/TierProgressBar.test.tsx
index 1afc552..c252c80 100644
--- a/src/features/quiz/components/__tests__/TierProgressBar.test.tsx
+++ b/src/features/quiz/components/__tests__/TierProgressBar.test.tsx
@@ -1,10 +1,10 @@
-import { describe, it, expect, vi, beforeEach } from 'vitest';
+癤퓁mport { describe, it, expect, vi, beforeEach } from 'vitest';
 import { render, screen, waitFor } from '@testing-library/react';
-import { TierProgressBar } from '../TierProgressBar';
-import { calculateTier, getNextTierInfo } from '../../constants/tiers';
+import { TierProgressBar } from '@/features/quiz/components/TierProgressBar';
+import { calculateTier, getNextTierInfo } from '@/features/quiz/constants/tiers';
 
 // Mock tier functions
-vi.mock('../../constants/tiers', () => ({
+vi.mock('@/features/quiz/constants/tiers', () => ({
   calculateTier: vi.fn(),
   getNextTierInfo: vi.fn(),
 }));
@@ -41,7 +41,7 @@ describe('TierProgressBar', () => {
 
     await waitFor(
       () => {
-        expect(screen.getByText(/?꾩옱 ?곗뼱/)).toBeInTheDocument();
+        expect(screen.getByText(/?袁⑹삺 ?怨쀫선/)).toBeInTheDocument();
       },
       { timeout: 10000 }
     );
@@ -66,11 +66,11 @@ describe('TierProgressBar', () => {
     const { rerender } = render(<TierProgressBar totalScore={1000} size="small" />);
 
     await waitFor(() => {
-      expect(screen.getByText(/?꾩옱 ?곗뼱/)).toBeInTheDocument();
+      expect(screen.getByText(/?袁⑹삺 ?怨쀫선/)).toBeInTheDocument();
     });
 
     rerender(<TierProgressBar totalScore={1000} size="large" />);
-    expect(screen.getByText(/?꾩옱 ?곗뼱/)).toBeInTheDocument();
+    expect(screen.getByText(/?袁⑹삺 ?怨쀫선/)).toBeInTheDocument();
   });
 
   it('should display stars when stars > 0', async () => {
@@ -92,11 +92,11 @@ describe('TierProgressBar', () => {
     render(<TierProgressBar totalScore={1000} />);
 
     await waitFor(() => {
-      expect(screen.getByText(/?꾩옱 ?곗뼱/)).toBeInTheDocument();
+      expect(screen.getByText(/?袁⑹삺 ?怨쀫선/)).toBeInTheDocument();
     });
   });
 
-  it('should display stars in 횞N format when stars >= 5', async () => {
+  it('should display stars in ?얧 format when stars >= 5', async () => {
     const mockTier = {
       tier: 'Bronze',
       stars: 5,
@@ -115,7 +115,7 @@ describe('TierProgressBar', () => {
     render(<TierProgressBar totalScore={1000} />);
 
     await waitFor(() => {
-      expect(screen.getByText(/?끖?/)).toBeInTheDocument();
+      expect(screen.getByText(/???/)).toBeInTheDocument();
     });
   });
 
@@ -138,7 +138,7 @@ describe('TierProgressBar', () => {
     render(<TierProgressBar totalScore={1000} />);
 
     await waitFor(() => {
-      expect(screen.getByText(/500???⑥쓬/)).toBeInTheDocument();
+      expect(screen.getByText(/500????μ벉/)).toBeInTheDocument();
     });
   });
 
@@ -161,7 +161,7 @@ describe('TierProgressBar', () => {
     render(<TierProgressBar totalScore={1000} />);
 
     await waitFor(() => {
-      expect(screen.getByText(/??Silver/)).toBeInTheDocument();
+      expect(screen.getByText(/??Silver/)).toBeInTheDocument();
     });
   });
 
diff --git a/src/features/quiz/components/__tests__/TierUpgradeModal.test.tsx b/src/features/quiz/components/__tests__/TierUpgradeModal.test.tsx
index b9fe0b8..a746b1b 100644
--- a/src/features/quiz/components/__tests__/TierUpgradeModal.test.tsx
+++ b/src/features/quiz/components/__tests__/TierUpgradeModal.test.tsx
@@ -1,10 +1,10 @@
-import { describe, it, expect, vi, beforeEach } from 'vitest';
+癤퓁mport { describe, it, expect, vi, beforeEach } from 'vitest';
 import { render, screen, waitFor } from '@testing-library/react';
-import { TierUpgradeModal } from '../TierUpgradeModal';
-import { calculateTier, getTierInfo } from '../../constants/tiers';
+import { TierUpgradeModal } from '@/features/quiz/components/TierUpgradeModal';
+import { calculateTier, getTierInfo } from '@/features/quiz/constants/tiers';
 
 // Mock dependencies
-vi.mock('../../constants/tiers', () => ({
+vi.mock('@/features/quiz/constants/tiers', () => ({
   calculateTier: vi.fn(),
   getTierInfo: vi.fn(),
 }));
@@ -19,7 +19,7 @@ describe('TierUpgradeModal', () => {
       <TierUpgradeModal isOpen={false} previousScore={1000} currentScore={2000} onClose={vi.fn()} />
     );
 
-    expect(screen.queryByText('?럦 ?곗뼱 ?밴툒! ?럦')).not.toBeInTheDocument();
+    expect(screen.queryByText('????怨쀫선 ?諛댄닋! ???)).not.toBeInTheDocument();
   });
 
   it('should render tier upgrade when tier level increases', async () => {
@@ -35,15 +35,15 @@ describe('TierUpgradeModal', () => {
       minScore: 1000,
       maxScore: 2000,
     });
-    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?룙截?, name: 'Camp 1' });
-    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?룙截?, name: 'Camp 2' });
+    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?猷숁닼?, name: 'Camp 1' });
+    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?猷숁닼?, name: 'Camp 2' });
 
     render(
       <TierUpgradeModal isOpen={true} previousScore={1000} currentScore={2000} onClose={vi.fn()} />
     );
 
     await waitFor(() => {
-      expect(screen.getByText(/?곗뼱 ?밴툒!/)).toBeInTheDocument();
+      expect(screen.getByText(/?怨쀫선 ?諛댄닋!/)).toBeInTheDocument();
     });
   });
 
@@ -60,15 +60,15 @@ describe('TierUpgradeModal', () => {
       minScore: 0,
       maxScore: 1000,
     });
-    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?룙截?, name: 'Camp 1' });
-    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?룙截?, name: 'Camp 1' });
+    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?猷숁닼?, name: 'Camp 1' });
+    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?猷숁닼?, name: 'Camp 1' });
 
     render(
       <TierUpgradeModal isOpen={true} previousScore={1000} currentScore={1500} onClose={vi.fn()} />
     );
 
     await waitFor(() => {
-      expect(screen.getByText(/?덈줈???ъ씠?댁씠 ?쒖옉?⑸땲??)).toBeInTheDocument();
+      expect(screen.getByText(/??덉쨮???????곸뵠 ??뽰삂??몃빍??)).toBeInTheDocument();
     });
   });
 
@@ -85,15 +85,15 @@ describe('TierUpgradeModal', () => {
       minScore: 0,
       maxScore: 1000,
     });
-    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?룙截?, name: 'Camp 1' });
-    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?룙截?, name: 'Camp 1' });
+    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?猷숁닼?, name: 'Camp 1' });
+    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?猷숁닼?, name: 'Camp 1' });
 
     render(
       <TierUpgradeModal isOpen={true} previousScore={1000} currentScore={1100} onClose={vi.fn()} />
     );
 
     await waitFor(() => {
-      expect(screen.queryByText(/?곗뼱 ?밴툒!/)).not.toBeInTheDocument();
+      expect(screen.queryByText(/?怨쀫선 ?諛댄닋!/)).not.toBeInTheDocument();
     });
   });
 
@@ -111,15 +111,15 @@ describe('TierUpgradeModal', () => {
       minScore: 1000,
       maxScore: 2000,
     });
-    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?룙截?, name: 'Camp 1' });
-    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?룙截?, name: 'Camp 2' });
+    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?猷숁닼?, name: 'Camp 1' });
+    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?猷숁닼?, name: 'Camp 2' });
 
     render(
       <TierUpgradeModal isOpen={true} previousScore={1000} currentScore={2000} onClose={onClose} />
     );
 
     await waitFor(() => {
-      const closeButton = screen.getByText('怨꾩냽?섍린');
+      const closeButton = screen.getByText('?④쑴???띾┛');
       closeButton.click();
       expect(onClose).toHaveBeenCalled();
     });
@@ -139,8 +139,8 @@ describe('TierUpgradeModal', () => {
       minScore: 1000,
       maxScore: 2000,
     });
-    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?룙截?, name: 'Camp 1' });
-    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?룙截?, name: 'Camp 2' });
+    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?猷숁닼?, name: 'Camp 1' });
+    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?猷숁닼?, name: 'Camp 2' });
 
     const { container } = render(
       <TierUpgradeModal isOpen={true} previousScore={1000} currentScore={2000} onClose={onClose} />
@@ -169,8 +169,8 @@ describe('TierUpgradeModal', () => {
       minScore: 1000,
       maxScore: 2000,
     });
-    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?룙截?, name: 'Camp 1' });
-    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?룙截?, name: 'Camp 2' });
+    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?猷숁닼?, name: 'Camp 1' });
+    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?猷숁닼?, name: 'Camp 2' });
 
     const { container } = render(
       <TierUpgradeModal isOpen={true} previousScore={1000} currentScore={2000} onClose={onClose} />
@@ -198,8 +198,8 @@ describe('TierUpgradeModal', () => {
       minScore: 1000,
       maxScore: 2000,
     });
-    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?룙截?, name: 'Camp 1' });
-    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?룙截?, name: 'Camp 2' });
+    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?猷숁닼?, name: 'Camp 1' });
+    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?猷숁닼?, name: 'Camp 2' });
 
     render(
       <TierUpgradeModal isOpen={true} previousScore={1000} currentScore={2000} onClose={vi.fn()} />
@@ -211,7 +211,7 @@ describe('TierUpgradeModal', () => {
     });
   });
 
-  it('should display stars in 횞N format when stars >= 5', async () => {
+  it('should display stars in ?얧 format when stars >= 5', async () => {
     vi.mocked(calculateTier).mockResolvedValueOnce({
       level: 1,
       stars: 5,
@@ -224,15 +224,15 @@ describe('TierUpgradeModal', () => {
       minScore: 0,
       maxScore: 1000,
     });
-    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?룙截?, name: 'Camp 1' });
-    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?룙截?, name: 'Camp 1' });
+    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?猷숁닼?, name: 'Camp 1' });
+    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?猷숁닼?, name: 'Camp 1' });
 
     render(
       <TierUpgradeModal isOpen={true} previousScore={1000} currentScore={1500} onClose={vi.fn()} />
     );
 
     await waitFor(() => {
-      expect(screen.getByText(/?끖?/)).toBeInTheDocument();
+      expect(screen.getByText(/???/)).toBeInTheDocument();
     });
   });
 
@@ -249,15 +249,15 @@ describe('TierUpgradeModal', () => {
       minScore: 0,
       maxScore: 1000,
     });
-    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?룙截?, name: 'Camp 1' });
-    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?룙截?, name: 'Camp 1' });
+    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?猷숁닼?, name: 'Camp 1' });
+    vi.mocked(getTierInfo).mockResolvedValueOnce({ icon: '?猷숁닼?, name: 'Camp 1' });
 
     render(
       <TierUpgradeModal isOpen={true} previousScore={1000} currentScore={1500} onClose={vi.fn()} />
     );
 
     await waitFor(() => {
-      expect(screen.getByText(/?덈줈???ъ씠?댁씠 ?쒖옉?⑸땲??)).toBeInTheDocument();
+      expect(screen.getByText(/??덉쨮???????곸뵠 ??뽰삂??몃빍??)).toBeInTheDocument();
     });
   });
 
diff --git a/src/features/quiz/components/__tests__/TimerCircle.test.tsx b/src/features/quiz/components/__tests__/TimerCircle.test.tsx
index ae91085..5c70936 100644
--- a/src/features/quiz/components/__tests__/TimerCircle.test.tsx
+++ b/src/features/quiz/components/__tests__/TimerCircle.test.tsx
@@ -1,6 +1,6 @@
-import { describe, it, expect, beforeEach, vi, afterEach } from 'vitest';
+癤퓁mport { describe, it, expect, beforeEach, vi, afterEach } from 'vitest';
 import { render, screen } from '@testing-library/react';
-import { TimerCircle } from '../TimerCircle';
+import { TimerCircle } from '@/features/quiz/components/TimerCircle';
 
 describe('TimerCircle', () => {
   beforeEach(() => {
diff --git a/src/components/__tests__/TopicHeader.test.tsx b/src/features/quiz/components/__tests__/TopicHeader.test.tsx
similarity index 71%
rename from src/components/__tests__/TopicHeader.test.tsx
rename to src/features/quiz/components/__tests__/TopicHeader.test.tsx
index 74befa9..5911656 100644
--- a/src/components/__tests__/TopicHeader.test.tsx
+++ b/src/features/quiz/components/__tests__/TopicHeader.test.tsx
@@ -1,7 +1,7 @@
-import { describe, it, expect, vi, beforeEach } from 'vitest';
+癤퓁mport { describe, it, expect, vi, beforeEach } from 'vitest';
 import { render, screen } from '@testing-library/react';
 import { BrowserRouter } from 'react-router-dom';
-import { TopicHeader } from '../TopicHeader';
+import { TopicHeader } from '@/features/quiz/components/TopicHeader';
 
 // Mock useNavigate
 const mockNavigate = vi.fn();
@@ -27,13 +27,13 @@ describe('TopicHeader', () => {
   };
 
   it('should render title correctly', () => {
-    renderTopicHeader('?뚯뒪???쒕ぉ');
-    expect(screen.getByText('?뚯뒪???쒕ぉ')).toBeInTheDocument();
+    renderTopicHeader('???뮞????뺛걠');
+    expect(screen.getByText('???뮞????뺛걠')).toBeInTheDocument();
   });
 
   it('should navigate back when back button is clicked and no onBack provided', () => {
-    renderTopicHeader('?뚯뒪???쒕ぉ');
-    const backButton = screen.getByLabelText('?ㅻ줈 媛湲?);
+    renderTopicHeader('???뮞????뺛걠');
+    const backButton = screen.getByLabelText('??살쨮 揶쎛疫?);
     backButton.click();
 
     expect(mockNavigate).toHaveBeenCalledTimes(1);
@@ -42,9 +42,9 @@ describe('TopicHeader', () => {
 
   it('should call onBack when back button is clicked and onBack is provided', () => {
     const mockOnBack = vi.fn();
-    renderTopicHeader('?뚯뒪???쒕ぉ', mockOnBack);
+    renderTopicHeader('???뮞????뺛걠', mockOnBack);
 
-    const backButton = screen.getByLabelText('?ㅻ줈 媛湲?);
+    const backButton = screen.getByLabelText('??살쨮 揶쎛疫?);
     backButton.click();
 
     expect(mockOnBack).toHaveBeenCalledTimes(1);
@@ -52,7 +52,7 @@ describe('TopicHeader', () => {
   });
 
   it('should render header with correct structure', () => {
-    const { container } = renderTopicHeader('?뚯뒪???쒕ぉ');
+    const { container } = renderTopicHeader('???뮞????뺛걠');
     const header = container.querySelector('.topic-header');
     expect(header).toBeInTheDocument();
   });
diff --git a/src/components/__tests__/UnknownMountainCard.test.tsx b/src/features/quiz/components/__tests__/UnknownMountainCard.test.tsx
similarity index 73%
rename from src/components/__tests__/UnknownMountainCard.test.tsx
rename to src/features/quiz/components/__tests__/UnknownMountainCard.test.tsx
index fc9ad40..8ba38b7 100644
--- a/src/components/__tests__/UnknownMountainCard.test.tsx
+++ b/src/features/quiz/components/__tests__/UnknownMountainCard.test.tsx
@@ -1,6 +1,6 @@
-import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
+癤퓁mport { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
 import { render, screen, fireEvent } from '@testing-library/react';
-import { UnknownMountainCard } from '../UnknownMountainCard';
+import { UnknownMountainCard } from '@/features/quiz/components/UnknownMountainCard';
 
 describe('UnknownMountainCard', () => {
   beforeEach(() => {
@@ -15,15 +15,15 @@ describe('UnknownMountainCard', () => {
   it('should render card content', () => {
     render(<UnknownMountainCard />);
 
-    expect(screen.getByText('誘몄?????)).toBeInTheDocument();
-    expect(screen.getByText('踰좎씠?ㅼ틺??援ъ텞 以?)).toBeInTheDocument();
+    expect(screen.getByText('沃섎챷?????)).toBeInTheDocument();
+    expect(screen.getByText('甕곗쥙???쇳떧???닌딇뀧 餓?)).toBeInTheDocument();
   });
 
   it('should call onToast when clicked', () => {
     const onToast = vi.fn();
     render(<UnknownMountainCard onToast={onToast} />);
 
-    const card = screen.getByText('誘몄?????).closest('.unknown-mountain-card');
+    const card = screen.getByText('沃섎챷?????).closest('.unknown-mountain-card');
     if (card) fireEvent.click(card);
 
     expect(onToast).toHaveBeenCalled();
@@ -33,7 +33,7 @@ describe('UnknownMountainCard', () => {
     const onToast = vi.fn();
     render(<UnknownMountainCard onToast={onToast} />);
 
-    const card = screen.getByText('誘몄?????).closest('.unknown-mountain-card');
+    const card = screen.getByText('沃섎챷?????).closest('.unknown-mountain-card');
 
     // First click
     if (card) fireEvent.click(card);
@@ -45,7 +45,7 @@ describe('UnknownMountainCard', () => {
     const onToast = vi.fn();
     render(<UnknownMountainCard onToast={onToast} />);
 
-    const card = screen.getByText('誘몄?????).closest('.unknown-mountain-card');
+    const card = screen.getByText('沃섎챷?????).closest('.unknown-mountain-card');
 
     // First click
     if (card) fireEvent.click(card);
@@ -62,7 +62,7 @@ describe('UnknownMountainCard', () => {
   it('should not call onToast when onToast is not provided', () => {
     render(<UnknownMountainCard />);
 
-    const card = screen.getByText('誘몄?????).closest('.unknown-mountain-card');
+    const card = screen.getByText('沃섎챷?????).closest('.unknown-mountain-card');
 
     // Should not throw error when clicking without onToast
     expect(() => {
@@ -73,7 +73,7 @@ describe('UnknownMountainCard', () => {
   it('should set pressed state on mouse down', () => {
     render(<UnknownMountainCard />);
 
-    const card = screen.getByText('誘몄?????).closest('.unknown-mountain-card');
+    const card = screen.getByText('沃섎챷?????).closest('.unknown-mountain-card');
 
     if (card) fireEvent.mouseDown(card);
     expect(card).toHaveClass('pressed');
@@ -82,7 +82,7 @@ describe('UnknownMountainCard', () => {
   it('should clear pressed state on mouse up', () => {
     render(<UnknownMountainCard />);
 
-    const card = screen.getByText('誘몄?????).closest('.unknown-mountain-card');
+    const card = screen.getByText('沃섎챷?????).closest('.unknown-mountain-card');
 
     if (card) fireEvent.mouseDown(card);
     expect(card).toHaveClass('pressed');
@@ -94,7 +94,7 @@ describe('UnknownMountainCard', () => {
   it('should clear pressed state on mouse leave', () => {
     render(<UnknownMountainCard />);
 
-    const card = screen.getByText('誘몄?????).closest('.unknown-mountain-card');
+    const card = screen.getByText('沃섎챷?????).closest('.unknown-mountain-card');
 
     if (card) fireEvent.mouseDown(card);
     expect(card).toHaveClass('pressed');
@@ -106,7 +106,7 @@ describe('UnknownMountainCard', () => {
   it('should set pressed state on touch start', () => {
     render(<UnknownMountainCard />);
 
-    const card = screen.getByText('誘몄?????).closest('.unknown-mountain-card');
+    const card = screen.getByText('沃섎챷?????).closest('.unknown-mountain-card');
 
     if (card) fireEvent.touchStart(card);
     expect(card).toHaveClass('pressed');
@@ -115,7 +115,7 @@ describe('UnknownMountainCard', () => {
   it('should clear pressed state on touch end', () => {
     render(<UnknownMountainCard />);
 
-    const card = screen.getByText('誘몄?????).closest('.unknown-mountain-card');
+    const card = screen.getByText('沃섎챷?????).closest('.unknown-mountain-card');
 
     if (card) fireEvent.touchStart(card);
     expect(card).toHaveClass('pressed');
@@ -128,7 +128,7 @@ describe('UnknownMountainCard', () => {
     const onToast = vi.fn();
     render(<UnknownMountainCard onToast={onToast} />);
 
-    const card = screen.getByText('誘몄?????).closest('.unknown-mountain-card');
+    const card = screen.getByText('沃섎챷?????).closest('.unknown-mountain-card');
     if (card) fireEvent.click(card);
 
     // Should call onToast with one of the explorer messages
diff --git a/src/features/quiz/components/game/__tests__/GameTipModal.test.tsx b/src/features/quiz/components/game/__tests__/GameTipModal.test.tsx
index 9b0848e..a6716c0 100644
--- a/src/features/quiz/components/game/__tests__/GameTipModal.test.tsx
+++ b/src/features/quiz/components/game/__tests__/GameTipModal.test.tsx
@@ -1,19 +1,19 @@
-import { describe, it, expect, vi, beforeEach } from 'vitest';
+癤퓁mport { describe, it, expect, vi, beforeEach } from 'vitest';
 import { render, screen, fireEvent } from '@testing-library/react';
-import { GameTipModal } from '../GameTipModal';
+import { GameTipModal } from '@/features/quiz/components/game/GameTipModal';
 
 // Mock dependencies
-vi.mock('../../../../utils/storageKey', () => ({
+vi.mock('@/utils/storageKey', () => ({
   createSafeStorageKey: vi.fn((...args) => args.join('_')),
 }));
 
-vi.mock('../../../../utils/storage', () => ({
+vi.mock('@/utils/storage', () => ({
   storage: {
     setString: vi.fn(),
   },
 }));
 
-vi.mock('../BackpackBottomSheet', () => ({
+vi.mock('@/features/quiz/components/game/BackpackBottomSheet', () => ({
   BackpackBottomSheet: ({ isOpen, onClose }: { isOpen: boolean; onClose: () => void }) =>
     isOpen ? (
       <div data-testid="backpack-bottom-sheet">
@@ -102,7 +102,7 @@ describe('GameTipModal', () => {
       />
     );
 
-    const backpackButton = screen.getByText(/?럲/);
+    const backpackButton = screen.getByText(/???);
     fireEvent.click(backpackButton);
 
     expect(screen.getByTestId('backpack-bottom-sheet')).toBeInTheDocument();
@@ -119,7 +119,7 @@ describe('GameTipModal', () => {
       />
     );
 
-    const closeButton = screen.queryByText(/?リ린/) || screen.queryByText(/횞/);
+    const closeButton = screen.queryByText(/??る┛/) || screen.queryByText(/??);
     if (closeButton) {
       fireEvent.click(closeButton);
       expect(mockOnClose).toHaveBeenCalled();
diff --git a/src/features/quiz/components/game/__tests__/LastChanceModal.test.tsx b/src/features/quiz/components/game/__tests__/LastChanceModal.test.tsx
index 8b8c176..f62cafc 100644
--- a/src/features/quiz/components/game/__tests__/LastChanceModal.test.tsx
+++ b/src/features/quiz/components/game/__tests__/LastChanceModal.test.tsx
@@ -1,6 +1,6 @@
-import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
+癤퓁mport { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
 import { render, screen, fireEvent } from '@testing-library/react';
-import { LastChanceModal } from '../LastChanceModal';
+import { LastChanceModal } from '@/features/quiz/components/game/LastChanceModal';
 
 describe('LastChanceModal', () => {
   beforeEach(() => {
@@ -45,7 +45,7 @@ describe('LastChanceModal', () => {
 
     expect(screen.getByText('LAST CHANCE!')).toBeInTheDocument();
     // Check for time-attack specific content
-    const content = screen.getByText(/?쇱뒪???ㅽ띁?몃줈 15珥????????덉뒿?덈떎/);
+    const content = screen.getByText(/??깅뮞????쎈쓠?紐껋쨮 15??????????됰뮸??덈뼄/);
     expect(content).toBeInTheDocument();
   });
 
@@ -65,7 +65,7 @@ describe('LastChanceModal', () => {
     );
 
     // Check for survival specific content
-    const content = screen.getByText(/援ъ“ ?좏샇?꾩쓣 ?ъ슜?섏뿬 遺?쒗븯?쒓쿋?듬땲源?);
+    const content = screen.getByText(/?닌듼??醫륁깈?袁⑹뱽 ?????뤿연 ?봔??쀫릭??볦퓢??щ빍繹?);
     expect(content).toBeInTheDocument();
   });
 
@@ -84,7 +84,7 @@ describe('LastChanceModal', () => {
       />
     );
 
-    expect(screen.getByText(/?꾩씠???ъ슜/)).toBeInTheDocument();
+    expect(screen.getByText(/?袁⑹뵠??????)).toBeInTheDocument();
   });
 
   it('should call onUseItem when use button is clicked', () => {
@@ -103,7 +103,7 @@ describe('LastChanceModal', () => {
       />
     );
 
-    const useButton = screen.getByText(/?꾩씠???ъ슜/);
+    const useButton = screen.getByText(/?袁⑹뵠??????);
     fireEvent.click(useButton);
 
     expect(onUseItem).toHaveBeenCalled();
@@ -125,7 +125,7 @@ describe('LastChanceModal', () => {
       />
     );
 
-    const buyButton = screen.getByText(/利됱떆 援щℓ & ?ъ슜/);
+    const buyButton = screen.getByText(/筌앸맩???닌됤꼻 & ????);
     fireEvent.click(buyButton);
 
     expect(onPurchaseAndUse).toHaveBeenCalled();
@@ -147,7 +147,7 @@ describe('LastChanceModal', () => {
       />
     );
 
-    const giveUpButton = screen.getByText(/洹몃깷 湲곕줉 ?④린湲?);
+    const giveUpButton = screen.getByText(/域밸챶源?疫꿸퀡以???ｋ┛疫?);
     fireEvent.click(giveUpButton);
 
     expect(onGiveUp).toHaveBeenCalled();
@@ -168,9 +168,9 @@ describe('LastChanceModal', () => {
       />
     );
 
-    const buyButton = screen.getByText(/利됱떆 援щℓ & ?ъ슜/);
+    const buyButton = screen.getByText(/筌앸맩???닌됤꼻 & ????);
     expect(buyButton).toBeDisabled();
-    expect(screen.getByText(/誘몃꽕??遺議?)).toBeInTheDocument();
+    expect(screen.getByText(/沃섎챶苑???봔鈺?)).toBeInTheDocument();
   });
 
   it('should show countdown timer', () => {
@@ -227,7 +227,7 @@ describe('LastChanceModal', () => {
       />
     );
 
-    const adButton = screen.getByText(/愿묎퀬 蹂닿퀬 臾대즺 遺??);
+    const adButton = screen.getByText(/?용쵌??癰귣떯???얜?利??봔??);
     fireEvent.click(adButton);
 
     expect(onWatchAd).toHaveBeenCalled();
diff --git a/src/constants/__tests__/tiers.test.ts b/src/features/quiz/constants/__tests__/tiers.test.ts
similarity index 84%
rename from src/constants/__tests__/tiers.test.ts
rename to src/features/quiz/constants/__tests__/tiers.test.ts
index 18d89c0..31af162 100644
--- a/src/constants/__tests__/tiers.test.ts
+++ b/src/features/quiz/constants/__tests__/tiers.test.ts
@@ -1,17 +1,16 @@
-import { describe, it, expect, beforeEach, vi } from 'vitest';
+癤퓁mport { describe, it, expect, beforeEach, vi } from 'vitest';
 import {
   loadTierDefinitions,
   loadCycleCap,
   calculateTier,
-  calculateTierSync,
   getNextTierInfo,
   getTierInfo,
   type TierInfo,
-} from '../tiers';
-import { supabase } from '../../utils/supabaseClient';
+} from '@/features/quiz/constants/tiers';
+import { supabase } from '@/utils/supabaseClient';
 
 // Mock supabase
-vi.mock('../../utils/supabaseClient', () => ({
+vi.mock('@/utils/supabaseClient', () => ({
   supabase: {
     from: vi.fn(),
   },
@@ -47,8 +46,8 @@ describe('tiers', () => {
 
     it('should return database tier definitions when available', async () => {
       const mockTierData = [
-        { level: 0, name: '踰좎씠?ㅼ틺??, icon: '??, min_score: 0, color_var: '--color-tier-base' },
-        { level: 1, name: '?깆궛濡?, icon: '??', min_score: 1000, color_var: '--color-tier-trail' },
+        { level: 0, name: '甕곗쥙???쇳떧??, icon: '??, min_score: 0, color_var: '--color-tier-base' },
+        { level: 1, name: '?源녾텦嚥?, icon: '??', min_score: 1000, color_var: '--color-tier-trail' },
       ];
 
       const mockFrom = {
@@ -72,8 +71,8 @@ describe('tiers', () => {
       // When database returns data, it should use that data (but may merge with fallback)
       expect(result.length).toBeGreaterThanOrEqual(2);
       // Check that database data is included
-      const hasLevel0 = result.some((t) => t.level === 0 && t.name === '踰좎씠?ㅼ틺??);
-      const hasLevel1 = result.some((t) => t.level === 1 && t.name === '?깆궛濡?);
+      const hasLevel0 = result.some((t) => t.level === 0 && t.name === '甕곗쥙???쇳떧??);
+      const hasLevel1 = result.some((t) => t.level === 1 && t.name === '?源녾텦嚥?);
       expect(hasLevel0 || hasLevel1).toBe(true);
     });
 
@@ -261,7 +260,7 @@ describe('tiers', () => {
 
     it('should use cache when valid', async () => {
       const mockTierData = [
-        { level: 0, name: '踰좎씠?ㅼ틺??, icon: '??, min_score: 0, color_var: '--color-tier-base' },
+        { level: 0, name: '甕곗쥙???쇳떧??, icon: '??, min_score: 0, color_var: '--color-tier-base' },
       ];
 
       const mockFrom = {
@@ -365,52 +364,6 @@ describe('tiers', () => {
     });
   });
 
-  describe('calculateTierSync', () => {
-    it('should calculate tier synchronously for score below cycle cap', () => {
-      const tierLevels: TierInfo[] = [
-        { level: 0, name: '踰좎씠?ㅼ틺??, icon: '??, minScore: 0, colorVar: '--color-tier-base' },
-        { level: 1, name: '?깆궛濡?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
-        { level: 2, name: '以묓꽦', icon: '?곤툘', minScore: 5000, colorVar: '--color-tier-mid' },
-      ];
-      const cycleCap = 250000;
-
-      const result = calculateTierSync(10000, tierLevels, cycleCap);
-
-      expect(result).toBeDefined();
-      expect(result.totalScore).toBe(10000);
-      expect(result.stars).toBe(0);
-      expect(result.currentCycleScore).toBe(10000);
-      expect(result.level).toBe(2);
-    });
-
-    it('should calculate tier synchronously for score above cycle cap', () => {
-      const tierLevels: TierInfo[] = [
-        { level: 0, name: '踰좎씠?ㅼ틺??, icon: '??, minScore: 0, colorVar: '--color-tier-base' },
-        { level: 1, name: '?깆궛濡?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
-      ];
-      const cycleCap = 250000;
-
-      const result = calculateTierSync(300000, tierLevels, cycleCap);
-
-      expect(result).toBeDefined();
-      expect(result.totalScore).toBe(300000);
-      expect(result.stars).toBe(1);
-      expect(result.currentCycleScore).toBe(50000);
-    });
-
-    it('should return level 0 for very low score', () => {
-      const tierLevels: TierInfo[] = [
-        { level: 0, name: '踰좎씠?ㅼ틺??, icon: '??, minScore: 0, colorVar: '--color-tier-base' },
-        { level: 1, name: '?깆궛濡?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
-      ];
-      const cycleCap = 250000;
-
-      const result = calculateTierSync(500, tierLevels, cycleCap);
-
-      expect(result.level).toBe(0);
-    });
-  });
-
   describe('getNextTierInfo', () => {
     it('should return next tier info for score below cycle cap', async () => {
       const result = await getNextTierInfo(10000);
diff --git a/src/features/quiz/constants/tiers.ts b/src/features/quiz/constants/tiers.ts
index 4c8a810..a8638f8 100644
--- a/src/features/quiz/constants/tiers.ts
+++ b/src/features/quiz/constants/tiers.ts
@@ -1,4 +1,3 @@
-// src/constants/tiers.ts
 import { supabase } from '@/utils/supabaseClient';
 
 export type TierLevel = 0 | 1 | 2 | 3 | 4 | 5 | 6;
@@ -23,10 +22,10 @@ const FALLBACK_TIER_DEFINITIONS: TierInfo[] = [
   { level: 0, name: '踰좎씠?ㅼ틺??, icon: '??, minScore: 0, colorVar: '--color-tier-base' },
   { level: 1, name: '?깆궛濡?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
   { level: 2, name: '以묓꽦', icon: '?곤툘', minScore: 5000, colorVar: '--color-tier-mid' },
-  { level: 3, name: '怨좎??', icon: '?룘截?, minScore: 20000, colorVar: '--color-tier-high' },
-  { level: 4, name: '遊됱슦由?, icon: '?쫭', minScore: 50000, colorVar: '--color-tier-peak' },
-  { level: 5, name: '?뺤긽', icon: '?슜', minScore: 100000, colorVar: '--color-tier-summit' },
-  { level: 6, name: '?꾩꽕', icon: '?몣', minScore: 250000, colorVar: '--color-tier-legend' },
+  { level: 3, name: '怨좎궛吏?', icon: '?룘截?, minScore: 20000, colorVar: '--color-tier-high' },
+  { level: 4, name: '遊됱슦由?, icon: '?룘截?, minScore: 50000, colorVar: '--color-tier-peak' },
+  { level: 5, name: '?뺤긽', icon: '?룘截?, minScore: 100000, colorVar: '--color-tier-summit' },
+  { level: 6, name: '?꾩꽕', icon: '?룘截?, minScore: 250000, colorVar: '--color-tier-legend' },
 ];
 
 const FALLBACK_CYCLE_CAP = 250000;
@@ -126,10 +125,10 @@ function calculateLevel(score: number, tierLevels: TierInfo[]): TierLevel {
 }
 
 /**
- * 臾댄븳 ?깅컲(?쒗솚?? 怨꾩궛 濡쒖쭅
+ * 臾댄븳 ?깅컲(?쒗솚) 怨꾩궛 濡쒖쭅
  *
  * @param totalScore 珥?留덉뒪?곕━ ?먯닔
- * @returns { level: ?곗뼱 ?덈꺼, stars: 蹂?媛쒖닔, totalScore: 珥??먯닔, currentCycleScore: ?꾩옱 ?ъ씠?????먯닔 }
+ * @returns { level: ?곗뼱 ?덈꺼, stars: 蹂?媛쒖닔, totalScore: 珥??먯닔, currentCycleScore: ?꾩옱 ?ъ씠?댁쓽 ?먯닔 }
  */
 export async function calculateTier(totalScore: number): Promise<TierCalculationResult> {
   const [tierLevels, cycleCap] = await Promise.all([loadTierDefinitions(), loadCycleCap()]);
@@ -144,10 +143,10 @@ export async function calculateTier(totalScore: number): Promise<TierCalculation
     };
   }
 
-  // ?ъ씠???댄썑: ?ъ씠???섏? ?꾩옱 ?ъ씠?????먯닔 怨꾩궛
-  // 250,001?먮????ㅼ쓬 ?ъ씠???쒖옉 (踰꾪띁 ?곸슜)
+  // ?ъ씠???댄썑: ?ъ씠??諛??꾩옱 ?ъ씠?댁쓽 ?먯닔 怨꾩궛
+  // 250,001?먮????ㅼ쓬 ?ъ씠???쒖옉 (踰꾪띁 ?덉슜)
   const cycleCount = Math.floor((totalScore - 1) / cycleCap); // ?ъ씠????(蹂?媛쒖닔)
-  const currentCycleScore = ((totalScore - 1) % cycleCap) + 1; // ?꾩옱 ?ъ씠?????먯닔 (1遺???쒖옉)
+  const currentCycleScore = ((totalScore - 1) % cycleCap) + 1; // ?꾩옱 ?ъ씠?댁쓽 ?먯닔 (1遺???쒖옉)
 
   return {
     level: calculateLevel(currentCycleScore, tierLevels),
@@ -190,7 +189,7 @@ export async function getNextTierInfo(
   const nextTier = tierLevels.find((t) => t.level === tierResult.level + 1);
 
   if (nextTier) {
-    // 媛숈? ?ъ씠?????ㅼ쓬 ?덈꺼
+    // 媛숈? ?ъ씠?댁쓽 ?ㅼ쓬 ?덈꺼
     return {
       name: nextTier.name,
       minScore: nextTier.minScore,
diff --git a/src/features/quiz/hooks/bridge/__tests__/useQuizAnimations.test.ts b/src/features/quiz/hooks/bridge/__tests__/useQuizAnimations.test.ts
index ee03472..b00ac63 100644
--- a/src/features/quiz/hooks/bridge/__tests__/useQuizAnimations.test.ts
+++ b/src/features/quiz/hooks/bridge/__tests__/useQuizAnimations.test.ts
@@ -1,6 +1,6 @@
-import { describe, it, expect } from 'vitest';
+癤퓁mport { describe, it, expect } from 'vitest';
 import { renderHook, act } from '@testing-library/react';
-import { useQuizAnimations } from '../useQuizAnimations';
+import { useQuizAnimations } from '@/features/quiz/hooks/bridge/useQuizAnimations';
 
 describe('useQuizAnimations', () => {
   it('should return initial animation states', () => {
diff --git a/src/features/quiz/hooks/bridge/__tests__/useQuizInput.test.ts b/src/features/quiz/hooks/bridge/__tests__/useQuizInput.test.ts
index 290f45a..73d7174 100644
--- a/src/features/quiz/hooks/bridge/__tests__/useQuizInput.test.ts
+++ b/src/features/quiz/hooks/bridge/__tests__/useQuizInput.test.ts
@@ -1,6 +1,6 @@
-import { describe, it, expect, beforeEach, vi } from 'vitest';
+癤퓁mport { describe, it, expect, beforeEach, vi } from 'vitest';
 import { renderHook, act } from '@testing-library/react';
-import { useQuizInput } from '../useQuizInput';
+import { useQuizInput } from '@/features/quiz/hooks/bridge/useQuizInput';
 
 // Mock navigator.vibrate
 Object.defineProperty(navigator, 'vibrate', {
diff --git a/src/features/quiz/hooks/core/__tests__/useQuizGameState.test.ts b/src/features/quiz/hooks/core/__tests__/useQuizGameState.test.ts
index 2999a86..9e5a6ec 100644
--- a/src/features/quiz/hooks/core/__tests__/useQuizGameState.test.ts
+++ b/src/features/quiz/hooks/core/__tests__/useQuizGameState.test.ts
@@ -1,7 +1,7 @@
-import { describe, it, expect, beforeEach, vi } from 'vitest';
+癤퓁mport { describe, it, expect, beforeEach, vi } from 'vitest';
 import { renderHook, act } from '@testing-library/react';
-import { useQuizGameState } from '../useQuizGameState';
-import type { GameMode } from '../../types/quiz';
+import { useQuizGameState } from '@/features/quiz/hooks/core/useQuizGameState';
+import type { GameMode } from '@/features/quiz/types/quiz';
 
 describe('useQuizGameState', () => {
   const mockNavigate = vi.fn();
@@ -273,8 +273,7 @@ describe('useQuizGameState', () => {
     const queryString = navigateCall.split('?')[1];
     const params = new URLSearchParams(queryString);
 
-    // wrongAnswers媛 ?놁쑝硫?last_q, wrong_a, correct_a, wrong_qs, wrong_as, correct_as媛 ?놁뼱????-    expect(params.get('last_q')).toBeNull();
+    // wrongAnswers揶쎛 ??곸몵筌?last_q, wrong_a, correct_a, wrong_qs, wrong_as, correct_as揶쎛 ??곷선????    expect(params.get('last_q')).toBeNull();
     expect(params.get('wrong_a')).toBeNull();
     expect(params.get('correct_a')).toBeNull();
     expect(params.get('wrong_qs')).toBeNull();
@@ -327,7 +326,7 @@ describe('useQuizGameState', () => {
     const wrongAnswers = [
       { question: '5 + 3 = ?', wrongAnswer: '7', correctAnswer: '8' },
       { question: '10 - 4 = ?', wrongAnswer: '5', correctAnswer: '6' },
-      { question: '2 횞 3 = ?', wrongAnswer: '5', correctAnswer: '6' },
+      { question: '2 ??3 = ?', wrongAnswer: '5', correctAnswer: '6' },
     ];
 
     act(() => {
@@ -344,10 +343,10 @@ describe('useQuizGameState', () => {
     const queryString = navigateCall.split('?')[1];
     const params = new URLSearchParams(queryString);
 
-    expect(params.get('last_q')).toBe('2 횞 3 = ?');
+    expect(params.get('last_q')).toBe('2 ??3 = ?');
     expect(params.get('wrong_a')).toBe('5');
     expect(params.get('correct_a')).toBe('6');
-    expect(params.get('wrong_qs')).toBe('5 + 3 = ?|10 - 4 = ?|2 횞 3 = ?');
+    expect(params.get('wrong_qs')).toBe('5 + 3 = ?|10 - 4 = ?|2 ??3 = ?');
     expect(params.get('wrong_as')).toBe('7|5|5');
     expect(params.get('correct_as')).toBe('8|6|6');
   });
@@ -372,7 +371,7 @@ describe('useQuizGameState', () => {
       result.current.setTotalQuestions(10);
     });
 
-    // ?곹깭 ?낅뜲?댄듃瑜?湲곕떎由???handleGameOver ?몄텧
+    // ?怨밴묶 ??낅쑓??꾨뱜??疫꿸퀡?롧뵳???handleGameOver ?紐꾪뀱
     act(() => {
       result.current.handleGameOver();
     });
@@ -399,7 +398,7 @@ describe('useQuizGameState', () => {
       result.current.setQuestionIds([]);
     });
 
-    // ?곹깭 ?낅뜲?댄듃瑜?湲곕떎由???handleGameOver ?몄텧
+    // ?怨밴묶 ??낅쑓??꾨뱜??疫꿸퀡?롧뵳???handleGameOver ?紐꾪뀱
     act(() => {
       result.current.handleGameOver();
     });
@@ -423,7 +422,7 @@ describe('useQuizGameState', () => {
       result.current.setQuestionIds(['q1', 'q2']);
     });
 
-    // ?곹깭 ?낅뜲?댄듃瑜?湲곕떎由???handleGameOver ?몄텧
+    // ?怨밴묶 ??낅쑓??꾨뱜??疫꿸퀡?롧뵳???handleGameOver ?紐꾪뀱
     act(() => {
       result.current.handleGameOver();
     });
diff --git a/src/features/quiz/hooks/core/__tests__/useQuizRevive.test.ts b/src/features/quiz/hooks/core/__tests__/useQuizRevive.test.ts
index 73426c9..a8e0782 100644
--- a/src/features/quiz/hooks/core/__tests__/useQuizRevive.test.ts
+++ b/src/features/quiz/hooks/core/__tests__/useQuizRevive.test.ts
@@ -1,11 +1,11 @@
 import { describe, it, expect, vi, beforeEach } from 'vitest';
 import { renderHook, act } from '@testing-library/react';
-import { useQuizRevive } from '../useQuizRevive';
-import type { InventoryItem } from '../../../../../types/user';
-import { quizEventBus } from '../../../../../lib/eventBus';
+import { useQuizRevive } from '@/features/quiz/hooks/core/useQuizRevive';
+import type { InventoryItem } from '@/types/user';
+import { quizEventBus } from '@/lib/eventBus';
 
 // Mock dependencies
-vi.mock('../../../../../lib/eventBus', () => ({
+vi.mock('@/lib/eventBus', () => ({
   quizEventBus: {
     emit: vi.fn(),
     on: vi.fn(),
diff --git a/src/features/quiz/hooks/core/__tests__/useQuizSubmit.test.ts b/src/features/quiz/hooks/core/__tests__/useQuizSubmit.test.ts
index 15b3103..b280356 100644
--- a/src/features/quiz/hooks/core/__tests__/useQuizSubmit.test.ts
+++ b/src/features/quiz/hooks/core/__tests__/useQuizSubmit.test.ts
@@ -1,28 +1,28 @@
-import { describe, it, expect, beforeEach, vi, afterEach } from 'vitest';
+癤퓁mport { describe, it, expect, beforeEach, vi, afterEach } from 'vitest';
 import { renderHook, act } from '@testing-library/react';
-import { useQuizSubmit } from '../useQuizSubmit';
-import type { QuizQuestion, GameMode } from '../../types/quiz';
-import { useGameStore } from '../../../../../stores/useGameStore';
-import { CLIMB_PER_CORRECT, MAX_POSSIBLE_ANSWER } from '../../../../../constants/game';
-import { quizEventBus } from '../../../../../lib/eventBus';
+import { useQuizSubmit } from '@/features/quiz/hooks/core/useQuizSubmit';
+import type { QuizQuestion, GameMode } from '@/features/quiz/types/quiz';
+import { useGameStore } from '@/stores/useGameStore';
+import { CLIMB_PER_CORRECT, MAX_POSSIBLE_ANSWER } from '@/features/quiz/constants/game';
+import { quizEventBus } from '@/lib/eventBus';
 
 // Mock dependencies
-vi.mock('../../../../../stores/useGameStore', () => ({
+vi.mock('@/stores/useGameStore', () => ({
   useGameStore: Object.assign(vi.fn(), {
     getState: vi.fn(() => ({ feverLevel: 0 })),
   }),
 }));
 
-vi.mock('../../utils/haptic', () => ({
+vi.mock('@/features/quiz/haptic', () => ({
   vibrateMedium: vi.fn(),
   vibrateLong: vi.fn(),
 }));
 
-vi.mock('../../utils/japanese', () => ({
+vi.mock('@/features/quiz/utils/japanese', () => ({
   normalizeRomaji: vi.fn((str: string) => str.toLowerCase().trim()),
 }));
 
-vi.mock('../../../../../lib/eventBus', () => ({
+vi.mock('@/lib/eventBus', () => ({
   quizEventBus: {
     emit: vi.fn(),
     on: vi.fn(),
@@ -189,7 +189,7 @@ describe('useQuizSubmit', () => {
         categoryParam: 'language',
         subParam: 'japanese',
         currentQuestion: {
-          question: '??,
+          question: '??,
           answer: 'a',
         },
         answerInput: 'a',
@@ -450,7 +450,7 @@ describe('useQuizSubmit', () => {
         categoryParam: 'language',
         subParam: 'japanese',
         currentQuestion: {
-          question: '??,
+          question: '??,
           answer: 'a',
         },
         answerInput: 'b',
diff --git a/src/utils/__tests__/AlgebraAdvancedGenerator.test.ts b/src/features/quiz/utils/__tests__/AlgebraAdvancedGenerator.test.ts
similarity index 79%
rename from src/utils/__tests__/AlgebraAdvancedGenerator.test.ts
rename to src/features/quiz/utils/__tests__/AlgebraAdvancedGenerator.test.ts
index 2b0e565..d70f1c7 100644
--- a/src/utils/__tests__/AlgebraAdvancedGenerator.test.ts
+++ b/src/features/quiz/utils/__tests__/AlgebraAdvancedGenerator.test.ts
@@ -1,5 +1,5 @@
-import { describe, it, expect } from 'vitest';
-import { generateHardAlgebraProblem } from '../AlgebraAdvancedGenerator';
+癤퓁mport { describe, it, expect } from 'vitest';
+import { generateHardAlgebraProblem } from '@/features/quiz/utils/AlgebraAdvancedGenerator';
 
 describe('AlgebraAdvancedGenerator', () => {
   const mockRng = (values: number[]) => {
@@ -14,7 +14,7 @@ describe('AlgebraAdvancedGenerator', () => {
     const rng = mockRng([1, 2, 3]);
     const problem = generateHardAlgebraProblem(5, 'hard', rng);
 
-    expect(problem.expression).toContain('x짼');
+    expect(problem.expression).toContain('x吏?);
     expect(problem.expression).toContain('- 5x'); // a+b
     expect(problem.expression).toContain('+ 6'); // a*b
     expect(problem.answer).toBe(3); // larger of 2, 3
@@ -26,7 +26,7 @@ describe('AlgebraAdvancedGenerator', () => {
     const rng = mockRng([3, 0, 3]);
     const problem = generateHardAlgebraProblem(5, 'hard', rng);
 
-    expect(problem.expression).toBe('2甲 = 8, x = ?');
+    expect(problem.expression).toBe('2??= 8, x = ?');
     expect(problem.answer).toBe(3);
   });
 
@@ -36,15 +36,15 @@ describe('AlgebraAdvancedGenerator', () => {
     const rng = mockRng([3, 1, 2]);
     const problem = generateHardAlgebraProblem(15, 'hard', rng);
 
-    // base 3 -> subMap gives '??
-    expect(problem.expression).toBe('log??9) = ?');
+    // base 3 -> subMap gives '??
+    expect(problem.expression).toBe('log??9) = ?');
     expect(problem.answer).toBe(2);
   });
 
   it('should handle level > 20 correctly', () => {
     const rng = mockRng([2, 0, 2]);
     const problem = generateHardAlgebraProblem(25, 'hard', rng);
-    expect(problem.expression).toBe('2甲 = 4, x = ?');
+    expect(problem.expression).toBe('2??= 4, x = ?');
   });
 
   it('should use default Math.random if rng is not provided', () => {
diff --git a/src/utils/__tests__/CSProblemGenerator.test.ts b/src/features/quiz/utils/__tests__/CSProblemGenerator.test.ts
similarity index 87%
rename from src/utils/__tests__/CSProblemGenerator.test.ts
rename to src/features/quiz/utils/__tests__/CSProblemGenerator.test.ts
index 9835bcd..122a66f 100644
--- a/src/utils/__tests__/CSProblemGenerator.test.ts
+++ b/src/features/quiz/utils/__tests__/CSProblemGenerator.test.ts
@@ -1,5 +1,5 @@
-import { describe, it, expect } from 'vitest';
-import { generateCSProblem } from '../CSProblemGenerator';
+癤퓁mport { describe, it, expect } from 'vitest';
+import { generateCSProblem } from '@/features/quiz/utils/CSProblemGenerator';
 
 describe('CSProblemGenerator', () => {
   const mockRng = (intValues: number[], randomValues: number[] = []) => {
@@ -22,21 +22,21 @@ describe('CSProblemGenerator', () => {
   it('should generate Level 1 Binary to Decimal conversion', () => {
     const rng = mockRng([13]); // Binary for 13 is 1101
     const problem = generateCSProblem(1, 'easy', rng);
-    expect(problem.question).toContain('2吏꾩닔 1101');
+    expect(problem.question).toContain('2筌욊쑴??1101');
     expect(problem.answer).toBe(13);
   });
 
   it('should generate Level 2 Decimal to Binary conversion', () => {
     const rng = mockRng([10]); // Decimal 10
     const problem = generateCSProblem(2, 'easy', rng);
-    expect(problem.question).toContain('10吏꾩닔 10');
+    expect(problem.question).toContain('10筌욊쑴??10');
     expect(problem.answer).toBe('1010');
   });
 
   it('should generate Level 3 Hex to Decimal conversion', () => {
     const rng = mockRng([25]); // Hex 19
     const problem = generateCSProblem(3, 'easy', rng);
-    expect(problem.question).toContain('16吏꾩닔 19');
+    expect(problem.question).toContain('16筌욊쑴??19');
     expect(problem.answer).toBe(25);
   });
 
@@ -84,7 +84,7 @@ describe('CSProblemGenerator', () => {
     const rngMem = mockRng([1, 3]);
     const prob = generateCSProblem(10, 'easy', rngMem);
     expect(prob.answer).toBe(8);
-    expect(prob.question).toContain('8192 諛붿씠??);
+    expect(prob.question).toContain('8192 獄쏅뗄???);
   });
 
   it('should generate level > 10 advanced problems', () => {
@@ -92,6 +92,6 @@ describe('CSProblemGenerator', () => {
     const rngMem = mockRng([4, 2, 2]); // val=4, type=2, mb=2^2=4
     const prob = generateCSProblem(15, 'hard', rngMem);
     expect(prob.answer).toBe(4);
-    expect(prob.question).toContain('4096 KB??紐?MB');
+    expect(prob.question).toContain('4096 KB??筌?MB');
   });
 });
diff --git a/src/utils/__tests__/CalculusProblemGenerator.test.ts b/src/features/quiz/utils/__tests__/CalculusProblemGenerator.test.ts
similarity index 90%
rename from src/utils/__tests__/CalculusProblemGenerator.test.ts
rename to src/features/quiz/utils/__tests__/CalculusProblemGenerator.test.ts
index f2b840f..5b61c75 100644
--- a/src/utils/__tests__/CalculusProblemGenerator.test.ts
+++ b/src/features/quiz/utils/__tests__/CalculusProblemGenerator.test.ts
@@ -1,5 +1,5 @@
-import { describe, it, expect } from 'vitest';
-import { generateCalculusProblem } from '../CalculusProblemGenerator';
+癤퓁mport { describe, it, expect } from 'vitest';
+import { generateCalculusProblem } from '@/features/quiz/utils/CalculusProblemGenerator';
 
 describe('CalculusProblemGenerator', () => {
   const mockRng = (intValues: number[], randomValues: number[] = []) => {
@@ -45,13 +45,13 @@ describe('CalculusProblemGenerator', () => {
   it('should generate Level 3-5 Function problem (Type 2: square)', () => {
     const rng = mockRng([3, 2]); // x=3, type=2(square)
     const problem = generateCalculusProblem(5, 'easy', rng);
-    expect(problem.question).toContain('f(x) = x짼');
+    expect(problem.question).toContain('f(x) = x吏?);
     expect(problem.answer).toBe(9);
     expect(problem.hintData).toEqual({ type: 'square', value: 2, input: 3 });
   });
 
   it('should return static Limit problems for Level 6-8', () => {
-    expect(generateCalculusProblem(6, 'easy').answer).toBe('??);
+    expect(generateCalculusProblem(6, 'easy').answer).toBe('??);
     expect(generateCalculusProblem(7, 'easy').answer).toBe(0);
     expect(generateCalculusProblem(8, 'easy').answer).toBe(2);
   });
@@ -81,7 +81,7 @@ describe('CalculusProblemGenerator', () => {
 
     // Level 14: simple
     const rng14 = mockRng([1, 7]); // n=ignored, a=7
-    expect(generateCalculusProblem(14, 'easy', rng14).answer).toBe(7); // ??7 dx at x=1
+    expect(generateCalculusProblem(14, 'easy', rng14).answer).toBe(7); // ??7 dx at x=1
   });
 
   it('should handle Level 15 Randomization properly', () => {
@@ -91,14 +91,14 @@ describe('CalculusProblemGenerator', () => {
 
     const rngIntegration = mockRng([1, 5], [0.4]); // random <= 0.5 -> integral
     const p2 = generateCalculusProblem(15, 'easy', rngIntegration);
-    expect(p2.question).toContain('??);
+    expect(p2.question).toContain('??);
   });
 
   it('should generate Advanced problems for Level > 15', () => {
     // case 1: Definite Integral
     const rngInt = mockRng([1, 2, 3]); // randomVal=1, a=2, b=3
     const probInt = generateCalculusProblem(20, 'hard', rngInt);
-    expect(probInt.question).toContain('??0遺??3源뚯?) 4x dx');
+    expect(probInt.question).toContain('??0?봔??3繹먮슣?) 4x dx');
     expect(probInt.answer).toBe(18); // 2 * 3 * 3
 
     // case 2: Advanced Derivative (Product Rule)
@@ -109,13 +109,13 @@ describe('CalculusProblemGenerator', () => {
 
     // case 2: Advanced Derivative (Chain Rule)
     const probChain = generateCalculusProblem(20, 'hard', { ...rngProd, random: () => 0.4 });
-    expect(probChain.question).toContain('(x + 3)짼');
+    expect(probChain.question).toContain('(x + 3)吏?);
     expect(probChain.answer).toBe(8); // 2 * (1 + 3)
 
     // case 3: Factorization Limit
     const rngLimit = mockRng([3, 5]); // randomVal=3, a=5
     const probLimit = generateCalculusProblem(20, 'hard', rngLimit);
-    expect(probLimit.question).toContain('x ??5');
+    expect(probLimit.question).toContain('x ??5');
     expect(probLimit.answer).toBe(10); // 2 * 5
   });
 
diff --git a/src/utils/__tests__/EquationProblemGenerator.test.ts b/src/features/quiz/utils/__tests__/EquationProblemGenerator.test.ts
similarity index 93%
rename from src/utils/__tests__/EquationProblemGenerator.test.ts
rename to src/features/quiz/utils/__tests__/EquationProblemGenerator.test.ts
index ce83f23..f9befea 100644
--- a/src/utils/__tests__/EquationProblemGenerator.test.ts
+++ b/src/features/quiz/utils/__tests__/EquationProblemGenerator.test.ts
@@ -1,9 +1,9 @@
-import { describe, it, expect } from 'vitest';
+癤퓁mport { describe, it, expect } from 'vitest';
 import {
   generateEquation,
   EQUATION_STAGES,
   EquationProblemGenerator,
-} from '../EquationProblemGenerator';
+} from '@/features/quiz/utils/EquationProblemGenerator';
 
 describe('EquationProblemGenerator', () => {
   describe('generateEquation', () => {
@@ -11,7 +11,7 @@ describe('EquationProblemGenerator', () => {
       const problem = generateEquation(1);
       expect(problem).toHaveProperty('question');
       expect(problem).toHaveProperty('x');
-      expect(problem.question).toContain('??);
+      expect(problem.question).toContain('??);
       expect(typeof problem.x).toBe('number');
     });
 
@@ -19,7 +19,7 @@ describe('EquationProblemGenerator', () => {
       const problem = generateEquation(2);
       expect(problem).toHaveProperty('question');
       expect(problem).toHaveProperty('x');
-      expect(problem.question).toContain('??);
+      expect(problem.question).toContain('??);
     });
 
     it('should generate equation for stage 3', () => {
@@ -28,20 +28,20 @@ describe('EquationProblemGenerator', () => {
       expect(problem).toHaveProperty('x');
     });
 
-    it('should generate equation for stage 4 (??x a = b)', () => {
+    it('should generate equation for stage 4 (??x a = b)', () => {
       const problem = generateEquation(4);
       expect(problem).toHaveProperty('question');
       expect(problem).toHaveProperty('x');
-      expect(problem.question).toContain('??);
+      expect(problem.question).toContain('??);
       expect(problem.question).toContain('x');
     });
 
-    it('should generate equation for stage 5 (??/ a = b)', () => {
+    it('should generate equation for stage 5 (??/ a = b)', () => {
       const problem = generateEquation(5);
       expect(problem).toHaveProperty('question');
       expect(problem).toHaveProperty('x');
-      expect(problem.question).toContain('??);
-      expect(problem.question).toContain('첨');
+      expect(problem.question).toContain('??);
+      expect(problem.question).toContain('泥?);
     });
 
     it('should generate equation for stage 7 (coefficient multiply)', () => {
@@ -85,7 +85,7 @@ describe('EquationProblemGenerator', () => {
     it('should fallback to fill_plus format for invalid stage', () => {
       const problem = generateEquation(999, 'easy');
       expect(problem).toHaveProperty('question');
-      expect(problem.question).toContain('??);
+      expect(problem.question).toContain('??);
       expect(problem.question).toContain('+');
     });
 
@@ -128,17 +128,17 @@ describe('EquationProblemGenerator', () => {
     it('should test all format branches: fill_plus, fill_minus, fill_subtract', () => {
       // fill_plus (stage 1)
       const fillPlus = generateEquation(1);
-      expect(fillPlus.question).toContain('??);
+      expect(fillPlus.question).toContain('??);
       expect(fillPlus.question).toContain('+');
 
       // fill_minus (stage 2)
       const fillMinus = generateEquation(2);
-      expect(fillMinus.question).toContain('??);
+      expect(fillMinus.question).toContain('??);
       expect(fillMinus.question).toContain('-');
 
       // fill_subtract (stage 3)
       const fillSubtract = generateEquation(3);
-      expect(fillSubtract.question).toContain('??);
+      expect(fillSubtract.question).toContain('??);
       expect(fillSubtract.question).toContain('-');
     });
 
diff --git a/src/utils/__tests__/GeometryProblemGenerator.test.ts b/src/features/quiz/utils/__tests__/GeometryProblemGenerator.test.ts
similarity index 86%
rename from src/utils/__tests__/GeometryProblemGenerator.test.ts
rename to src/features/quiz/utils/__tests__/GeometryProblemGenerator.test.ts
index e6669b8..2f7388a 100644
--- a/src/utils/__tests__/GeometryProblemGenerator.test.ts
+++ b/src/features/quiz/utils/__tests__/GeometryProblemGenerator.test.ts
@@ -1,5 +1,5 @@
-import { describe, it, expect } from 'vitest';
-import { generateGeometryProblem } from '../GeometryProblemGenerator';
+癤퓁mport { describe, it, expect } from 'vitest';
+import { generateGeometryProblem } from '@/features/quiz/utils/GeometryProblemGenerator';
 
 describe('GeometryProblemGenerator', () => {
   const mockRng = (intValues: number[], randomValues: number[] = []) => {
@@ -20,23 +20,22 @@ describe('GeometryProblemGenerator', () => {
   };
 
   it('should generate Level 1 Basic Shapes vertices', () => {
-    const rng = mockRng([1]); // Index 1: ?ш컖??-    const problem = generateGeometryProblem(1, 'easy', rng);
-    expect(problem.question).toContain('?ш컖??);
+    const rng = mockRng([1]); // Index 1: ??而??    const problem = generateGeometryProblem(1, 'easy', rng);
+    expect(problem.question).toContain('??而??);
     expect(problem.answer).toBe(4);
   });
 
   it('should generate Level 2 Triangle Properties (Equilateral)', () => {
-    const rng = mockRng([0]); // Index 0: ?뺤궪媛곹삎
+    const rng = mockRng([0]); // Index 0: ?類ㅺ땔揶쏄낱??     const problem = generateGeometryProblem(2, 'easy', rng);
-    expect(problem.question).toContain('?뺤궪媛곹삎');
+    expect(problem.question).toContain('?類ㅺ땔揶쏄낱??);
     expect(problem.answer).toBe(60);
   });
 
   it('should generate Level 2 Triangle Properties (Right Angle)', () => {
-    const rng = mockRng([1, 40]); // Index 1: 吏곴컖?쇨컖?? a=40
+    const rng = mockRng([1, 40]); // Index 1: 筌욊낫而??⑥퍟?? a=40
     const problem = generateGeometryProblem(2, 'easy', rng);
-    expect(problem.question).toContain('吏곴컖?쇨컖??);
+    expect(problem.question).toContain('筌욊낫而??⑥퍟??);
     expect(problem.answer).toBe(50); // 90 - 40
   });
 
@@ -65,19 +64,19 @@ describe('GeometryProblemGenerator', () => {
   });
 
   it('should generate Level 7 Circle Advanced (Circumference)', () => {
-    const rng = { ...mockRng([4]), random: () => 0.6 }; // r=4, type=?섎젅
+    const rng = { ...mockRng([4]), random: () => 0.6 }; // r=4, type=??롮쟿
     const problem = generateGeometryProblem(7, 'easy', rng);
     expect(problem.answer).toBe(24); // 2 * 3 * 4
   });
 
   it('should generate Level 7 Circle Advanced (Area)', () => {
-    const rng = { ...mockRng([4]), random: () => 0.4 }; // r=4, type=?볦씠
+    const rng = { ...mockRng([4]), random: () => 0.4 }; // r=4, type=?蹂?뵠
     const problem = generateGeometryProblem(7, 'easy', rng);
     expect(problem.answer).toBe(48); // 3 * 4^2
   });
 
   it('should generate Level 8 Solid faces', () => {
-    const rng = mockRng([0]); // ?뺤쑁硫댁껜
+    const rng = mockRng([0]); // ?類ㅼ몓筌롫똻猿?     const problem = generateGeometryProblem(8, 'easy', rng);
     expect(problem.answer).toBe(6);
   });
@@ -99,7 +98,7 @@ describe('GeometryProblemGenerator', () => {
     // Level > 10, randomVal=1, type=Cylinder, r=2, h=5
     const rng = { ...mockRng([1, 2, 5]), random: () => 0.6 };
     const problem = generateGeometryProblem(15, 'hard', rng);
-    expect(problem.question).toContain('?먭린??);
+    expect(problem.question).toContain('?癒?┛??);
     expect(problem.answer).toBe(60); // 3 * 2^2 * 5
   });
 
diff --git a/src/utils/__tests__/LogicProblemGenerator.test.ts b/src/features/quiz/utils/__tests__/LogicProblemGenerator.test.ts
similarity index 81%
rename from src/utils/__tests__/LogicProblemGenerator.test.ts
rename to src/features/quiz/utils/__tests__/LogicProblemGenerator.test.ts
index afcde61..bd3b114 100644
--- a/src/utils/__tests__/LogicProblemGenerator.test.ts
+++ b/src/features/quiz/utils/__tests__/LogicProblemGenerator.test.ts
@@ -1,5 +1,5 @@
-import { describe, it, expect } from 'vitest';
-import { generateLogicProblem, generateSequenceProblem } from '../LogicProblemGenerator';
+癤퓁mport { describe, it, expect } from 'vitest';
+import { generateLogicProblem, generateSequenceProblem } from '@/features/quiz/utils/LogicProblemGenerator';
 
 describe('LogicProblemGenerator', () => {
   const mockRng = {
@@ -10,29 +10,29 @@ describe('LogicProblemGenerator', () => {
   describe('generateLogicProblem', () => {
     it('should generate Lv 1: Even/Odd problem', () => {
       const problem = generateLogicProblem(1, 'easy', mockRng);
-      expect(problem.question).toContain('??섏엯?덇퉴 吏앹닔?낅땲源?);
+      expect(problem.question).toContain('????륁뿯??뉙돱 筌욎빘???낅빍繹?);
       expect([1, 2]).toContain(problem.answer);
     });
 
     it('should generate Lv 2: Pos/Neg problem', () => {
       const problem = generateLogicProblem(2, 'easy', mockRng);
-      expect(problem.question).toContain('?묒닔?낅땲源??뚯닔?낅땲源?);
+      expect(problem.question).toContain('?臾믩땾??낅빍繹????땾??낅빍繹?);
       expect([1, 2]).toContain(problem.answer);
     });
 
     it('should generate Lv 3: Multiple problem', () => {
       const problem = generateLogicProblem(3, 'easy', mockRng);
-      expect(problem.question).toContain('諛곗닔?낅땲源?);
+      expect(problem.question).toContain('獄쏄퀣???낅빍繹?);
     });
 
     it('should generate Lv 4: Prime problem', () => {
       const problem = generateLogicProblem(4, 'easy', mockRng);
-      expect(problem.question).toContain('?뚯닔(Prime Number)?낅땲源?);
+      expect(problem.question).toContain('???땾(Prime Number)??낅빍繹?);
     });
 
     it('should generate Lv 5: Comparison Prime problem', () => {
       const problem = generateLogicProblem(5, 'easy', mockRng);
-      expect(problem.question).toContain('?뚯닔(Prime)??寃껋?');
+      expect(problem.question).toContain('???땾(Prime)??野껉퍔?');
     });
 
     it('should generate Lv 6-10: Sequence problems', () => {
@@ -45,23 +45,23 @@ describe('LogicProblemGenerator', () => {
 
     it('should generate Lv 11: Absolute value problem', () => {
       const problem = generateLogicProblem(11, 'hard', mockRng);
-      expect(problem.question).toContain('?덈뙎媛?);
+      expect(problem.question).toContain('??덈솊揶?);
       expect(problem.answer).toBeGreaterThanOrEqual(0);
     });
 
     it('should generate Lv 12: Modulo problem', () => {
       const problem = generateLogicProblem(12, 'hard', mockRng);
-      expect(problem.question).toContain('?섎㉧吏??);
+      expect(problem.question).toContain('??롢돢筌왖??);
     });
 
     it('should generate Lv 13: Factorial problem', () => {
       const problem = generateLogicProblem(13, 'hard', mockRng);
-      expect(problem.question).toContain('?⑺넗由ъ뼹');
+      expect(problem.question).toContain('??븍꽅?귐딅섰');
     });
 
     it('should generate Lv 14: Custom Operation problem', () => {
       const problem = generateLogicProblem(14, 'hard', mockRng);
-      expect(problem.question).toMatch(/[?끸뿃]/);
+      expect(problem.question).toMatch(/[??몃퓘]/);
     });
 
     it('should generate Lv 15: Random Sequence problem', () => {
diff --git a/src/utils/__tests__/MathProblemGenerator.fuzz.test.ts b/src/features/quiz/utils/__tests__/MathProblemGenerator.fuzz.test.ts
similarity index 53%
rename from src/utils/__tests__/MathProblemGenerator.fuzz.test.ts
rename to src/features/quiz/utils/__tests__/MathProblemGenerator.fuzz.test.ts
index 7d5a325..7e25371 100644
--- a/src/utils/__tests__/MathProblemGenerator.fuzz.test.ts
+++ b/src/features/quiz/utils/__tests__/MathProblemGenerator.fuzz.test.ts
@@ -1,49 +1,46 @@
-import { describe, it, expect } from 'vitest';
-import { generateProblem, STAGES } from '../MathProblemGenerator';
+癤퓁mport { describe, it, expect } from 'vitest';
+import { generateProblem, STAGES } from '@/features/quiz/utils/MathProblemGenerator';
 
 /**
  * Fuzz Test for MathProblemGenerator
  *
- * ???뚯뒪?몃뒗 紐⑤뱺 ?ㅽ뀒?댁???????섎쭔 踰덉쓽 臾댁옉??臾몄젣瑜??앹꽦?섏뿬
- * ?꾩＜ ?ш????뺣쪧濡?諛쒖깮?섎뒗 ?섑븰???ㅻ쪟(NaN, Infinity, ?쒖빟 議곌굔 ?꾨컲 ??瑜?李얠븘?낅땲??
+ * ?????뮞?紐껊뮉 筌뤴뫀諭???쎈???????????롮춸 甕곕뜆???얜똻????얜챷?ｇ몴???밴쉐??뤿연
+ * ?袁⑼폒 ??????類ｌぇ嚥?獄쏆뮇源??롫뮉 ??묐린????살첒(NaN, Infinity, ??뽯튋 鈺곌퀗援??袁⑥뺘 ????筌≪뼚釉??낅빍??
  */
 
 describe('MathProblemGenerator - Fuzz Testing', () => {
-  const ITERATIONS_PER_STAGE = 2000; // 媛뺥솕??諛섎났 ??-
+  const ITERATIONS_PER_STAGE = 2000; // 揶쏅벤???獄쏆꼶????
   /**
-   * ?꾩슜 ?섏떇 ?뚯꽌 諛?怨꾩궛湲?(寃利앹슜)
+   * ?袁⑹뒠 ??뤿뻼 ???퐣 獄??④쑴沅쎿묾?(野꺜筌앹빘??
    */
   function verifyAnswer(expr: string, expected: number | string) {
-    if (typeof expected === 'string') return; // 遺꾩닔 ?깆? ?쇰떒 ?듦낵
+    if (typeof expected === 'string') return; // ?브쑴???源? ??곕뼊 ???궢
 
-    // ??梨꾩슦湲??섏떇? 寃利??쒖쇅 (蹂듭옟??
-    if (expr.includes('??)) return;
+    // ??筌?쑴??묾???뤿뻼?? 野꺜筌???뽰뇚 (癰귣벊???
+    if (expr.includes('??)) return;
 
-    // ?섏떇??JS媛 ?댄빐?????덇쾶 蹂??-    // 1. 湲곌린??湲고샇 蹂??-    let cleanExpr = expr.replace(/횞/g, '*').replace(/첨/g, '/');
+    // ??뤿뻼??JS揶쎛 ??꾨퉸??????뉗쓺 癰궰??    // 1. 疫꿸퀗由??疫꿸퀬??癰궰??    let cleanExpr = expr.replace(/??g, '*').replace(/泥?g, '/');
 
-    // 2. ?쒓? ?쒓굅 (?섎㉧吏 湲???
-    cleanExpr = cleanExpr.replace(/???섎㉧吏/g, '');
-    if (expr.includes('???섎㉧吏')) {
+    // 2. ??? ??볤탢 (??롢돢筌왖 疫???
+    cleanExpr = cleanExpr.replace(/????롢돢筌왖/g, '');
+    if (expr.includes('????롢돢筌왖')) {
       const [a, b] = cleanExpr.split('/').map((n) => Number(n.trim()));
       expect(a % b).toBe(expected);
       return;
     }
 
-    // 3. ?쒓컙 臾몄젣 ?뱀닔 泥섎━
+    // 3. ??볦퍢 ?얜챷???諭??筌ｌ꼶??     if (expr.includes(':')) {
-      // "14:00 + 40遺? ?뺥깭??answer媛 ?대? 1440 ?뺥깭?대?濡?蹂꾨룄 寃利??꾩슂 ?놁쓬
+      // "14:00 + 40?? ?類κ묶??answer揶쎛 ??? 1440 ?類κ묶???嚥?癰귢쑬猷?野꺜筌??袁⑹뒄 ??곸벉
       return;
     }
 
     try {
-      // fuzz ?뚯뒪?? ?섏떇 寃利앹슜 (sandboxed Function?쇰줈 eval ?泥?
+      // fuzz ???뮞?? ??뤿뻼 野꺜筌앹빘??(sandboxed Function??곗쨮 eval ??筌?
       const evalResult = new Function(`return (${cleanExpr})`)() as number;
 
       if (typeof expected === 'number') {
-        // 遺???뚯닔???ㅼ감 ?덉슜 (0.0001)
+        // ?봔?????땾????쇨컧 ??됱뒠 (0.0001)
         const diff = Math.abs(evalResult - expected);
         expect(
           diff,
@@ -51,7 +48,7 @@ describe('MathProblemGenerator - Fuzz Testing', () => {
         ).toBeLessThan(0.0001);
       }
     } catch (_) {
-      // ?뚯떛 ?ㅽ뙣 耳?댁뒪 (臾댁떆)
+      // ???뼓 ??쎈솭 ?냈??곷뮞 (?얜똻??
     }
   }
 
@@ -61,17 +58,15 @@ describe('MathProblemGenerator - Fuzz Testing', () => {
         const problem = generateProblem(stage.id);
         const { expression, answer } = problem;
 
-        // 1. ?좏슚??寃??(NaN/Infinity)
+        // 1. ?醫륁뒞??野꺜??(NaN/Infinity)
         if (typeof answer === 'number') {
           expect(Number.isNaN(answer)).toBe(false);
           expect(Number.isFinite(answer)).toBe(true);
         }
 
-        // 2. ?섏떇怨??뺣떟 ?쇱튂 寃利?-        verifyAnswer(expression, answer);
+        // 2. ??뤿뻼???類ｋ뼗 ??깊뒄 野꺜筌?        verifyAnswer(expression, answer);
 
-        // 3. ?쒖빟 議곌굔 ?ы솗??-        if (typeof answer === 'number') {
+        // 3. ??뽯튋 鈺곌퀗援??????        if (typeof answer === 'number') {
           if (stage.constraints?.resultMax !== undefined) {
             expect(answer).toBeLessThanOrEqual(stage.constraints.resultMax + 0.0001);
           }
diff --git a/src/utils/__tests__/MathProblemGenerator.test.ts b/src/features/quiz/utils/__tests__/MathProblemGenerator.test.ts
similarity index 92%
rename from src/utils/__tests__/MathProblemGenerator.test.ts
rename to src/features/quiz/utils/__tests__/MathProblemGenerator.test.ts
index ff5d25a..830de8f 100644
--- a/src/utils/__tests__/MathProblemGenerator.test.ts
+++ b/src/features/quiz/utils/__tests__/MathProblemGenerator.test.ts
@@ -1,5 +1,5 @@
-import { describe, it, expect } from 'vitest';
-import { generateProblem, STAGES, type StageConfig } from '../MathProblemGenerator';
+癤퓁mport { describe, it, expect } from 'vitest';
+import { generateProblem, STAGES, type StageConfig } from '@/features/quiz/utils/MathProblemGenerator';
 
 describe('MathProblemGenerator', () => {
   const mockRng = {
@@ -25,7 +25,7 @@ describe('MathProblemGenerator', () => {
     it('should generate a valid problem for stage 7 (division)', () => {
       // Division logic ignores rng for quotient (uses 2-9)
       const problem = generateProblem(7, 'easy', 'normal', mockRng);
-      expect(problem.expression).toContain('첨');
+      expect(problem.expression).toContain('泥?);
       expect(problem.answer).toBeGreaterThan(0);
     });
 
@@ -45,13 +45,13 @@ describe('MathProblemGenerator', () => {
 
     it('should generate a valid problem for stage 18 (modulo)', () => {
       const problem = generateProblem(18, 'medium', 'normal', mockRng);
-      expect(problem.expression).toContain('?섎㉧吏');
+      expect(problem.expression).toContain('??롢돢筌왖');
       expect(problem.answer).toBeLessThan(7);
     });
 
     it('should generate a valid problem for stage 19 (fill-blank)', () => {
       const problem = generateProblem(19, 'medium', 'normal', mockRng);
-      expect(problem.expression).toContain('??);
+      expect(problem.expression).toContain('??);
       expect(problem.answer).toBeGreaterThan(0);
     });
 
@@ -114,7 +114,7 @@ describe('MathProblemGenerator', () => {
       STAGES.push(timeStage);
 
       const problem = generateProblem(99, 'easy', 'normal', mockRng);
-      expect(problem.expression).toContain('遺?);
+      expect(problem.expression).toContain('??);
       expect(problem.expression).toContain(':');
 
       STAGES.pop();
diff --git a/src/utils/__tests__/StatsProblemGenerator.test.ts b/src/features/quiz/utils/__tests__/StatsProblemGenerator.test.ts
similarity index 93%
rename from src/utils/__tests__/StatsProblemGenerator.test.ts
rename to src/features/quiz/utils/__tests__/StatsProblemGenerator.test.ts
index dde8a22..d2f4040 100644
--- a/src/utils/__tests__/StatsProblemGenerator.test.ts
+++ b/src/features/quiz/utils/__tests__/StatsProblemGenerator.test.ts
@@ -1,5 +1,5 @@
-import { describe, it, expect } from 'vitest';
-import { generateStatsProblem } from '../StatsProblemGenerator';
+癤퓁mport { describe, it, expect } from 'vitest';
+import { generateStatsProblem } from '@/features/quiz/utils/StatsProblemGenerator';
 
 describe('StatsProblemGenerator', () => {
   const mockRng = (intValues: number[], randomValues: number[] = []) => {
@@ -23,7 +23,7 @@ describe('StatsProblemGenerator', () => {
     // 1, 2, 3 sum to 6, avg 2 (Integer)
     const rng = mockRng([1, 2, 3]);
     const problem = generateStatsProblem(1, 'easy', rng);
-    expect(problem.question).toContain('1, 2, 3???됯퇏??');
+    expect(problem.question).toContain('1, 2, 3?????뇧???');
     expect(problem.answer).toBe(2);
   });
 
@@ -50,14 +50,14 @@ describe('StatsProblemGenerator', () => {
   it('should generate Level 5 Dice problem (random path sum)', () => {
     const rng = { ...mockRng([]), random: () => 0.6 };
     const problem = generateStatsProblem(5, 'easy', rng);
-    expect(problem.question).toContain('?⑹씠 7');
+    expect(problem.question).toContain('??뱀뵠 7');
     expect(problem.answer).toBe(6);
   });
 
   it('should generate Level 5 Dice problem (random path count)', () => {
     const rng = { ...mockRng([]), random: () => 0.4 };
     const problem = generateStatsProblem(5, 'easy', rng);
-    expect(problem.question).toContain('3??諛곗닔');
+    expect(problem.question).toContain('3??獄쏄퀣??);
     expect(problem.answer).toBe(2);
   });
 
diff --git a/src/utils/__tests__/algebra.test.ts b/src/features/quiz/utils/__tests__/algebra.test.ts
similarity index 95%
rename from src/utils/__tests__/algebra.test.ts
rename to src/features/quiz/utils/__tests__/algebra.test.ts
index bdc5e6b..1174a84 100644
--- a/src/utils/__tests__/algebra.test.ts
+++ b/src/features/quiz/utils/__tests__/algebra.test.ts
@@ -1,5 +1,5 @@
-import { describe, it, expect, vi } from 'vitest';
-import { parseEquation } from '../algebra';
+癤퓁mport { describe, it, expect, vi } from 'vitest';
+import { parseEquation } from '@/features/quiz/types/algebra';
 
 describe('parseEquation', () => {
   it('should parse a simple equation with spaces', () => {
diff --git a/src/features/quiz/utils/__tests__/challenge.test.ts b/src/features/quiz/utils/__tests__/challenge.test.ts
index 4ecfaef..a5bc7c9 100644
--- a/src/features/quiz/utils/__tests__/challenge.test.ts
+++ b/src/features/quiz/utils/__tests__/challenge.test.ts
@@ -1,15 +1,15 @@
-import { describe, it, expect, beforeEach, vi, afterEach } from 'vitest';
+癤퓁mport { describe, it, expect, beforeEach, vi, afterEach } from 'vitest';
 import {
   getTodayChallenge,
   generateTodayChallenge,
   SeededRandom,
   type TodayChallenge,
-} from '../challenge';
-import { storageService, STORAGE_KEYS } from '../../services';
-import { APP_CONFIG } from '../../config/app';
+} from '@/features/quiz/types/challenge';
+import { storageService, STORAGE_KEYS } from '@/features/quiz/services';
+import { APP_CONFIG } from '@/features/quiz/config/app';
 
 // Mock storage
-vi.mock('../../services', () => ({
+vi.mock('@/features/quiz/services', () => ({
   storageService: {
     get: vi.fn(),
     set: vi.fn(),
@@ -68,7 +68,7 @@ describe('challenge', () => {
 
       const challenge = generateTodayChallenge({}, mockFlags);
       expect(challenge.topicId).toBe('default');
-      expect(challenge.title).toContain('湲곕낯 梨뚮┛吏');
+      expect(challenge.title).toContain('疫꿸퀡??筌?슢?쏉쭪?');
 
       // Cleanup
       // @ts-expect-error -- Restoring readonly config
@@ -85,7 +85,7 @@ describe('challenge', () => {
 
       const challenge = generateTodayChallenge({}, mockFlags);
       expect(challenge.level).toBe(1);
-      expect(challenge.title).toContain('?낅Ц!');
+      expect(challenge.title).toContain('??끦?');
 
       // Cleanup
       // @ts-expect-error -- Restoring readonly config
@@ -99,9 +99,9 @@ describe('challenge', () => {
       const cachedChallenge: TodayChallenge = {
         id: `today_challenge_${todayDate}`,
         title: 'Cached Challenge',
-        category: '?섑븰',
+        category: '??묐린',
         categoryId: 'math',
-        topic: '?㏃뀍',
+        topic: '??껊?,
         topicId: 'addition',
         mode: 'time-attack',
         level: 1,
diff --git a/src/features/quiz/utils/__tests__/japanese.test.ts b/src/features/quiz/utils/__tests__/japanese.test.ts
index 485b1a6..55fb26a 100644
--- a/src/features/quiz/utils/__tests__/japanese.test.ts
+++ b/src/features/quiz/utils/__tests__/japanese.test.ts
@@ -1,5 +1,5 @@
-import { describe, it, expect } from 'vitest';
-import { generateJapaneseQuestion, normalizeRomaji } from '../japanese';
+癤퓁mport { describe, it, expect } from 'vitest';
+import { generateJapaneseQuestion, normalizeRomaji } from '@/features/quiz/utils/japanese';
 
 describe('japanese utility', () => {
   it('should generate a Hiragana question by default', () => {
@@ -12,15 +12,15 @@ describe('japanese utility', () => {
   });
 
   it('should generate a Katakana question', () => {
-    const question = generateJapaneseQuestion('medium', '媛?移대굹');
+    const question = generateJapaneseQuestion('medium', '揶쎛??燁삳?援?);
     expect(question).toHaveProperty('question');
     expect(question).toHaveProperty('answer');
   });
 
   it('should generate a Vocabulary question', () => {
-    const question = generateJapaneseQuestion('medium', '?댄쐶');
+    const question = generateJapaneseQuestion('medium', '??꾩맯');
     expect(question).toHaveProperty('question');
-    expect(question.question).toContain('???쎈뒗 踰뺤??');
+    expect(question.question).toContain('????덈뮉 甕곕벡??');
     expect(question).toHaveProperty('answer');
   });
 
diff --git a/src/utils/__tests__/logic_latency.test.ts b/src/features/quiz/utils/__tests__/logic_latency.test.ts
similarity index 59%
rename from src/utils/__tests__/logic_latency.test.ts
rename to src/features/quiz/utils/__tests__/logic_latency.test.ts
index 588fce7..13b1f95 100644
--- a/src/utils/__tests__/logic_latency.test.ts
+++ b/src/features/quiz/utils/__tests__/logic_latency.test.ts
@@ -1,9 +1,9 @@
-import { describe, it, expect, vi } from 'vitest';
-import { calculateScoreForTier } from '../tierUtils';
-import { generateProblem } from '../MathProblemGenerator';
+癤퓁mport { describe, it, expect, vi } from 'vitest';
+import { calculateScoreForTier } from '@/features/quiz/utils/tierUtils';
+import { generateProblem } from '@/features/quiz/utils/MathProblemGenerator';
 
-// ?㎦ Mock environment and constants to avoid network requests during logic tests
-vi.mock('../constants/tiers', () => ({
+// ???Mock environment and constants to avoid network requests during logic tests
+vi.mock('@/features/quiz/constants/tiers', () => ({
   loadTierDefinitions: vi.fn().mockResolvedValue([
     { level: 0, name: 'Base', icon: '', minScore: 0, colorVar: '' },
     { level: 5, name: 'Summit', icon: '', minScore: 100000, colorVar: '' },
@@ -11,7 +11,7 @@ vi.mock('../constants/tiers', () => ({
   loadCycleCap: vi.fn().mockResolvedValue(250000),
 }));
 
-vi.mock('../env', () => ({
+vi.mock('@/features/quiz/env', () => ({
   ENV: {
     VITE_SUPABASE_URL: 'https://mock.supabase.co',
     VITE_SUPABASE_ANON_KEY: 'mock-key',
@@ -28,8 +28,8 @@ vi.mock('../env', () => ({
 }));
 
 /**
- * ??Logic Leash (濡쒖쭅 紐⑹쨪)
- * "?먮━硫?諛고룷 湲덉?" ?뺤콉??吏묓뻾?섎뒗 ?깅뒫 蹂댁쬆 ?뚯뒪?몄엯?덈떎.
+ * ??Logic Leash (嚥≪뮇彛?筌뤴뫗夷?
+ * "?癒?봺筌?獄쏄퀬猷?疫뀀뜆?" ?類ㅼ퐠??筌욌쵑六??롫뮉 ?源낅뮟 癰귣똻弛????뮞?紐꾩뿯??덈뼄.
  */
 describe('Gameplay Logic Latency Check', () => {
   const measureExecution = (fn: () => void, iterations: number) => {
@@ -42,31 +42,28 @@ describe('Gameplay Logic Latency Check', () => {
   };
 
   /**
-   * 1. 臾몄젣 ?앹꽦 ?띾룄 寃利?-   * - 紐⑺몴: 臾몄젣 100媛쒕? 50ms ?덉뿉 ?앹꽦 (媛쒕떦 0.5ms)
-   * - ?ㅽ뙣 ?? 臾몄젣 ?앹꽦 ?뚭퀬由ъ쬁???덈Т 臾닿굅?뚯죱?뚯쓣 ?섎? (???좊컻)
+   * 1. ?얜챷????밴쉐 ??얜즲 野꺜筌?   * - 筌뤴뫚紐? ?얜챷??100揶쏆뮆? 50ms ??됰퓠 ??밴쉐 (揶쏆뮆??0.5ms)
+   * - ??쎈솭 ?? ?얜챷????밴쉐 ???х뵳?弛????댭??얜떯援???１???뱽 ??? (???醫딆뻣)
    */
   it('should generate 100 arithmetic problems under 50ms', () => {
     const iterations = 100;
     const limitMs = 100; // Increased from 50ms for CI stability
 
     const duration = measureExecution(() => {
-      // ?덈꺼 1~15 臾댁옉???앹꽦
+      // ??덇볼 1~15 ?얜똻?????밴쉐
       generateProblem(Math.floor(Math.random() * 15) + 1);
     }, iterations);
 
     console.log(
-      `?깍툘 [Problem Gen] ${iterations} items took ${duration.toFixed(2)}ms (Limit: ${limitMs}ms)`
+      `?源랁닔 [Problem Gen] ${iterations} items took ${duration.toFixed(2)}ms (Limit: ${limitMs}ms)`
     );
 
     expect(duration).toBeLessThan(limitMs);
   });
 
   /**
-   * 2. ?곗뼱 ?먯닔 怨꾩궛 ?띾룄 寃利?-   * - 紐⑺몴: ?먯닔 怨꾩궛 1000?뚮? 20ms ?덉뿉 ?꾨즺 (?뚮떦 0.02ms)
-   * - ?ㅽ뙣 ?? ??궧 ?섏씠吏??寃곌낵李쎌뿉??UI ?앸슍 ?딄? 諛쒖깮 媛??-   */
+   * 2. ?怨쀫선 ?癒?땾 ?④쑴沅???얜즲 野꺜筌?   * - 筌뤴뫚紐? ?癒?땾 ?④쑴沅?1000??? 20ms ??됰퓠 ?袁⑥┷ (???뼣 0.02ms)
+   * - ??쎈솭 ?? ??沅???륁뵠筌왖??野껉퀗?듸㎕?뚮퓠??UI ??몄뒐 ??? 獄쏆뮇源?揶쎛??   */
   it('should calculate tier scores 1000 times under 300ms', async () => {
     const iterations = 1000;
     const limitMs = 300; // Increased for pre-commit hook stability on varied hardware
@@ -79,7 +76,7 @@ describe('Gameplay Logic Latency Check', () => {
     const duration = performance.now() - start;
 
     console.log(
-      `?깍툘 [Tier Calc] ${iterations} ops took ${duration.toFixed(2)}ms (Limit: ${limitMs}ms)`
+      `?源랁닔 [Tier Calc] ${iterations} ops took ${duration.toFixed(2)}ms (Limit: ${limitMs}ms)`
     );
 
     expect(duration).toBeLessThan(limitMs);
diff --git a/src/features/quiz/utils/__tests__/math.test.ts b/src/features/quiz/utils/__tests__/math.test.ts
index 4bec607..1c0d428 100644
--- a/src/features/quiz/utils/__tests__/math.test.ts
+++ b/src/features/quiz/utils/__tests__/math.test.ts
@@ -1,9 +1,9 @@
-import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
-import { generateRandomNumber } from '../math';
+癤퓁mport { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
+import { generateRandomNumber } from '@/features/quiz/utils/math';
 import { Difficulty } from '@/features/quiz';
 
 // Mock NUMBER_RANGE_BY_DIFFICULTY
-vi.mock('../constants/game', () => ({
+vi.mock('@/features/quiz/constants/game', () => ({
   NUMBER_RANGE_BY_DIFFICULTY: {
     easy: { min: 0, max: 9 },
     medium: { min: 10, max: 99 },
diff --git a/src/utils/__tests__/performance.bench.ts b/src/features/quiz/utils/__tests__/performance.bench.ts
similarity index 100%
rename from src/utils/__tests__/performance.bench.ts
rename to src/features/quiz/utils/__tests__/performance.bench.ts
diff --git a/src/features/quiz/utils/__tests__/quizGenerator.test.ts b/src/features/quiz/utils/__tests__/quizGenerator.test.ts
index b740209..cc9c821 100644
--- a/src/features/quiz/utils/__tests__/quizGenerator.test.ts
+++ b/src/features/quiz/utils/__tests__/quizGenerator.test.ts
@@ -1,13 +1,13 @@
-import { describe, it, expect, vi, beforeEach } from 'vitest';
-import { generateQuestion } from '../quizGenerator';
-import * as MathProblemGenerator from '../MathProblemGenerator';
-import * as EquationProblemGenerator from '../EquationProblemGenerator';
-import * as LogicProblemGenerator from '../LogicProblemGenerator';
-import * as GeometryProblemGenerator from '../GeometryProblemGenerator';
-import * as StatsProblemGenerator from '../StatsProblemGenerator';
-import * as CSProblemGenerator from '../CSProblemGenerator';
-import * as CalculusProblemGenerator from '../CalculusProblemGenerator';
-import { Mountain, World } from '../../types/quiz';
+癤퓁mport { describe, it, expect, vi, beforeEach } from 'vitest';
+import { generateQuestion } from '@/features/quiz/utils/quizGenerator';
+import * as MathProblemGenerator from '@/features/quiz/utils/MathProblemGenerator';
+import * as EquationProblemGenerator from '@/features/quiz/utils/EquationProblemGenerator';
+import * as LogicProblemGenerator from '@/features/quiz/utils/LogicProblemGenerator';
+import * as GeometryProblemGenerator from '@/features/quiz/utils/GeometryProblemGenerator';
+import * as StatsProblemGenerator from '@/features/quiz/utils/StatsProblemGenerator';
+import * as CSProblemGenerator from '@/features/quiz/utils/CSProblemGenerator';
+import * as CalculusProblemGenerator from '@/features/quiz/utils/CalculusProblemGenerator';
+import { Mountain, World } from '@/features/quiz/types/quiz';
 
 describe('quizGenerator Unit Tests', () => {
   beforeEach(() => {
@@ -18,44 +18,44 @@ describe('quizGenerator Unit Tests', () => {
 
   describe('generateQuestion', () => {
     it('should route to MathProblemGenerator for math World1 (Arithmetic)', () => {
-      const q = generateQuestion('math', 'World1', 'World1-湲곗큹', 1, 'easy');
+      const q = generateQuestion('math', 'World1', 'World1-疫꿸퀣??, 1, 'easy');
       expect(q).toHaveProperty('question');
       expect(q).toHaveProperty('answer');
     });
 
     it('should route to EquationProblemGenerator for math World1 (Algebra)', () => {
-      const q = generateQuestion('math', 'World1', 'World1-???, 1, 'easy');
+      const q = generateQuestion('math', 'World1', 'World1-????, 1, 'easy');
       expect(q).toHaveProperty('question');
-      expect(q.question).toContain('??); // Or '=' depending on difficulty, but usually '?? for easy
+      expect(q.question).toContain('??); // Or '=' depending on difficulty, but usually '?? for easy
     });
 
     it('should route to GeometryProblemGenerator for math World2 (Geometry)', () => {
-      const q = generateQuestion('math', 'World2', 'World2-湲곗큹', 1, 'easy');
+      const q = generateQuestion('math', 'World2', 'World2-疫꿸퀣??, 1, 'easy');
       expect(q).toHaveProperty('question');
     });
 
     it('should route to StatsProblemGenerator for math World3 (Stats)', () => {
-      const q = generateQuestion('math', 'World3', 'World3-湲곗큹', 1, 'easy');
+      const q = generateQuestion('math', 'World3', 'World3-疫꿸퀣??, 1, 'easy');
       expect(q).toHaveProperty('question');
     });
 
     it('should route to CSProblemGenerator for math World4 (CS)', () => {
-      const q = generateQuestion('math', 'World4', 'World4-湲곗큹', 1, 'easy');
+      const q = generateQuestion('math', 'World4', 'World4-疫꿸퀣??, 1, 'easy');
       expect(q).toHaveProperty('question');
     });
 
     it('should route to LogicProblemGenerator for logic mountain', () => {
-      const q = generateQuestion('logic', 'World1', 'World1-湲곗큹', 1, 'easy');
+      const q = generateQuestion('logic', 'World1', 'World1-疫꿸퀣??, 1, 'easy');
       expect(q).toHaveProperty('question');
     });
 
     it('should route to CSProblemGenerator for general World1 (CS)', () => {
-      const q = generateQuestion('general', 'World1', 'World1-湲곗큹', 1, 'easy');
+      const q = generateQuestion('general', 'World1', 'World1-疫꿸퀣??, 1, 'easy');
       expect(q).toHaveProperty('question');
     });
 
     it('should route to CalculusProblemGenerator for general World2 (Calculus)', () => {
-      const q = generateQuestion('general', 'World2', 'World2-湲곗큹', 1, 'easy');
+      const q = generateQuestion('general', 'World2', 'World2-疫꿸퀣??, 1, 'easy');
       expect(q).toHaveProperty('question');
     });
 
@@ -63,7 +63,7 @@ describe('quizGenerator Unit Tests', () => {
       const q = generateQuestion(
         'general',
         'UnknownWorld' as unknown as World,
-        'World1-湲곗큹',
+        'World1-疫꿸퀣??,
         1,
         'easy'
       );
@@ -74,7 +74,7 @@ describe('quizGenerator Unit Tests', () => {
       const q = generateQuestion(
         'unknown' as unknown as Mountain,
         'UnknownWorld' as unknown as World,
-        'World1-湲곗큹',
+        'World1-疫꿸퀣??,
         1,
         'easy'
       );
diff --git a/src/features/quiz/utils/__tests__/quizTimeCalculator.test.ts b/src/features/quiz/utils/__tests__/quizTimeCalculator.test.ts
index 4c51add..303e5e9 100644
--- a/src/features/quiz/utils/__tests__/quizTimeCalculator.test.ts
+++ b/src/features/quiz/utils/__tests__/quizTimeCalculator.test.ts
@@ -1,53 +1,50 @@
-import { describe, it, expect } from 'vitest';
-import { calculateDynamicTimeLimit } from '../quizTimeCalculator';
+癤퓁mport { describe, it, expect } from 'vitest';
+import { calculateDynamicTimeLimit } from '@/features/quiz/utils/quizTimeCalculator';
 
 describe('calculateDynamicTimeLimit', () => {
-  // Happy Path: 湲곕낯 移댄뀒怨좊━, ?덈꺼 1, 臾몄젣 0媛??? ?곹깭
+  // Happy Path: 疫꿸퀡??燁삳똾?믤⑥쥓?? ??덇볼 1, ?얜챷??0揶????? ?怨밴묶
   it('should return baseTime * START pressure for first question at level 1', () => {
     const result = calculateDynamicTimeLimit({
-      questionCategory: '湲곗큹',
+      questionCategory: '疫꿸퀣??,
       questionLevel: 1,
       totalQuestionsAnswered: 0,
     });
 
-    // 湲곗큹 maxLevel = 30, normalizedLv = ceil(1/30 * 10) = 1
+    // 疫꿸퀣??maxLevel = 30, normalizedLv = ceil(1/30 * 10) = 1
     // LEVEL_BASE_TIME[1] = 3, PRESSURE_FACTOR.START = 2.0
     // result = 3 * 2.0 = 6
     expect(result).toBe(6);
   });
 
-  // 以묎컙 ?덈꺼 ?뚯뒪??-  it('should return correct time for mid-level question', () => {
+  // 餓λ쵌而???덇볼 ???뮞??  it('should return correct time for mid-level question', () => {
     const result = calculateDynamicTimeLimit({
-      questionCategory: '湲곗큹',
+      questionCategory: '疫꿸퀣??,
       questionLevel: 15,
       totalQuestionsAnswered: 0,
     });
 
-    // 湲곗큹 maxLevel = 30, normalizedLv = ceil(15/30 * 10) = 5
+    // 疫꿸퀣??maxLevel = 30, normalizedLv = ceil(15/30 * 10) = 5
     // LEVEL_BASE_TIME[5] = 10, PRESSURE_FACTOR.START = 2.0
     // result = 10 * 2.0 = 20
     expect(result).toBe(20);
   });
 
-  // 理쒓퀬 ?덈꺼 ?뚯뒪??-  it('should return correct time for max-level question', () => {
+  // 筌ㅼ뮄????덇볼 ???뮞??  it('should return correct time for max-level question', () => {
     const result = calculateDynamicTimeLimit({
-      questionCategory: '湲곗큹',
+      questionCategory: '疫꿸퀣??,
       questionLevel: 30,
       totalQuestionsAnswered: 0,
     });
 
-    // 湲곗큹 maxLevel = 30, normalizedLv = ceil(30/30 * 10) = 10
+    // 疫꿸퀣??maxLevel = 30, normalizedLv = ceil(30/30 * 10) = 10
     // LEVEL_BASE_TIME[10] = 20, PRESSURE_FACTOR.START = 2.0
     // result = 20 * 2.0 = 40
     expect(result).toBe(40);
   });
 
-  // ?뺣컯 怨꾩닔 媛먯냼 ?뚯뒪??-  it('should apply pressure decay based on totalQuestionsAnswered', () => {
+  // ?類ｌ뺏 ?④쑴??揶쏅Ŋ?????뮞??  it('should apply pressure decay based on totalQuestionsAnswered', () => {
     const result = calculateDynamicTimeLimit({
-      questionCategory: '湲곗큹',
+      questionCategory: '疫꿸퀣??,
       questionLevel: 1,
       totalQuestionsAnswered: 50,
     });
@@ -58,10 +55,9 @@ describe('calculateDynamicTimeLimit', () => {
     expect(result).toBe(4.5);
   });
 
-  // 理쒖냼 ?뺣컯 怨꾩닔 蹂댁옣 ?뚯뒪??-  it('should clamp pressure at MIN value', () => {
+  // 筌ㅼ뮇???類ｌ뺏 ?④쑴??癰귣똻?????뮞??  it('should clamp pressure at MIN value', () => {
     const result = calculateDynamicTimeLimit({
-      questionCategory: '湲곗큹',
+      questionCategory: '疫꿸퀣??,
       questionLevel: 1,
       totalQuestionsAnswered: 200,
     });
@@ -71,24 +67,23 @@ describe('calculateDynamicTimeLimit', () => {
     expect(result).toBeCloseTo(2.4);
   });
 
-  // ?ㅻⅨ 移댄뀒怨좊━ ?뚯뒪??-  it('should use category-specific maxLevel for normalization', () => {
+  // ??삘뀲 燁삳똾?믤⑥쥓?????뮞??  it('should use category-specific maxLevel for normalization', () => {
     const result = calculateDynamicTimeLimit({
-      questionCategory: '?쇰━',
+      questionCategory: '??겸봺',
       questionLevel: 15,
       totalQuestionsAnswered: 0,
     });
 
-    // ?쇰━ maxLevel = 15, normalizedLv = ceil(15/15 * 10) = 10
+    // ??겸봺 maxLevel = 15, normalizedLv = ceil(15/15 * 10) = 10
     // LEVEL_BASE_TIME[10] = 20, pressure = 2.0
     // result = 20 * 2.0 = 40
     expect(result).toBe(40);
   });
 
-  // Edge: ?????녿뒗 移댄뀒怨좊━ ??default maxLevel ?ъ슜
+  // Edge: ??????용뮉 燁삳똾?믤⑥쥓????default maxLevel ????   it('should use default maxLevel for unknown category', () => {
     const result = calculateDynamicTimeLimit({
-      questionCategory: '?뚯닔?녿뒗',
+      questionCategory: '???땾??용뮉',
       questionLevel: 5,
       totalQuestionsAnswered: 0,
     });
@@ -99,16 +94,16 @@ describe('calculateDynamicTimeLimit', () => {
     expect(result).toBe(20);
   });
 
-  // Edge: 鍮?移댄뀒怨좊━ ??湲곗큹濡??대갚
-  it('should fallback to 湲곗큹 when category is empty string', () => {
+  // Edge: ??燁삳똾?믤⑥쥓????疫꿸퀣?방에???媛?+  it('should fallback to 疫꿸퀣??when category is empty string', () => {
     const result = calculateDynamicTimeLimit({
       questionCategory: '',
       questionLevel: 1,
       totalQuestionsAnswered: 0,
     });
 
-    // '' ??'湲곗큹', maxLevel = 30, normalizedLv = ceil(1/30*10) = 1
-    // baseTime = 3, pressure = 2.0 ??6
+    // '' ??'疫꿸퀣??, maxLevel = 30, normalizedLv = ceil(1/30*10) = 1
+    // baseTime = 3, pressure = 2.0 ??6
     expect(result).toBe(6);
   });
 });
diff --git a/src/features/quiz/utils/__tests__/scoreCalculator.test.ts b/src/features/quiz/utils/__tests__/scoreCalculator.test.ts
index 40a29bf..edc4a00 100644
--- a/src/features/quiz/utils/__tests__/scoreCalculator.test.ts
+++ b/src/features/quiz/utils/__tests__/scoreCalculator.test.ts
@@ -1,4 +1,4 @@
-import { describe, it, expect } from 'vitest';
+癤퓁mport { describe, it, expect } from 'vitest';
 import {
   calculateTotalAltitude,
   calculateSubTopicAltitude,
@@ -6,18 +6,18 @@ import {
   calculateSubTopicProgress,
   calculateCategoryAltitude,
   calculateCategoryProgress,
-} from '../scoreCalculator';
+} from '@/features/quiz/utils/scoreCalculator';
 
 describe('scoreCalculator', () => {
   const mockProgress = {
     World1: {
-      湲곗큹: {
+      疫꿸퀣?? {
         L1: { bestScore: { 'time-attack': 100 } },
         L2: { bestScore: { survival: 150 } },
       },
     },
     World2: {
-      ?쇰━: {
+      ??겸봺: {
         L1: { bestScore: { 'time-attack': 50, survival: 200 } },
       },
     },
@@ -33,19 +33,19 @@ describe('scoreCalculator', () => {
 
   describe('calculateSubTopicAltitude', () => {
     it('should calculate altitude for a specific subtopic', () => {
-      const result = calculateSubTopicAltitude('World1', '湲곗큹', mockProgress);
+      const result = calculateSubTopicAltitude('World1', '疫꿸퀣??, mockProgress);
       expect(result).toBe(250); // 100 + 150
     });
 
     it('should return 0 for non-existent category/subtopic', () => {
-      const result = calculateSubTopicAltitude('World3', '湲곗큹', mockProgress);
+      const result = calculateSubTopicAltitude('World3', '疫꿸퀣??, mockProgress);
       expect(result).toBe(0);
     });
   });
 
   describe('calculateSubTopicTargetAltitude', () => {
     it('should calculate target altitude based on level config', () => {
-      const result = calculateSubTopicTargetAltitude('World1', '湲곗큹');
+      const result = calculateSubTopicTargetAltitude('World1', '疫꿸퀣??);
       expect(result).toBeGreaterThan(0);
     });
   });
@@ -54,12 +54,12 @@ describe('scoreCalculator', () => {
     it('should calculate progress percentage', () => {
       const progress = {
         World1: {
-          湲곗큹: {
+          疫꿸퀣?? {
             L1: { bestScore: { 'time-attack': 1000 } } // Increased score to ensure > 0%
           }
         }
       };
-      const result = calculateSubTopicProgress('World1', '湲곗큹', progress);
+      const result = calculateSubTopicProgress('World1', '疫꿸퀣??, progress);
       expect(result.currentAltitude).toBe(1000);
       expect(result.targetAltitude).toBeGreaterThan(0);
       expect(result.progressPercent).toBeGreaterThan(0);
@@ -69,11 +69,11 @@ describe('scoreCalculator', () => {
   describe('calculateCategoryAltitude', () => {
     it('should calculate altitude by searching through all worlds', () => {
       const progress = {
-        World1: { 湲곗큹: { L1: { bestScore: { 'time-attack': 100 } } } },
-        World2: { 湲곗큹: { L1: { bestScore: { survival: 200 } } } },
+        World1: { 疫꿸퀣?? { L1: { bestScore: { 'time-attack': 100 } } } },
+        World2: { 疫꿸퀣?? { L1: { bestScore: { survival: 200 } } } },
       };
 
-      const result = calculateCategoryAltitude('湲곗큹', progress);
+      const result = calculateCategoryAltitude('疫꿸퀣??, progress);
       expect(result.totalAltitude).toBe(300);
     });
 
@@ -86,9 +86,9 @@ describe('scoreCalculator', () => {
   describe('calculateCategoryProgress', () => {
     it('should calculate category progress', () => {
       const progress = {
-        World1: { 湲곗큹: { L1: { bestScore: { 'time-attack': 1000 } } } }
+        World1: { 疫꿸퀣?? { L1: { bestScore: { 'time-attack': 1000 } } } }
       };
-      const result = calculateCategoryProgress('湲곗큹', progress);
+      const result = calculateCategoryProgress('疫꿸퀣??, progress);
       expect(result.currentAltitude).toBe(1000);
       expect(result.targetAltitude).toBeGreaterThan(0);
       expect(result.progressPercent).toBeGreaterThan(0);
diff --git a/src/features/quiz/utils/__tests__/tierUtils.test.ts b/src/features/quiz/utils/__tests__/tierUtils.test.ts
index 3dc434b..5368011 100644
--- a/src/features/quiz/utils/__tests__/tierUtils.test.ts
+++ b/src/features/quiz/utils/__tests__/tierUtils.test.ts
@@ -1,9 +1,9 @@
-import { describe, it, expect, beforeEach, vi } from 'vitest';
-import { calculateScoreForTier } from '../tierUtils';
-import { loadTierDefinitions, loadCycleCap, type TierInfo } from '../../constants/tiers';
+癤퓁mport { describe, it, expect, beforeEach, vi } from 'vitest';
+import { calculateScoreForTier } from '@/features/quiz/utils/tierUtils';
+import { loadTierDefinitions, loadCycleCap, type TierInfo } from '@/features/quiz/constants/tiers';
 
 // Mock tiers constants
-vi.mock('../../constants/tiers', () => ({
+vi.mock('@/features/quiz/constants/tiers', () => ({
   loadTierDefinitions: vi.fn(),
   loadCycleCap: vi.fn(),
 }));
@@ -16,13 +16,13 @@ describe('tierUtils', () => {
   describe('calculateScoreForTier', () => {
     it('should calculate score for first cycle (stars = 0)', async () => {
       const mockTierDefinitions = [
-        { level: 0, name: '踰좎씠?ㅼ틺??, icon: '??, minScore: 0, colorVar: '--color-tier-base' },
-        { level: 1, name: '?깆궛濡?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
-        { level: 2, name: '以묓꽦', icon: '?곤툘', minScore: 5000, colorVar: '--color-tier-mid' },
-        { level: 3, name: '怨좎??', icon: '?룘截?, minScore: 20000, colorVar: '--color-tier-high' },
-        { level: 4, name: '遊됱슦由?, icon: '?쫭', minScore: 50000, colorVar: '--color-tier-peak' },
-        { level: 5, name: '?뺤긽', icon: '?슜', minScore: 100000, colorVar: '--color-tier-summit' },
-        { level: 6, name: '?꾩꽕', icon: '?몣', minScore: 250000, colorVar: '--color-tier-legend' },
+        { level: 0, name: '甕곗쥙???쇳떧??, icon: '??, minScore: 0, colorVar: '--color-tier-base' },
+        { level: 1, name: '?源녾텦嚥?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
+        { level: 2, name: '餓λ쵑苑?, icon: '?怨ㅽ닔', minScore: 5000, colorVar: '--color-tier-mid' },
+        { level: 3, name: '?⑥쥙???', icon: '?猷섉닼?, minScore: 20000, colorVar: '--color-tier-high' },
+        { level: 4, name: '?딅맩??뵳?, icon: '?已?, minScore: 50000, colorVar: '--color-tier-peak' },
+        { level: 5, name: '?類ㅺ맒', icon: '???, minScore: 100000, colorVar: '--color-tier-summit' },
+        { level: 6, name: '?袁⑷퐬', icon: '?紐?, minScore: 250000, colorVar: '--color-tier-legend' },
       ];
 
       vi.mocked(loadTierDefinitions).mockResolvedValue(mockTierDefinitions as TierInfo[]);
@@ -36,8 +36,8 @@ describe('tierUtils', () => {
 
     it('should calculate score for second cycle (stars = 1)', async () => {
       const mockTierDefinitions = [
-        { level: 0, name: '踰좎씠?ㅼ틺??, icon: '??, minScore: 0, colorVar: '--color-tier-base' },
-        { level: 1, name: '?깆궛濡?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
+        { level: 0, name: '甕곗쥙???쇳떧??, icon: '??, minScore: 0, colorVar: '--color-tier-base' },
+        { level: 1, name: '?源녾텦嚥?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
       ];
 
       vi.mocked(loadTierDefinitions).mockResolvedValue(mockTierDefinitions as TierInfo[]);
@@ -50,7 +50,7 @@ describe('tierUtils', () => {
 
     it('should calculate score for third cycle (stars = 2)', async () => {
       const mockTierDefinitions = [
-        { level: 2, name: '以묓꽦', icon: '?곤툘', minScore: 5000, colorVar: '--color-tier-mid' },
+        { level: 2, name: '餓λ쵑苑?, icon: '?怨ㅽ닔', minScore: 5000, colorVar: '--color-tier-mid' },
       ];
 
       vi.mocked(loadTierDefinitions).mockResolvedValue(mockTierDefinitions as TierInfo[]);
@@ -63,7 +63,7 @@ describe('tierUtils', () => {
 
     it('should include bonus score in calculation', async () => {
       const mockTierDefinitions = [
-        { level: 1, name: '?깆궛濡?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
+        { level: 1, name: '?源녾텦嚥?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
       ];
 
       vi.mocked(loadTierDefinitions).mockResolvedValue(mockTierDefinitions as TierInfo[]);
@@ -76,7 +76,7 @@ describe('tierUtils', () => {
 
     it('should include bonus score in cycle calculation', async () => {
       const mockTierDefinitions = [
-        { level: 1, name: '?깆궛濡?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
+        { level: 1, name: '?源녾텦嚥?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
       ];
 
       vi.mocked(loadTierDefinitions).mockResolvedValue(mockTierDefinitions as TierInfo[]);
@@ -89,13 +89,13 @@ describe('tierUtils', () => {
 
     it('should handle all tier levels (0-6)', async () => {
       const mockTierDefinitions = [
-        { level: 0, name: '踰좎씠?ㅼ틺??, icon: '??, minScore: 0, colorVar: '--color-tier-base' },
-        { level: 1, name: '?깆궛濡?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
-        { level: 2, name: '以묓꽦', icon: '?곤툘', minScore: 5000, colorVar: '--color-tier-mid' },
-        { level: 3, name: '怨좎??', icon: '?룘截?, minScore: 20000, colorVar: '--color-tier-high' },
-        { level: 4, name: '遊됱슦由?, icon: '?쫭', minScore: 50000, colorVar: '--color-tier-peak' },
-        { level: 5, name: '?뺤긽', icon: '?슜', minScore: 100000, colorVar: '--color-tier-summit' },
-        { level: 6, name: '?꾩꽕', icon: '?몣', minScore: 250000, colorVar: '--color-tier-legend' },
+        { level: 0, name: '甕곗쥙???쇳떧??, icon: '??, minScore: 0, colorVar: '--color-tier-base' },
+        { level: 1, name: '?源녾텦嚥?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
+        { level: 2, name: '餓λ쵑苑?, icon: '?怨ㅽ닔', minScore: 5000, colorVar: '--color-tier-mid' },
+        { level: 3, name: '?⑥쥙???', icon: '?猷섉닼?, minScore: 20000, colorVar: '--color-tier-high' },
+        { level: 4, name: '?딅맩??뵳?, icon: '?已?, minScore: 50000, colorVar: '--color-tier-peak' },
+        { level: 5, name: '?類ㅺ맒', icon: '???, minScore: 100000, colorVar: '--color-tier-summit' },
+        { level: 6, name: '?袁⑷퐬', icon: '?紐?, minScore: 250000, colorVar: '--color-tier-legend' },
       ];
 
       vi.mocked(loadTierDefinitions).mockResolvedValue(mockTierDefinitions as TierInfo[]);
@@ -110,7 +110,7 @@ describe('tierUtils', () => {
 
     it('should return 0 when tier level does not exist', async () => {
       const mockTierDefinitions = [
-        { level: 1, name: '?깆궛濡?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
+        { level: 1, name: '?源녾텦嚥?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
       ];
 
       vi.mocked(loadTierDefinitions).mockResolvedValue(mockTierDefinitions as TierInfo[]);
@@ -123,7 +123,7 @@ describe('tierUtils', () => {
 
     it('should handle edge case: level 0 with stars', async () => {
       const mockTierDefinitions = [
-        { level: 0, name: '踰좎씠?ㅼ틺??, icon: '??, minScore: 0, colorVar: '--color-tier-base' },
+        { level: 0, name: '甕곗쥙???쇳떧??, icon: '??, minScore: 0, colorVar: '--color-tier-base' },
       ];
 
       vi.mocked(loadTierDefinitions).mockResolvedValue(mockTierDefinitions as TierInfo[]);
@@ -136,7 +136,7 @@ describe('tierUtils', () => {
 
     it('should handle edge case: maximum level with maximum stars', async () => {
       const mockTierDefinitions = [
-        { level: 6, name: '?꾩꽕', icon: '?몣', minScore: 250000, colorVar: '--color-tier-legend' },
+        { level: 6, name: '?袁⑷퐬', icon: '?紐?, minScore: 250000, colorVar: '--color-tier-legend' },
       ];
 
       vi.mocked(loadTierDefinitions).mockResolvedValue(mockTierDefinitions as TierInfo[]);
@@ -149,7 +149,7 @@ describe('tierUtils', () => {
 
     it('should use default cycleCap when not provided', async () => {
       const mockTierDefinitions = [
-        { level: 1, name: '?깆궛濡?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
+        { level: 1, name: '?源녾텦嚥?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
       ];
 
       vi.mocked(loadTierDefinitions).mockResolvedValue(mockTierDefinitions as TierInfo[]);
@@ -163,21 +163,20 @@ describe('tierUtils', () => {
 
     it('should handle negative stars value', async () => {
       const mockTierDefinitions = [
-        { level: 1, name: '?깆궛濡?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
+        { level: 1, name: '?源녾텦嚥?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
       ];
 
       vi.mocked(loadTierDefinitions).mockResolvedValue(mockTierDefinitions as TierInfo[]);
       vi.mocked(loadCycleCap).mockResolvedValue(250000);
 
-      // ?뚯닔 stars??0???꾨땲誘濡??ъ씠??怨꾩궛???ъ슜??-      const result = await calculateScoreForTier(1, -1, 0);
+      // ???땾 stars??0???袁⑤빍沃샕嚥???????④쑴沅???????      const result = await calculateScoreForTier(1, -1, 0);
 
       expect(result).toBe(250000 * -1 + 1000); // -250000 + 1000 = -249000
     });
 
     it('should handle negative bonusScore', async () => {
       const mockTierDefinitions = [
-        { level: 1, name: '?깆궛濡?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
+        { level: 1, name: '?源녾텦嚥?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
       ];
 
       vi.mocked(loadTierDefinitions).mockResolvedValue(mockTierDefinitions as TierInfo[]);
@@ -190,7 +189,7 @@ describe('tierUtils', () => {
 
     it('should handle very large bonusScore', async () => {
       const mockTierDefinitions = [
-        { level: 1, name: '?깆궛濡?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
+        { level: 1, name: '?源녾텦嚥?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
       ];
 
       vi.mocked(loadTierDefinitions).mockResolvedValue(mockTierDefinitions as TierInfo[]);
@@ -203,7 +202,7 @@ describe('tierUtils', () => {
 
     it('should handle very large stars value', async () => {
       const mockTierDefinitions = [
-        { level: 1, name: '?깆궛濡?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
+        { level: 1, name: '?源녾텦嚥?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
       ];
 
       vi.mocked(loadTierDefinitions).mockResolvedValue(mockTierDefinitions as TierInfo[]);
@@ -216,28 +215,27 @@ describe('tierUtils', () => {
 
     it('should handle decimal stars value', async () => {
       const mockTierDefinitions = [
-        { level: 1, name: '?깆궛濡?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
+        { level: 1, name: '?源녾텦嚥?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
       ];
 
       vi.mocked(loadTierDefinitions).mockResolvedValue(mockTierDefinitions as TierInfo[]);
       vi.mocked(loadCycleCap).mockResolvedValue(250000);
 
-      // ?뚯닔??stars??0???꾨땲誘濡??ъ씠??怨꾩궛???ъ슜??-      const result = await calculateScoreForTier(1, 1.5, 0);
+      // ???땾??stars??0???袁⑤빍沃샕嚥???????④쑴沅???????      const result = await calculateScoreForTier(1, 1.5, 0);
 
       expect(result).toBe(250000 * 1.5 + 1000); // 376000
     });
 
     it('should handle negative level gracefully', async () => {
       const mockTierDefinitions = [
-        { level: 0, name: '踰좎씠?ㅼ틺??, icon: '??, minScore: 0, colorVar: '--color-tier-base' },
-        { level: 1, name: '?깆궛濡?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
+        { level: 0, name: '甕곗쥙???쇳떧??, icon: '??, minScore: 0, colorVar: '--color-tier-base' },
+        { level: 1, name: '?源녾텦嚥?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
       ];
 
       vi.mocked(loadTierDefinitions).mockResolvedValue(mockTierDefinitions as TierInfo[]);
       vi.mocked(loadCycleCap).mockResolvedValue(250000);
 
-      // ?뚯닔 level? 李얠쓣 ???놁쑝誘濡?0 諛섑솚
+      // ???땾 level?? 筌≪뼚??????곸몵沃샕嚥?0 獄쏆꼹??       const result = await calculateScoreForTier(-1, 0, 0);
 
       expect(result).toBe(0); // targetTier?.minScore || 0
@@ -245,13 +243,13 @@ describe('tierUtils', () => {
 
     it('should handle very large level value', async () => {
       const mockTierDefinitions = [
-        { level: 6, name: '?꾩꽕', icon: '?몣', minScore: 250000, colorVar: '--color-tier-legend' },
+        { level: 6, name: '?袁⑷퐬', icon: '?紐?, minScore: 250000, colorVar: '--color-tier-legend' },
       ];
 
       vi.mocked(loadTierDefinitions).mockResolvedValue(mockTierDefinitions as TierInfo[]);
       vi.mocked(loadCycleCap).mockResolvedValue(250000);
 
-      // 留ㅼ슦 ??level? 李얠쓣 ???놁쑝誘濡?0 諛섑솚
+      // 筌띲끉????level?? 筌≪뼚??????곸몵沃샕嚥?0 獄쏆꼹??       const result = await calculateScoreForTier(999999, 0, 0);
 
       expect(result).toBe(0); // targetTier?.minScore || 0
@@ -259,7 +257,7 @@ describe('tierUtils', () => {
 
     it('should handle zero cycleCap', async () => {
       const mockTierDefinitions = [
-        { level: 1, name: '?깆궛濡?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
+        { level: 1, name: '?源녾텦嚥?, icon: '??', minScore: 1000, colorVar: '--color-tier-trail' },
       ];
 
       vi.mocked(loadTierDefinitions).mockResolvedValue(mockTierDefinitions as TierInfo[]);
@@ -272,13 +270,13 @@ describe('tierUtils', () => {
 
     it('should handle all parameters with edge values', async () => {
       const mockTierDefinitions = [
-        { level: 6, name: '?꾩꽕', icon: '?몣', minScore: 250000, colorVar: '--color-tier-legend' },
+        { level: 6, name: '?袁⑷퐬', icon: '?紐?, minScore: 250000, colorVar: '--color-tier-legend' },
       ];
 
       vi.mocked(loadTierDefinitions).mockResolvedValue(mockTierDefinitions as TierInfo[]);
       vi.mocked(loadCycleCap).mockResolvedValue(250000);
 
-      // 理쒕? ?덈꺼, 理쒕? stars, 理쒕? bonusScore
+      // 筌ㅼ뮆? ??덇볼, 筌ㅼ뮆? stars, 筌ㅼ뮆? bonusScore
       const result = await calculateScoreForTier(6, 100, 1000000);
 
       expect(result).toBe(250000 * 100 + 250000 + 1000000);
diff --git a/src/features/quiz/utils/math.ts b/src/features/quiz/utils/math.ts
index 0c1410a..d4cf2e7 100644
--- a/src/features/quiz/utils/math.ts
+++ b/src/features/quiz/utils/math.ts
@@ -1,4 +1,4 @@
-// src/features/quiz/utils/math.ts
+// src/features/quiz/math.ts
 import { Difficulty } from '../types/quiz';
 import { NUMBER_RANGE_BY_DIFFICULTY } from '../constants/game';
 
diff --git a/src/features/quiz/utils/quizTimeCalculator.ts b/src/features/quiz/utils/quizTimeCalculator.ts
index d8a8ba9..c89cfc3 100644
--- a/src/features/quiz/utils/quizTimeCalculator.ts
+++ b/src/features/quiz/utils/quizTimeCalculator.ts
@@ -1,4 +1,4 @@
-import { SURVIVAL_CONFIG, CATEGORY_CONFIG } from '@/features/quiz';
+import { SURVIVAL_CONFIG, CATEGORY_CONFIG } from '../constants/game';
 import { safeAccess } from '@/utils/validation';
 
 interface DynamicTimeLimitParams {
@@ -41,5 +41,5 @@ export function calculateDynamicTimeLimit({
     PRESSURE_FACTOR.START - totalQuestionsAnswered * PRESSURE_FACTOR.DECAY
   );
 
-  return baseTime * currentPressure;
+  return (baseTime as number) * currentPressure;
 }
diff --git a/src/stores/__tests__/useGameStore.test.ts b/src/stores/__tests__/useGameStore.test.ts
deleted file mode 100644
index fe5fe69..0000000
--- a/src/stores/__tests__/useGameStore.test.ts
+++ /dev/null
@@ -1,348 +0,0 @@
-import { describe, it, expect, beforeEach } from 'vitest';
-import { renderHook, act } from '@testing-library/react';
-import { useGameStore } from '../useGameStore';
-
-describe('useGameStore', () => {
-  beforeEach(() => {
-    // Reset store to initial state before each test
-    const { result } = renderHook(() => useGameStore());
-    act(() => {
-      result.current.resetGame();
-    });
-  });
-
-  it('should initialize with default values', () => {
-    const { result } = renderHook(() => useGameStore());
-
-    expect(result.current.score).toBe(0);
-    expect(result.current.combo).toBe(0);
-    expect(result.current.feverLevel).toBe(0);
-    expect(result.current.isExhausted).toBe(false);
-    expect(result.current.showSpeedLines).toBe(false);
-    expect(result.current.showVignette).toBe(false);
-    expect(result.current.activeItems).toEqual([]);
-    expect(result.current.isStaminaConsumed).toBe(false);
-  });
-
-  it('should set score', () => {
-    const { result } = renderHook(() => useGameStore());
-
-    act(() => {
-      result.current.setScore(100);
-    });
-
-    expect(result.current.score).toBe(100);
-  });
-
-  it('should increment combo', () => {
-    const { result } = renderHook(() => useGameStore());
-
-    act(() => {
-      result.current.incrementCombo();
-    });
-
-    expect(result.current.combo).toBe(1);
-  });
-
-  it('should set fever level to 1 when combo reaches 5', () => {
-    const { result } = renderHook(() => useGameStore());
-
-    act(() => {
-      for (let i = 0; i < 5; i++) {
-        result.current.incrementCombo();
-      }
-    });
-
-    expect(result.current.combo).toBe(5);
-    expect(result.current.feverLevel).toBe(1);
-    expect(result.current.showSpeedLines).toBe(true);
-  });
-
-  it('should set fever level to 2 when combo reaches 20', () => {
-    const { result } = renderHook(() => useGameStore());
-
-    act(() => {
-      for (let i = 0; i < 20; i++) {
-        result.current.incrementCombo();
-      }
-    });
-
-    expect(result.current.combo).toBe(20);
-    expect(result.current.feverLevel).toBe(2);
-    expect(result.current.showSpeedLines).toBe(true);
-  });
-
-  it('should reset combo', () => {
-    const { result } = renderHook(() => useGameStore());
-
-    act(() => {
-      result.current.incrementCombo();
-      result.current.incrementCombo();
-      result.current.resetCombo();
-    });
-
-    expect(result.current.combo).toBe(0);
-    expect(result.current.feverLevel).toBe(0);
-    expect(result.current.showSpeedLines).toBe(false);
-  });
-
-  it('should set combo directly', () => {
-    const { result } = renderHook(() => useGameStore());
-
-    act(() => {
-      result.current.setCombo(10);
-    });
-
-    expect(result.current.combo).toBe(10);
-    expect(result.current.feverLevel).toBe(1);
-    expect(result.current.showSpeedLines).toBe(true);
-  });
-
-  it('should set exhausted state', () => {
-    const { result } = renderHook(() => useGameStore());
-
-    act(() => {
-      result.current.setExhausted(true);
-    });
-
-    expect(result.current.isExhausted).toBe(true);
-    expect(result.current.showVignette).toBe(true);
-
-    act(() => {
-      result.current.setExhausted(false);
-    });
-
-    expect(result.current.isExhausted).toBe(false);
-    expect(result.current.showVignette).toBe(false);
-  });
-
-  it('should set stamina consumed state', () => {
-    const { result } = renderHook(() => useGameStore());
-
-    act(() => {
-      result.current.setStaminaConsumed(true);
-    });
-
-    expect(result.current.isStaminaConsumed).toBe(true);
-
-    act(() => {
-      result.current.setStaminaConsumed(false);
-    });
-
-    expect(result.current.isStaminaConsumed).toBe(false);
-  });
-
-  it('should set active items', () => {
-    const { result } = renderHook(() => useGameStore());
-
-    act(() => {
-      result.current.setActiveItems(['safety_rope', 'last_spurt']);
-    });
-
-    expect(result.current.activeItems).toEqual(['safety_rope', 'last_spurt']);
-  });
-
-  it('should consume active item', () => {
-    const { result } = renderHook(() => useGameStore());
-
-    act(() => {
-      result.current.setActiveItems(['safety_rope', 'last_spurt', 'flare']);
-      result.current.consumeActiveItem('safety_rope');
-    });
-
-    expect(result.current.activeItems).not.toContain('safety_rope');
-    expect(result.current.activeItems).toContain('last_spurt');
-    expect(result.current.activeItems).toContain('flare');
-  });
-
-  it('should reset game to initial state', () => {
-    const { result } = renderHook(() => useGameStore());
-
-    act(() => {
-      result.current.setScore(100);
-      result.current.setCombo(10);
-      result.current.setExhausted(true);
-      result.current.setActiveItems(['safety_rope']);
-      result.current.setStaminaConsumed(true);
-    });
-
-    act(() => {
-      result.current.resetGame();
-    });
-
-    expect(result.current.score).toBe(0);
-    expect(result.current.combo).toBe(0);
-    expect(result.current.feverLevel).toBe(0);
-    expect(result.current.isExhausted).toBe(false);
-    expect(result.current.showSpeedLines).toBe(false);
-    expect(result.current.showVignette).toBe(false);
-    expect(result.current.activeItems).toEqual([]);
-    expect(result.current.isStaminaConsumed).toBe(false);
-  });
-
-  it('should handle setCombo with boundary values', () => {
-    const { result } = renderHook(() => useGameStore());
-
-    // Combo 4 (below threshold)
-    act(() => {
-      result.current.setCombo(4);
-    });
-    expect(result.current.combo).toBe(4);
-    expect(result.current.feverLevel).toBe(0);
-    expect(result.current.showSpeedLines).toBe(false);
-
-    // Combo 5 (threshold for level 1)
-    act(() => {
-      result.current.setCombo(5);
-    });
-    expect(result.current.combo).toBe(5);
-    expect(result.current.feverLevel).toBe(1);
-    expect(result.current.showSpeedLines).toBe(true);
-
-    // Combo 19 (below threshold for level 2)
-    act(() => {
-      result.current.setCombo(19);
-    });
-    expect(result.current.combo).toBe(19);
-    expect(result.current.feverLevel).toBe(1);
-    expect(result.current.showSpeedLines).toBe(true);
-
-    // Combo 20 (threshold for level 2)
-    act(() => {
-      result.current.setCombo(20);
-    });
-    expect(result.current.combo).toBe(20);
-    expect(result.current.feverLevel).toBe(2);
-    expect(result.current.showSpeedLines).toBe(true);
-
-    // Combo 0
-    act(() => {
-      result.current.setCombo(0);
-    });
-    expect(result.current.combo).toBe(0);
-    expect(result.current.feverLevel).toBe(0);
-    expect(result.current.showSpeedLines).toBe(false);
-  });
-
-  it('should handle incrementCombo boundary transitions', () => {
-    const { result } = renderHook(() => useGameStore());
-
-    // Increment to 4 (still level 0)
-    act(() => {
-      for (let i = 0; i < 4; i++) {
-        result.current.incrementCombo();
-      }
-    });
-    expect(result.current.combo).toBe(4);
-    expect(result.current.feverLevel).toBe(0);
-    expect(result.current.showSpeedLines).toBe(false);
-
-    // Increment to 5 (transition to level 1)
-    act(() => {
-      result.current.incrementCombo();
-    });
-    expect(result.current.combo).toBe(5);
-    expect(result.current.feverLevel).toBe(1);
-    expect(result.current.showSpeedLines).toBe(true);
-
-    // Increment to 19 (still level 1)
-    act(() => {
-      for (let i = 0; i < 14; i++) {
-        result.current.incrementCombo();
-      }
-    });
-    expect(result.current.combo).toBe(19);
-    expect(result.current.feverLevel).toBe(1);
-    expect(result.current.showSpeedLines).toBe(true);
-
-    // Increment to 20 (transition to level 2)
-    act(() => {
-      result.current.incrementCombo();
-    });
-    expect(result.current.combo).toBe(20);
-    expect(result.current.feverLevel).toBe(2);
-    expect(result.current.showSpeedLines).toBe(true);
-  });
-
-  it('should handle consumeActiveItem with non-existent item', () => {
-    const { result } = renderHook(() => useGameStore());
-
-    act(() => {
-      result.current.setActiveItems(['safety_rope', 'last_spurt']);
-      result.current.consumeActiveItem('non_existent');
-    });
-
-    // Should not throw error and items should remain unchanged
-    expect(result.current.activeItems).toEqual(['safety_rope', 'last_spurt']);
-  });
-
-  it('should handle consumeActiveItem with empty array', () => {
-    const { result } = renderHook(() => useGameStore());
-
-    act(() => {
-      result.current.setActiveItems([]);
-      result.current.consumeActiveItem('safety_rope');
-    });
-
-    expect(result.current.activeItems).toEqual([]);
-  });
-
-  it('should handle setCombo with very large values', () => {
-    const { result } = renderHook(() => useGameStore());
-
-    act(() => {
-      result.current.setCombo(100);
-    });
-
-    expect(result.current.combo).toBe(100);
-    expect(result.current.feverLevel).toBe(2);
-    expect(result.current.showSpeedLines).toBe(true);
-  });
-
-  it('should handle multiple active items consumption', () => {
-    const { result } = renderHook(() => useGameStore());
-
-    act(() => {
-      result.current.setActiveItems(['safety_rope', 'last_spurt', 'flare', 'compass']);
-      result.current.consumeActiveItem('safety_rope');
-      result.current.consumeActiveItem('flare');
-    });
-
-    expect(result.current.activeItems).toEqual(['last_spurt', 'compass']);
-    expect(result.current.activeItems).not.toContain('safety_rope');
-    expect(result.current.activeItems).not.toContain('flare');
-  });
-  it('should reset fever when incrementing combo while exhausted', () => {
-    const { result } = renderHook(() => useGameStore());
-
-    act(() => {
-      result.current.setCombo(10); // Fever Level 1
-      result.current.setExhausted(true);
-      result.current.incrementCombo();
-    });
-
-    expect(result.current.combo).toBe(11);
-    expect(result.current.feverLevel).toBe(0);
-    expect(result.current.showSpeedLines).toBe(false);
-  });
-
-  it('should consume a life', () => {
-    const { result } = renderHook(() => useGameStore());
-
-    const initialLives = result.current.lives;
-    act(() => {
-      result.current.consumeLife();
-    });
-
-    expect(result.current.lives).toBe(initialLives - 1);
-
-    act(() => {
-      // Consume all lives
-      for (let i = 0; i < 10; i++) {
-        result.current.consumeLife();
-      }
-    });
-
-    expect(result.current.lives).toBe(0);
-  });
-});
